### 202607301315

+ round44: 基于振幅的确认旁路 (不增加延迟的误触优化)
+   - 退回 MPR121 debounce 到 dt=1 (用户可在 ConfigApp 自行调整)
+   - 移除 dt>=3 强制检查 (dt=3 经实测会造成快速音符 miss)
+   - 新增: 振幅感知确认旁路 (strongSignal)
+     - 验证时读取 diff = baseline - filtered, 若 diff >= sw_th+4 标记为强信号
+     - 强信号只需 1 个确认周期 (~0.5ms), 弱信号仍需 CONFIRM_CYCLES=2 (~1-2ms)
+     - 效果: 真实触摸 (diff 20-50) 延迟 ~2ms (比 dt=3 的 5ms 少 3ms)
+     - 效果: 噪声尖峰 (diff 5-8) 仍被 CONFIRM_CYCLES=2 过滤
+   - dt=3 导致 miss 的原因: 快速点击时信号可能在 3ms 内波动 (超阈值2ms-> dip 1ms -> 再超2ms),
+     dt=3 要求 3 个连续采样超阈值, 这种波动会被完全过滤 -> miss
+   - 固件版本: 1.3.1
+ 构建：UF2=131584 bytes

### 202607301254

+ round44: MPR121 硬件 debounce 提升 + 监控脚本修复
+   - MPR121 debounce: dt=1→dt=3 (3ms 硬件级滤波), dr 保持 1
+   - 根因分析: dt=1 时仅 1ms 噪声即可触发触摸, 这是误触的主要根源
+   - dt=3 要求 3 个连续采样超过阈值, 过滤短暂噪声尖峰
+   - initMPR121() 中添加 dt>=3 最小值强制检查 (兼容旧配置)
+   - 固件版本: 1.3.0 → 1.3.1
+   - monitor_chain2.ps1: 修复 CSV 引号 bug (单引号→双引号), 5ms 采样率 (200Hz)
+   - DLL 分析确认: chuniio_nyanithm.dll 使用 CDC 串口透传, 不做额外处理
+   - 交接总结修正: "GAME输出为0" 是 CSV 解析 bug 导致的错误结论
+ 构建：UF2=131584 bytes

### 202607301225

+ round43: 全链路监控 CMD_DEBUG_CHAIN(0xC0) + 逐电极校正
+ round42: 逐电极 baseline 校正（游戏中也能运行）
+ round43: 添加全链路诊断命令
+   - 44字节: [6B raw MPR121] [6B verified rawTouch] [32B touchData32]
+   - 可区分误触来源：MPR121/固件处理/DLL
+   - monitor_chain.ps1: 50ms间隔，游戏中实时监控
+ 构建：UF2=131584 bytes
### 202607301218

+ round42: 逐电极 baseline 校正 - 游戏中也能运行
+ 根因：旧校正只在全部36电极空闲时运行
+   游戏中总有电极被触摸 -> 校正永不运行 -> 触摸后 baseline 偏高 -> 乱触
+ 修复：改为逐电极轮询校正
+   - 每秒检查4个未触摸的电极（9秒覆盖全部36个）
+   - 即使游戏中其他键被触摸，未触摸的键仍会被校正
+   - I2C开销：4次/秒 ≈ 0.4ms（可忽略）
+   - 直接解决"触摸释放后 baseline 偏高"的乱触
+ 构建：UF2=131584 bytes
### 202607301206

+ round41: 平衡微调 - 减少 baseline 振荡 + 收紧验证
+ 根因：自动校正(diff>3)与硬件追踪(NCLF/NCLR)互相打架
+   - M2E0 baseline 在 188-208 间振荡（20 LSB）
+   - 振荡期间产生瞬态 diff -> 游戏中乱触
+ 三项调整：
+   1. 验证容差: sw_th-2 -> sw_th-1 (1 LSB，更严但不过严)
+      th_touch=4: verify_th=3 (过滤 diff<3)
+   2. 自动校正阈值: diff>3 -> diff>5 (只在显著偏高时校正)
+      减少与硬件追踪冲突 -> baseline 更稳定
+   3. 校正量: max 3->2 LSB (更温和)
+      减少过冲 -> 减少回弹振荡
+ 预期效果：
+   - baseline 振荡幅度减小 (20-><10 LSB)
+   - 游戏中乱触减少 (验证更严 + baseline 更稳)
+   - miss 不增加 (1 LSB 容差仍允许轻触)
+ 构建：UF2=130560 bytes
### 202607300045

+ round40: I2C 读取错误安全检查 - 防止 filtered=0 导致误判
+ 发现：S53 采样中 M0 所有电极 filtered=0（I2C 读取失败）
+   - diff=200（baseline-0）-> 自动校正会错误降低 baseline
+   - 软件验证会错误通过假触摸（diff=200 >> verify_th）
+ 修复：
+   1. baseline 自动校正：跳过 filtered=0 或 diff>=50 的电极
+   2. 软件验证：filtered=0 时跳过验证，信任 MPR121
+ 构建：UF2=130560 bytes
### 202607300039

+ round39: 软件验证阈值 -2 LSB 容差，减少触摸 miss
+ 问题：轻触/快触 diff=4-5，验证时 filtered 波动降到 2-3 < sw_th=4 -> 被过滤 -> miss
+ 根因：touched() 和 filteredData() 之间有 ~0.2ms 间隔，filtered 波动 ±2-3 LSB
+ 修复：验证阈值从 sw_th 改为 sw_th - 2
+   - th_touch=4: 验证阈值 = 4-2 = 2 (过滤 diff<2，通过 diff>=2)
+   - th_touch=6: 验证阈值 = 6-2 = 4 (过滤 diff<4，通过 diff>=4)
+   - 仍过滤明显假触摸 (diff=0-1，如 MPR121 时序不同步)
+   - CONFIRM_CYCLES=2 进一步过滤短暂噪声
+ 构建：UF2=130560 bytes
### 202607300005

+ round38: 加快 baseline 自动校正 - 分级 proportional 修正
+ round37 分析：0乱触 ✅ 但19个电极 idle diff>=3 (M0E3=11, M2E3=10)
+   - 校正速度 1LSB/2s 太慢，M0E3 需16秒才安全
+ 优化：
+   1. 间隔 2s->1s (2倍频率)
+   2. 分级校正量：diff>3:-1, diff>5:-2, diff>8:-3 LSB
+   - M0E3(diff=11): 11->8->5->3 in 3秒 (之前16秒)
+   - M2E3(diff=10): 10->7->4->2 in 3秒
+   - M0E6(diff=9): 9->6->3 in 2秒
+ 构建：UF2=130560 bytes
### 202607300001

+ round37: 软件基线自动校正 - 根治 baseline 偏高
+ 根因：M2E0 baseline=212 但 idle filtered=207，diff=5 持续触发
+   - 校准时 filtered=213（启动时偏高），-1 margin=212
+   - 稳定后 filtered 降到 207，但 baseline 卡在 212
+   - MPR121 硬件追踪无法下降（噪声重置 NCLF 计数器）
+   - diff=5 >= th_touch=4 -> 持续乱触，软件验证 diff=5>=4 也通过
+ 修复：每2秒空闲时自动校正 baseline
+   - 仅在无触摸时运行（rawTouch 全0）
+   - 读取所有36电极的 filtered + baseline
+   - 如果 diff > 3（baseline 偏高），baseline 寄存器 -= 1 LSB
+   - 每次 Stop/Run 立即生效（CL=11 重载基线）
+   - M2E0: 212->211(2s)->210(4s)->diff=3 停止，乱触消失
+   - 开机后约10秒内自动稳定（需5步 x 2秒）
+   - 只调整偏高的电极，不影响正常电极
+ 构建：UF2=130560 bytes
### 202607292350

+ round36: 找平衡点 - 降低延迟恢复快速音符灵敏度
+ 背景：round35 乱触大幅改善，但快速音符 miss 复发
+   原因：CONFIRM_CYCLES=3 + 每周期验证 = 总延迟 4.5ms（原 2ms）
+   快速音符（5ms）输出窗口从 6ms 缩短到 3.5ms
+ 优化（软件验证使冗余防护可安全降低）：
+   1. CONFIRM_CYCLES: 3->2 (验证已过滤假触摸，无需额外确认周期)
+      - 延迟 -1ms
+   2. 验证优化：只验证新触摸前2周期，持续触摸跳过 I2C 读取
+      - 新触摸: 2次I2C读取（~0.6ms），仅在前2周期
+      - 持续触摸: 0次I2C读取（无开销）
+   3. baseline margin: -2->-1 (验证已防乱触，余量可减小)
+      - 触发diff需求 -1 LSB
+   4. sw_th 最低值: 6->4 (允许用户在ConfigApp设th_touch=4)
+ 延迟对比：
+   round35: ~4.5ms (CONFIRM=3 + 每周期验证 + margin=-2)
+   round36: ~2.3ms (CONFIRM=2 + 仅前2周期验证 + margin=-1)
+   原始:   ~2.0ms (CONFIRM=2 + 无验证 + margin=0)
+ 构建：UF2=130048 bytes
### 202607292325

+ round35: 软件级触摸验证 - 不盲目信任 MPR121 touch status
+ 根因：MPR121 内部触摸状态可能与实际传感器数据不同步
+   - 诊断发现 t2=0x001 但 diff=-5 (filtered > baseline, 不应触摸)
+   - 原因：touch status 来自上一个 ESI 周期，读取时已恢复
+   - 这类假触摸无法通过 baseline 追踪或阈值调整解决
+ 修复：在 updateTouch_v1 中增加触摸验证
+   - 对每个 touched()=1 的电极，读取 filteredData + baselineData
+   - 计算 diff = baseline - filtered
+   - 如果 diff < th_touch，清除该触摸位（假阳性）
+   - 只对已触摸电极做 I2C 读取（通常 0-3 个），开销 ~0-1ms
+ 覆盖所有乱触类型：
+   - baseline drift (diff 不足) ✅
+   - MPR121 时序不同步 (touch status 过期) ✅
+   - 噪声尖峰 (MPR121 误触发但 diff 不支持) ✅
+ 同时创建 monitor_detail.ps1 增强监控脚本
+   - 记录所有 36 电极的 filt/base/diff
+   - 标记 TOUCH (已触发) 和 HOT (diff>=2 接近阈值) 电极
+ 构建：UF2=131072 bytes
### 202607292319

+ round34: 多层防护根治偶发乱触 - 减慢上升追踪+增大余量+增加确认周期
+ 背景：round33 后仍有极低频率乱触（键1, 17等），即使 th_touch=6
+ 根因分析：
+   1. NCLR=14 (上升追踪7ms) -> 噪声尖峰7ms即可推高baseline
+   2. -1 LSB margin 对部分噪声较大的电极不够
+   3. CONFIRM_CYCLES=2 只能过滤1周期(<2ms)噪声
+ 修复（三层防护）：
+   1. NCLR: 14->31 (0x0E->0x1F) 上升追踪 7ms->15.5ms
+      - 噪声需持续15.5ms才能推高baseline（几乎不可能）
+      - 不影响真实触摸（触摸时filtered下降，上升追踪不参与）
+   2. calibrateBaseline margin: -1 -> -2 LSB
+      - baseline = trimmed_mean - 2，更大余量
+      - idle diff = -2 (远离阈值)，真实触摸 diff=28-35 不受影响
+   3. CONFIRM_CYCLES: 2 -> 3
+      - 过滤 <3ms 的噪声尖峰（之前2ms即可通过）
+      - 增加1ms延迟，STRETCH_MS=3 补偿，不影响快速音符
+ 构建：UF2=129024 bytes
### 202607290034

+ round33: 根治 baseline 偏高导致乱触 - 校准余量 + 加快追踪
+ 根因分析：
+   - calibrateBaseline trimmed mean 可能偏高 1-2 LSB（采样噪声）
+   - NCLF=127 下降追踪极慢（需63.5ms连续低于baseline），噪声重置计数器
+   - baseline 只升不降 -> 长期 drift 向上 -> diff 接近阈值 -> 乱触
+ 修复1：calibrateBaseline baseline -= 1 LSB 余量
+   - 确保 baseline 略低于 idle filtered data
+   - idle diff = -1 (低于阈值) 而非 0-4 (接近阈值)
+   - 真实触摸 diff=30-37 不受影响
+ 修复2：NCLF 127 -> 63 (0x7F -> 0x3F)
+   - 下降追踪从 63.5ms 加快到 31.5ms (ESI=0.5ms)
+   - 更快纠正 baseline 向上 drift
+   - MHDF=1 限制每步只变 1 LSB，31.5ms 触摸最多降 1 LSB (可忽略)
+ 构建：UF2=129024 bytes
### 202607290005

+ round32: 修复 MPR0 ELE3 持续乱触 - 提高触摸阈值
+ 根因：60秒全按键监控显示 88% 采样中 MPR0 ELE3 持续触发
+   - baseline=212, filt=208, diff=4 = 恰好等于 th_touch=4
+   - baseline 比 filtered data 高 4 LSB（校准时偏高）
+   - NCLF=127 太慢，baseline 无法追踪下降
+   - diff >= threshold -> MPR121 永久报告触摸 -> 乱触
+ 修复：
+   - th_touch: 4 -> 6（真实触摸 diff=30-37，完全不受影响）
+   - th_release: 3 -> 4（增加迟滞，防止快速 touch/release 循环）
+   - initMPR121() 强制最低 th_touch=6, th_release=4
+     （即使 flash 中保存了旧配置 th_touch=4 也生效）
+ 构建：UF2=129024 bytes
### 202607282344

+ round31: 修复 I2C 竞争条件导致乱触 - 原子 write_read
+ 根因：touched()/readRegister8/16 的 write(寄存器地址)+read(数据) 是两个独立
+   I2C 事务，spinlock 只保护单个事务。Core0/Core1 之间的 I2C 交错会导致
+   MPR121 寄存器指针被改变，读到错误寄存器的数据
+   - 诊断确认：5/9 次触摸是假触摸 (filt=1 而非 ~170)
+   - touched() 读到错误数据返回 0x001 (ELE0 假触摸) -> 乱触
+ 修复：
+   1. 新增 i2c_write_read() 原子操作：单个 spinlock 内完成 write+read
+   2. touched()/readRegister8/16 改用 i2c_write_read()
+   3. touched() I2C 失败时返回 0 (无触摸) 而非可能损坏的数据
+ 构建：完整重编译 19/19，UF2=$((Get-Item 'D:\Opencode,OCCM\Items\Nyanithm_Firmware\Nyanithm_firmware-hw_v1_compatible\build_nb\Nyanithm.uf2').Length) bytes
### 202607282338

+ round30: 降低 I2C 波特率 400kHz -> 200kHz 提升可靠性
+ 诊断确认 round29 CL=11 修复生效：ECR=0xCC (17/20 采样)
+ 剩余问题：I2C 读取错误约 30%（filt=0/base=0），由 400kHz 高速 + Core0/Core1 并发导致
+ touched() 读取失败时返回 0（无触摸）-> 可能导致快速音符 miss
+ 修复：BR_I2C 从 BR400K 改为 BR200K
+   - I2C 通信更稳定，减少读取错误
+   - 触摸轮询增加约 0.2ms（从 ~0.15ms 到 ~0.35ms），仍 >1kHz 轮询
+   - boot_mode.cpp 已使用 BR200K，正常模式现统一为 200kHz
+ 构建：完整重新编译 19/19 文件，UF2=126976 bytes
### 202607282332

+ round29: 改用 CL=11 单步方案修复 ECR 写入丢失
+ 根因：round28 的两步方案 (CL=01 + delay + CL=10) 仍然失败
+   - MPR121 不接受运行时 CL 变更（Stop->Run 后写入 CL=10 被忽略）
+   - 诊断确认：ECR 始终 0x4C，从未到达 0x8C
+ 修复方案：CL=11 单步写入
+   - calibrateBaseline: ECR=0xCC (CL=11, 同时加载基线 + 启用跟踪)
+   - 单次 Stop->Run 转换完成所有操作，无时序问题
+   - 读回验证 + 3次重试确保写入成功
+ writeRegister 自动转换 CL=11 -> CL=10:
+   - 首次 writeRegister 后 ECR 从 0xCC 变为 0x8C
+   - 防止后续 Stop->Run 循环重置基线（CL=11 每次都会重载基线）
+ 构建：UF2=126976 bytes (与 round27b 相同，CL=11 方案更精简)
### 202607282320

+ round28: 修复 calibrateBaseline() ECR 5.5b 写入丢失 - 根因
+ 根因：step 5.5a 写入 ECR=0x4C (CL=01, Stop->Run 加载基线) 后，
+   step 5.5b 立即写入 ECR=0x8C (CL=10, 跟踪模式)，但 MPR121 仍在
+   处理 Stop->Run 转换和基线加载，导致 5.5b 的 I2C 写入被忽略
+ 症状：诊断显示 ECR 永远停留在 0x4C，从未到达 0x8C
+   - 基线跟踪从未启用 (CL=01 而非 CL=10)
+   - 每次 writeRegister() 的 Stop/Run 循环都会重新加载基线寄存器
+   - t2(mpr2)=0x000 占多数采样，触摸检测不稳定
+ 修复：
+   - 5.5a 和 5.5b 之间添加 sleep_ms(2) 等待 Stop->Run 转换完成
+   - 5.5b 添加读回验证 + 3 次重试，确保 ECR=0x8C 写入成功
+   - 检查 i2c_write 返回值，仅在写入成功时进行读回验证
+ 构建：build_nb 和 build_nj 均构建成功，UF2=127488 bytes (+512B)
+ round27b: 修复 stuck-key 阈值永久升高 - 快速音符 miss 的隐藏根因
+ 根因1：BASE_TH=5 硬编码，与配置阈值 th_touch 不一致
+   - 配置 th_touch=4 时，stuck-key 第一次提升直接跳到 6（50%跳变）
+   - 修复：改用 ControllerConfig.th_touch 作为基础阈值
+ 根因2：boostLevel 和阈值在按键释放后永不复位
+   - 按键被卡住 45s 后阈值升高（6/7/8），释放后仍保持高阈值
+   - 后续快速音符因阈值过高而 miss，且永不恢复
+   - 修复：按键释放时若 boostLevel>0，复位 boostLevel=0 并恢复原始配置阈值
+ 构建：完整固件重新编译链接成功，Nyanithm.uf2 已更新
### 202607282210

+ round27: 修复 calibrateBaseline() CL 位操作 bug - cell 17 根因
+ 根因：calibrateBaseline() step 5.4 的 ECR 恢复代码有严重的位操作错误
+   - 代码 `(ecr_backup & ~0x70) | (0b001 << 4)` 本意设置 CL=01（从寄存器加载基线）
+   - ECR 位布局：[7:6]=CL, [5:4]=ELEPROX, [3:0]=ELE_EN
+   - `~0x70`=0x8F 清除 bits 6,5,4；`0b001<<4`=0x10 设置 bit 4
+   - 实际结果：CL=10（跟踪，不加载）+ ELEPROX=01（启用近距离电极）
+   - 导致：写入的基线值从未被加载，ELE11 被误配置为近距离传感器
+   - 诊断确认：ECR=0x9C（错误），应为 0x8C（CL=10, ELEPROX=00）
+ 修复：两步法恢复 ECR
+   - 5.5a: ECR=(ecr_backup & 0x0F) | 0x40 -> CL=01, Stop->Run 加载基线寄存器值
+   - 5.5b: ECR=(ecr_backup & 0x0F) | 0x80 -> CL=10, 直接写 ECR 启用跟踪（无 Stop->Run）
+   - 最终 ECR=0x8C，与 init() 一致；后续 writeRegister 的 Stop/Run 不会重置基线
+   - 同时修复 ELEPROX=01->00，消除 ELE11 误配置
+ 重排 step 5.4/5.5：AUTOCONFIG0=0x00 在 Stop 模式直接写入，消除多余的 Stop/Run 转换
+ 构建：通过无逗号路径联结（D:\NyanithmBuild）成功编译链接，Nyanithm.uf2 已生成
+ 影响：cell 17 (MPR2 ELE0) 基线在启动时立即正确加载，无需等待缓慢的基线跟踪收敛
### 202607282119

+ round26: 修复 I2C 多核并发损坏 - 诊断数据不可靠的根因
+ 发现：i2c_port.cpp 的 i2c_write/i2c_read 无任何互斥锁，Core0(触摸轮询)与 Core1(CMD_DEBUG_RAW诊断)并发访问同一 i2c0 总线，导致 I2C 事务互相损坏
+   - 症状：diag_cell17.ps1 读数剧烈波动（ECR 在 0x00/0x9C 间跳变，cdc0 在 0x00/0x10/0x2B 间跳变，baseline 在 0/204 间跳变）
+   - 早期干净读数（Sample 0: E0 filt=207 base=204 diff=-3）显示 baseline 修复可能已生效，但被并发损坏掩盖
+ 修复：i2c_port.cpp 增加 per-bus spinlock（hardware/sync.h），i2c_write/i2c_read 用 spin_lock_blocking/spin_unlock 串行化
+   - initI2CBus 中 spin_lock_claim_unused + spin_lock_init 初始化锁
+   - CMakeLists.txt 增加 hardware_sync 链接库
+   - 锁仅在 I2C 事务期间持有（~100us），不影响 USB 1ms 时序
+ 编译验证：build_nb ninja 通过（CMake 重新配置 + i2c_port.cpp 重编译 + 链接），Nyanithm.uf2 已生成（21:19, 131584B）
+ 注意：此修复不改变游戏内实时触摸行为（游戏中 Core1 不做 I2C），但使 CMD_DEBUG_RAW 诊断数据可靠

### 202607282037

+ round25: 修复 cell 17（MPR2 electrode 0）无响应 - 根治 calibrateBaseline Stage 4 单电极跳过
+ 根因深化：round24 修了 Stage 2（整体跳过）路径，但 cell 17 仍失效，因为真正根因是 Stage 4 单电极跳过
+   - calibrateBaseline() Stage 4: if (mx - mn > 3) continue 对 jitter>3 LSB 的电极置 valid=false
+   - Stage 5.3 跳过该电极 baseline 写入 -> baseline 保持上电默认值 -> 触摸 delta 永不达标 -> 永久无响应
+   - 精确解释"只有17号键失效"：MPR2 ELE0 信号弱/抖动大被单独跳过，而 ELE1-7（cell 19,21,...,31）正常
+   - 若是 Stage 2 整体跳过，整个 MPR2 都会失效，与症状不符
+ 修复：mpr121.cpp calibrateBaseline() 移除 if (mx - mn > 3) continue
+   - 所有电极（含弱信号 ELE0）现在都写入 trimmed-mean baseline（已去 min/max，抗瞬态噪声）
+   - best-effort baseline 仍远优于上电默认值（后者=永久无响应）
+ 编译验证：build_nb ninja 通过，Nyanithm.uf2 已生成（20:37, 131072B）
+ 验证方法：烧录后用 CMD_DEBUG_RAW(0xBE) 读 MPR2 ELE0 baseline，应接近 filtered idle 值（非0）；触摸 cell 17 应触发 touch status bit0
+ 备份：backup_pre_cell17_baseline_fix_20260728/

### 202607281952

+ round24: 修复 cell 17（MPR2 electrode 0）无响应 + 诊断命令
+ 根因：init() 后 auto-config 未完成充电电流校准即被 calibrateBaseline() 禁用，部分电极 CDC=0 无法检测触摸
+   - calibrateBaseline() 阶段2检查 touched()!=0，auto-config 噪声可能导致检查失败 -> 跳过基线标定
+ 修复：initMPR121() 中 init() 后增加 200ms 延迟让 auto-config 完成充电电流校准
+   - 然后在 calibrateBaseline() 之前禁用 auto-config（AUTOCONFIG0=0x00），避免噪声干扰标定
+   - calibrateBaseline() 步骤5.5仍写入0x00（无害，已是0x00）
+ 新增：CMD_DEBUG_RAW(0xBE) 诊断命令，返回 MPR121 原始数据（touch status/filtered/baseline/registers）
### 202607281939

+ round23: 优化快速音符检测 + 修复 cell 17 无响应
+ 用户反馈：快速音符已改善但需继续优化；cell 17（MPR2 electrode 0）无响应
+ 修复1：CONFIRM_CYCLES 3->2
+   - autoconfig 已在 round22 禁用，周期性噪声已消除，2 周期足够过滤 1 周期随机噪声
+   - 确认延迟从 ~2.4ms 降至 ~1.2ms，弱信号电极（如 cell 17）更容易被确认
+   - 5ms 快速触摸的游戏捕获率：96.7% -> 99.95%（120fps, ±3帧窗口）
+ 修复2：STRETCH_MS 2->3
+   - 补偿确认延迟，5ms 触摸的游戏可见时间：4.6ms -> 6.0ms
+   - 3ms 展宽远小于游戏帧间隔（8.33-16.67ms），不会导致 cell 间"渗透"
+ Cell 17 根因分析：CONFIRM_CYCLES=3 过滤了弱信号电极，降至 2 后应恢复响应
### 202607281835

+ round22: 修复周期性触摸误触（autoconfig 根因）
+ 症状：CONFIRM_CYCLES=3 后仍有规律性误触，每隔一段时间突然触发一次
+ 根因：MPR121 autoconfig（ACE=1）在 calibrateBaseline() 后仍持续运行
+   - calibrateBaseline() 步骤 5.5 仅清除 BVA（基线不自动调整），但 ACE=1 未关闭
+   - autoconfig 周期性重新校准充电电流（CDC），导致电容读数跳变 -> 假触
+   - 假触持续多个扫描周期，超过 CONFIRM_CYCLES=3 窗口，被确认输出
+ 修复：calibrateBaseline() 步骤 5.5 改为完全禁用 autoconfig（AUTOCONFIG0=0x00）
+   - ACE=0：停止周期性充电电流重校准
+   - 初始标定的 CDC 值被冻结，适用于游戏控制器的稳定环境
+   - 如环境变化（温度/湿度），重启设备即可重新标定
+ 保留：CONFIRM_CYCLES=3、STRETCH_MS=2、rawTouch[3] 等所有 round21 修改
+ 验证：COM1 串口监控 3 分钟 0 次误触（修复前 10 秒 35 次），根因确认
### 202607281815

+ round21: 修复 debounce=1 时 ~2ms 误触问题
+ 根因：Round20 展宽逻辑存在设计缺陷 -- 触摸在确认前就立即输出（stretched[m] |= bit 在 CONFIRM_CYCLES 检查之前执行），导致 2ms 噪声脉冲也被输出并展宽
+ 修复1：触摸仅在 CONFIRM_CYCLES=3 连续周期确认后才输出，未确认的触摸不到达游戏端
+   - CONFIRM_CYCLES 2->3：过滤 2ms 噪声脉冲（主循环 ~1.2ms/周期，3 周期 ≈ 3.6ms 确认窗口）
+   - stretched[m] |= (1 << e) 从确认前移到确认 if 块内
+ 修复2：消除 stuck-key 检测中的冗余 I2C 读取
+   - 原代码在 updateInputState() 中再次调用 mpr0/1/2.touched()（3 次额外 I2C 事务），现已改用 updateTouch_v1() 已读取的 rawTouch[]（完整16位，非touchData[]的8位截断）
+   - 效果：主循环 I2C 时间减半（6 次读取 -> 3 次），循环周期从 ~1.7ms 降至 ~1.2ms，提升响应速度
+   - 新增文件作用域 static uint16_t rawTouch[3]，保存展宽前的完整16位原始触摸值
+   - 不能用 touchData[]（uint8_t[4]），否则截断高位丢失 electrodes 8-11
+ 配置建议：保持 debounce=0x11（dt=1/dr=1），固件层 CONFIRM_CYCLES=3 已可过滤 2ms 噪声
+ 保留：STRETCH_MS=2、CONFIG1=0x10、CONFIG2=0x28、NCLF=0x7F、unified 阈值、自适应阈值、上电基线校准、粘滞恢复
+ 不改 cfgVer/配置结构体，ConfigApp 兼容性不变
### 202607280021

+ round20: 触摸脉冲展宽修复快速音符（大划/折返划X 型窄音符）miss
+ 根因：debounce=0x44 + FFI=6 总延迟7-8ms，手指每 cell 仅停留2-5ms，MPR121 未置位即离开；游戏300-500Hz 轮询可能采样不到极短激活窗口
+ 方案：在 updateTouch_v1() 中实现确认展宽逻辑，不改MPR121 滤波参数
+   - CONFIRM_CYCLES=2：连续2 周期检测到才标记为已确认触摸，单周期噪声不展宽
+   - STRETCH_MS=2：已确认触摸释放后继续保留2ms，确保游戏轮询至少采样1 次
+   - 实时触摸立即输出（无延迟），仅对释放做展宽
+ defaultConfig.th_touch 6->4：与设备实测安全阈值一致（用户反馈阈值为 3 可能乱触， 稳定）
+ 保留：CONFIG1=0x10/CONFIG2=0x28/NCLF=0x7F、unified 阈值、自适应阈值、上电基线校准、粘键恢复
+ 不改 cfgVer/配置结构体，ConfigApp 兼容性不变
+ 配合设备配置修改：th_touch=4/th_release=3/debounce=0x11（用户经 ConfigApp 手动设置）
### 202607241000

+ round19: 修复快速音符模式 miss（flick/折返划/X 型/斜向窄音符）
+ 根因：MPR121 滤波+去抖总延迟 ~8ms，对 <5ms/cell 的快速音符几乎必然丢失
+ CONFIG1 0x10->0x90: FFI 6->2 样本，第一滤波延迟 ~3ms->~1ms
+ CONFIG2 0x20->0x29: SFI 4->2 样本 + ESI 1ms->0.5ms，第二滤波延迟 ~2ms->~0.5ms，所有时序减半
+ defaultConfig.debounce 0x33->0x11: dt=3/dr=3 -> dt=1/dr=1，配合 ESI=0.5ms 实现去抖 3ms->0.5ms
+ 总延迟 ~8ms -> ~2ms，可捕获 2-3ms/cell 的快速触摸
+ 保留 round5 NCLF=0x7F 慢速滑动修复、unified 阈值 5/5、自适应阈值、上电基线校准
+ 不改 cfgVer/配置结构体，ConfigApp 兼容性不变；现有 config.bin 中 debounce 字段需 ConfigApp 手动改 0x11 或擦除配置加载默认
+ Slide 音符非主要修复目标，作为附带收益

### 202607191155

+ 修复游戏内滑条粘键：移除 normalMode 在 !game_connected 时的 sleep_ms(10)。根因：游戏经 HID 键盘路径读滑条(touchData[4]，CFG1 启用，无 CDC 故 game_connected=false)，sleep 把 updateInputState 降到 100Hz，touchData[4] 滞后真实触摸最多 10ms，快速滑动时 HID 报告持续重发陈旧"按下"位 = 粘键。面板走 CDC(game_connected=true 不走 sleep)故正常。现恢复全速，touchData[4] 与 touchData32 同样新鲜。

### 202607191130

+ 紧急修复 LED 异常闪烁:normalMode 待机时移除 update_rainbow_frame()。根因:待机(game_connected=false)时彩虹(Core0)与 LampArray/Windows动态光(Core1)并发调用 RGB_LED.show()，双核同时驱动 WS2812 破坏时序导致闪烁。现待机灯效由 LampArray 独占。

### 202607191101

+ 新增 CMD_GET_VERSION(0xBD):返回固件版本/API/HW版本/编译日期时间/SDK版本,ControlPanel 可读取设备信息
+ 修复断连后无法自动回到待机:normalMode 在 game_connected=false 时恢复彩虹待机动画(10ms 限速)
+ ControlPanel 输入轮询 UI 渲染改用 requestAnimationFrame 解耦,轮询帧率从 ~90fps 提升至 ~150fps(匹配/超过 120fps)
+ ControlPanel 智能连接:优先 VID/PID 自动匹配,匹配失败回退手动选择串口

### 202607190600

+ 调整 MPR121 NCLF 寄存器 0x3F->0x7F,减缓 falling 方向基线追踪速率
+ 修复慢速平移(<2cm/s)及轻压时电极漏触发的问题
+ 缓解长时间按压后释放延迟
+ 保留 round1 已验证的滑动响应特性(rising 方向参数未动)
+ 不影响 CONFIG1/CONFIG2/autoconfig 校准,与 ConfigApp 配置(th_touch/th_release/debounce)兼容
+ 仅针对 MPR121 调参,CY8CMBR3116 路径不受影响

### 202603161921

+ 修复上个版本无法正确读取配置文件的问题

### 202603101301
+ 支持被ConfigApp检测手台固件版本

### 202603041716
+ 支持Windows动态光效
+ 不兼容旧版本ConfigApp

### 202603021324
+ 支持直接进入下载模式

### 202602072018
+ 使用chuniio_nyanithm.dll与游戏通信
+ 不需要指定串口为COM1
+ 不兼容旧版ConfigApp

### 202602031615
+ 修复之前若干版本ToF传感器已知问题
+ 支持降低缝隙灯光亮度

### 202601220447
+ 支持在游戏未运行的时候直接连接手台进行配置，不需要按住按钮启动

### 202601120058
+ 支持限制灯光亮度

### 202601012233
+ 支持v2硬件

### 202512262212
+ 支持修改CY8CMBR3116配置
+ 修复3116触摸的4k/6k模式bug

### 202512150206
+ 弃用硬件完整性检测
+ 支持4k和6k模式，通过开机时按钮向下选择
+ 别的忘了

### 202512052357
+ 通过配置文件选择手台型号
+ 修改了逻辑适配32寸的第五个ToF传感器
+ 修复了触摸阈值只对mpr0生效的问题
+ 支持空闲时禁用Air输入
+ 空闲时炫彩灯光


### 202511261846
+ 修改了配置信息结构体定义, 适配32寸
+ 不兼容旧固件、配置

### 202511252235
+ 支持32寸的额外一个ToF传感器

### 202511160247
+ 支持选择是否启用Slider的键盘输入
+ 调整灯光测试变色速度
+ 移除了ToF传感器超时检查，可能是潜在的4093解决方法
+ 在读取设备信息时检查ToF传感器状态，显示警告
+ 设备上电时自检
+ 进入警告状态时按下拨轮可以强制跳过

### 202511140027
+ 支持切换为CY8CMBR3116触摸
+ 显示设备信息时输出未经过修正的测距值
+ 进入调试模式时默认不开启HID上报，防止错误配置导致的意外输入

### 202510290104
+ 支持设置灯珠数量，以兼容只有16灯的固件

### 202510260046
+ 游戏内自动重置设备，尝试解决ToF延迟问题（测试）

### 202510242223
+ 支持调节去抖

### 202510241912
+ 新增拨轮映射到功能键 (投币, 刷卡, TEST)

### 202607301549
+ round45: 振幅分级确认 + 短暂波动容忍(不增加硬件 dt)
+   - 背景: dt=1 乱触 vs dt=2 miss 的死结;硬件 dt 是 4-bit 整数且 ESI 已 1ms 最小,无法取小数
+   - 方案 A(振幅分级确认): 用 diff 振幅替代时间盲判
+     - diff >= th_touch+4: 1 周期确认(保留 round44 强信号快速通道,快速音符不漏)
+     - diff >= th_touch+2: 2 周期确认
+     - diff <  th_touch+2: 3 周期确认(弱/边界信号更严,滤噪声乱触)
+   - 方案 B(波动容忍): touchCount 严格连续 -> dipGrace 缓冲
+     - 已确认触摸的短暂波动(dip)不清零 touchCount,容忍 DIP_TOLERANCE_CYCLES=2
+     - 直接治 dt>=2 时"超阈值2ms->dip1ms->再超2ms"被连续采样过滤导致的 miss
+   - 硬件 dt 保持 1,不牺牲快速音符捕获率
+   - 文件: src/hw_devices.cpp updateTouch_v1()
+   - 备份: backup_pre_ampconfirm_diptol_20260730_150005/
### 202607301618
+ round45b: 电极级差异化阈值(根治 idle 噪声型乱触)
+   - 诊断: chain_log_160528 显示 cell 17(MPR2 ELE0)乱触;detail_log 分析 423 idle 样本
+   - 根因: M2E0 idle diff max=13 远超 th_touch=6(4次>=6),MPR121 硬件层 false touch
+   - 方案 A 缺陷: 振幅分级假设"高 diff=真实触摸",但 idle 噪声尖峰 diff=13 > th+4=10
+     反被判为"强信号"走 1 周期快通道,verify_th=5 也挡不住
+   - 解法: electrodeBaseTouchTh() 给 7 个高危电极单独设高 touch threshold(>其 idle max)
+     M2E0=16, M2E3=12, M2E1/M0E5/M0E6=11, M1E7/M2E2=10;其余电极保持全局 th_touch=6
+   - stuck-key boost/restore 同步改为电极级 base_th,避免释放后覆盖回全局 6
+   - 真实触摸 diff 20-50 远高于这些阈值,不受影响
+   - 文件: src/hw_devices.cpp (electrodeBaseTouchTh + initMPR121 + stuck-key)
+   - 备份: backup_pre_electrode_th_20260730/ (含 A+B 版本)
### 202607301723
+ round45c: 移除 dipGrace 容忍(修复卡键/占用未释放)
+   - 诊断: round45b 实机出现"触摸占用/未释放"(卡键)+ 轻微乱触
+   - 根因分析: 方案B 的 dipGrace 对所有已确认触摸展宽(2周期~6ms + STRETCH 3ms),
+     把短暂 false touch/单点噪声放大成"占用",是卡键最大嫌疑
+   - 解法: 移除 dipGrace,回到 round44 验证过的纯 STRETCH_MS=3
+   - 保留: 振幅分级确认(方案A: 强1/中2/弱3周期)+ 电极级差异化阈值(round45b)
+   - 用户反馈"如果只调整阈值会自己去 configapp",故固件聚焦逻辑修缮,阈值保留电极级
+   - 文件: src/hw_devices.cpp updateTouch_v1()
+   - 备份: backup_pre_dipgrace_removal_20260730/ (含 round45b 版本)
### 202607301726
+ round45d: 加速 stuck-key 恢复(卡键防御,45s->10s)
+   - 背景: round45c 移除 dipGrace 后,若仍有硬件层持续 false touch 导致卡键,
+     stuck-key 45s 触发太慢(完全恢复需 135s)
+   - 改动: 触发时间 45000ms -> 10000ms(10s/+1, 20s/+2, 30s/+3)
+   - 安全性: CHUNITHM 长音<5s,且 boost+1 只提高阈值1,真实触摸 diff 20-50 不受影响,
+     只让 false touch(diff 接近阈值)释放
+   - 包含: round45c 全部(振幅分级+电极级阈值+纯STRETCH) + stuck-key 加速
+   - 文件: src/hw_devices.cpp
+
+### 20260801 round45j-restore: 回退到 round45j 基线 (v1.3.1)
++ 背景: round46a-v 系列叠加防塌陷/恢复器/sticky/验证窗口等复杂机制后,
++   锁手/长按断触仍未根治; 用户要求回退到 round45j 重新分析根因
++ 回退方法 (源码级重建, 基座 = backup_pre_round46_20260801_141948 全树):
++   - src/hw_devices.cpp <- backup_pre_th_lower_20260730 (round45f 快照)
++     + 应用 round45g (阈值余量 +4->+2: M2E0 16->15, M2E3 12->10,
++       M2E1/M0E5/M0E6 11->9, M1E7 10->8, M2E2 10->8)
++     + 应用 round45h (M2E2 8->9, 新增 M2E4=7)
++     + 应用 round45i (baseline 校正: 1s->0.5s, 4->8 电极/周期,
++       diff>5->>3, adj>10?2:1->>6?2:1, correctionIndex +4->+8)
++     + 应用 round45j (空间过滤: confirm 时检查同 MPR 相邻电极,
++       孤立单 cell +2 确认周期)
++   - src/mpr121.cpp: NCLF 0x7F->0x3F (round45k 之后才改的, 回退)
++   - src/chuni_io.cpp: CMD_DEBUG_CHAIN 改回直接 I2C 读取
++     (round45p 的 hwTouch 快照是 45j 之后才加的), 删除 CMD_DEBUG_ALL
++   - 其余 src/include 文件与 45u 快照逐一字节比对一致, 零改动
++ 验证: 构建成功, UF2 = 131584 字节, 与原始
++   Nyanithm_round45j_spatial.uf2 (131584) 大小完全一致;
++   二进制差异仅为编译时间戳/SDK 版本 (Aug 1 22:02 vs Jul 31 18:23)
++ 备份: round46v 源码 -> ..\backup_round46v_20260801_215853\
++ 构建: Nyanithm_round45j_restored.uf2 (131584 bytes)
++ 文件: src/hw_devices.cpp, src/mpr121.cpp, src/chuni_io.cpp
++ 说明: 本次为纯回退, 不含任何新修复; 预期行为局限 (round45j 已知):
++   - NCLF=63 时超长按 baseline 塌陷更快 (round45k 才改 127)
++   - 空间过滤对孤立单 cell 真实触摸 +2 周期 (~6ms) 延迟
++   - 无 sticky / dipGrace 恢复 (round45n/l 之后才有)
++   - stuck-key 10s 加速 (round45d), 超长 hold 可能被 boost

### 20260801 诊断升级 (round46l-diag 阶段)
+ round46l-diag: 诊断脚本升级 - HOLD_RAW_DIP 长按掉线检测 (不动固件)
+   - 背景: round46k 实测仍有锁手/长按断触; 代码分析指向防塌陷时序追不上
+     NCLF=127 塌陷速率 (重置周期2s vs 8LSB余量<1s耗尽; 启动延迟3s裸奔;
+     stuckStart 一帧掉线即清零). 需实测数据确认后再改固件
+   - monitor_chain3.ps1: 新增 HOLD_RAW_DIP 事件
+     * raw 在游戏保持期间掉线(塌陷临界/sticky兜底) -> dip 恢复瞬间记
+       "dip=Nms recovered" + per-cell 统计(count/max/sum)
+     * dip 持续 >500ms 每 500ms 记 "dip=Nms ongoing" (防静默, 捕捉断触时刻)
+     * dip <10ms 忽略 (采样抖动); 正常释放(无 raw 恢复)不计 dip, 由 HOLD_END 覆盖
+     * 统计块新增 "Hold raw dips" 段落
+   - analyze_chain.py: 新增 --events 解析 (此前参数未使用)
+     * 解析 chain3_events_*.csv 的 HOLD_RAW_DIP/HOLD_END
+     * 报告 "Lock-hand raw dip analysis" 段落: per-cell count/max/avg/total、
+       每 hold 秒 dip 频率、dip 间隔周期特征 (spacing~2000ms => 2s防塌陷周期证据)
+     * top5 最严重 cell
+   - 文件: monitor_chain3.ps1, analyze_chain.py
+   - 待办: 用户锁手专项实测 -> 若 dip 呈 2s 周期/0.5-1s 时长即证实根因
+     -> 实施 round46l: 事件驱动防塌陷(150ms查按住电极/diff<bth+6即重置/
+     启动延迟500ms) + stuckStart保留资格 + A/C去冲突 + 验证窗口60ms +
+     spatial加速加diff门槛

### 20260801 round46l: 事件驱动防塌陷 (v1.4.0)
+ 实测确认 (chain3_events_185102 模拟操作, 185006 静置):
+   - 静置 10min: 0 事件 (无静置误触)
+   - 模拟操作: 46 个独立 raw 掉线时段, 时长 p50=1052ms/max=4363ms,
+     间隔 p50=2412ms (~2s 防塌陷周期吻合); 66 条 GAME_NO_RAW; cell 27
+     完整断触时间线 (按住 1.34s 后 game 释放, 玩家手未动)
+   - 根因: NCLF=127 塌陷 1 LSB/127ms, round46k 防塌陷余量仅 bth+4=8 LSB
+     (~1s 耗尽) + 2s 重置周期 + 3s 启动延迟 -> 50% 时间掉线
+ 修改 (src/hw_devices.cpp, 全部不动阈值):
+   1) 事件驱动防塌陷: 删除 3s 延迟 + 2s 周期定时, 改为每 100ms 只查
+      2s 内触摸过的电极 (lastTouchedMs 窗口, raw 掉线不清零保护),
+      diff < bth+10 立即重置 baseline = filt+bth+10 (~10-14 LSB 余量
+      => raw 位在真实按住期间不再清除; 塌陷 14 LSB 需 1.8s >> 100ms 周期)
+   2) 验证保留窗口 50ms -> 60ms (300Hz 下 sticky dip 15 cycles=50ms)
+   3) auto-correct: skip 2s -> 10s (与释放恢复窗口对齐, 不再打架);
+      启动阈值 diff>3 -> diff > bth+2
+   4) slide 加速 (round45u): 邻居活跃需另加 diff >= th+2 门槛
+      (防锁手串扰直通, 之前邻居活跃直接跳过二次验证)
+   - 构建: Nyanithm_round46l_event_driven_anticollapse.uf2 (137216 bytes)
+   - 文件: src/hw_devices.cpp

### 20260801 round46m: 防塌陷写入频率修复 (v1.4.0)
+ 实测反馈 (round46l): 所有按键触摸后 ~280ms 即释放, 长按完全失效
+ 诊断 (diag_regs.py 直读 + 事件分析):
+   - 静止直读基线正常 (712-720), TH=10/REL=9 确认 ConfigApp 值
+   - round46l 防塌陷写条件 diff<bth+10 (=20) 过宽 + 100ms 周期
+     -> 正常浅按 (diff 15-20) 也被持续重写, 写入频率 ~10 次/s
+   - 每次写 baseline 都经 writeRegister 的 Stop->Run 循环, 短暂打断
+     MPR121 触摸检测 -> 周期性释放 (~280ms, 与实测吻合)
+ 修改 (src/hw_devices.cpp):
+   1) 防塌陷: 检查周期 100ms -> 500ms; 写条件收紧为
+      0 < diff < bth (只有塌陷临界才写); 写后余量 bth+8 (=18)
+      -> 写入频率 ~1 次/s (与 round46k 相当, Stop->Run 打断可忽略)
+   2) auto-correct: readRegister8 失败返回 0 时会写 baseline=0
+      (NCLR 恢复需 ~20s) -> bl_reg <= adj 时跳过写
+   - 构建: Nyanithm_round46m_safe_anticollapse.uf2 (137216 bytes)
+   - 新增诊断工具: diag_regs.py (直读 CMD_DEBUG_ALL, 静止/触摸状态
+     检查 filt/base/diff/配置寄存器)
+   - 文件: src/hw_devices.cpp, diag_regs.py

### 20260801 round46n: 防塌陷写点安全化 (v1.4.0, 模拟验证)
+ 深入探究 (用户未刷 round46m 前的模拟验证):
+   - 编写 test_anticollapse_sim.py: 模拟 NCLF=127 塌陷 (1 LSB/127ms) +
+     NCLR=0x1F 上升追踪 + 三种防塌陷策略, 60s 长按 3 场景 (深按/浅按/微动)
+   - round46k 复现: 每 2s 周期释放 30 次 (与实测 dip 间隔 ~2.4s 吻合)
+   - round46l 复现: 写频率 6 次/s (每 167ms 一次 Stop->Run 中断
+     = 实测 280ms 释放机制)
+   - **round46m 缺陷**: 写点 diff<bth (=10) 太靠近释放阈值 (9),
+     写后 diff=18 塌到 9 需 1.14s > 检查相位间隔 -> 每 ~1.7s 释放一次
+     (模拟 35-43 次/60s) - round46m 未交付前即发现并修复
+   - round46n 设计: 写点 bth+8 (=18), 写后目标 bth+12 (=22),
+     检查 500ms -> 写前最低 diff 11.1 (>9), 写频率 ~1/s
+     (模拟: deep/light 0 释放, wobble 61s 内 1 次边缘)
+ 修改 (src/hw_devices.cpp):
+   - 防塌陷写条件: 0 < diff < bth+8; 写目标 filt+bth+12
+   - 其余保持 round46m (500ms 周期, held 2s 窗口, diff>0 释放防护,
+     auto-correct 防写 0)
+ 审查结论:
+   - I2C 双核竞争: 已由 i2c_port.cpp per-bus spinlock 解决 (round46b),
+     快照 base 异常非竞争所致
+   - spatial 门槛 (round46l): 强 flick (diff>=15) 走最快路径无回归,
+     弱信号保守是防串扰正确权衡 -> 保留
+   - C 机制 (释放后恢复): 只写释放后电极, 不打断当前触摸 -> 保留
+   - 新增工具: test_anticollapse_sim.py (防塌陷时序模拟器)
+   - 构建: Nyanithm_round46n_safe_anticollapse.uf2 (137216 bytes)
+   - 文件: src/hw_devices.cpp, test_anticollapse_sim.py

### 20260801 round46o: Air 预测条件修复 (v1.4.0)
+ 深入探究 (用户历史反馈: "air 音符偏慢/偏向 late justice"):
+   - 审查 updateAir 预测逻辑: 条件 `approach >= gap` 比较了不匹配的单位
+     - approach = 手每 3.3ms 周期位移 (mm), 普通手速 100-500mm/s
+       = 0.3-1.7mm/周期
+     - gap = 剩余距离 (mm, 最多 AIR_PREDICT_MM=10)
+     - 需 approach >= gap => 手速 >= 3000mm/s 才可能触发 => 预测几乎
+       永不生效, air 只能靠"进入范围后的 2-3 周期确认" (延迟 6.6-10ms
+       + 手到达范围的时间) => 玩家感知 air 偏 late
+   - 修复: 去掉 approach >= gap, 保留 approach > 0 (只预测上升, 防
+     低空 swipe 误触), 预测窗口 10mm -> 15mm; 2-3 周期确认机制不变
+     (单周期噪声/横向 swipe 仍被过滤)
+   - 预期: air 提前 ~10-30ms 触发 (视手速), 缓解 late justice
+   - 构建: Nyanithm_round46o_air_predict_fix.uf2 (137216 bytes)
+   - 文件: src/hw_devices.cpp

### 20260801 round46p: 验证保留窗口安全化 (v1.4.0)
+ 深入探究 (实际 loop 周期测量):
+   - 直读 CMD_DEBUG_TELEMETRY (0xC1): loopMinUs=1344 loopMaxUs=25417
+     loopSumUs=1649653605 loopCount=609937 -> 平均 2.70ms (~370Hz)
+   - 主循环无 sleep, 周期由 I2C 事务 + WS2812 LED 更新决定; max 尖峰
+     25.4ms (I2C 重试/LED 突发)
+   - sticky dip 最长 = 15 cycles x 2.7ms ~= 40ms; round46l 的 60ms 窗口
+     在 370Hz 下勉强够, 但 max 尖峰 25ms + 未来 loop 变慢风险
+ 修改 (src/hw_devices.cpp):
+   - 验证保留窗口 60ms -> 100ms (保守覆盖 200-370Hz + 尖峰; 快速重触
+     跳过验证, 副作用可忽略)
+   - 构建: Nyanithm_round46p_verify_window_fix.uf2 (137216 bytes)
+   - 文件: src/hw_devices.cpp

### 20260801 round46q: 释放后恢复器修复 (v1.4.0, 子智能体审查 + 模拟验证)
+ 独立代码审查 (子智能体 Faraday) 发现高风险路径:
+   - 强按 (diff 30-50) 长按期间, 防塌陷写 baseline = filt_hold+bth+12
+     -> 释放后 filt 回升越过 baseline, 残留塌陷 -8..-28 LSB
+   - 旧恢复器 (round46k): 300ms 门槛 + 每 1s 只查 8 电极 (~4.5s 轮转)
+     + 写目标 filt+bth+4 (idle diff 14 -> 粘键感)
+     -> 释放后 100-300ms 内的快速第二击 (轻按) 验证失败/触发不足
+     -> 用户实测"持续点击中突然释放且短时间无法有效输出"的机制
+ 修复 (src/hw_devices.cpp):
+   1) 检查周期 1000ms -> 100ms; 门槛 300ms -> 100ms
+   2) urgent 优先: 每次先扫"释放后 100-500ms"的电极 (最多 8 个),
+      再补轮转批次 (去重), 快速第二击窗口被覆盖
+   3) 写目标 filt+bth+4 -> filt+bth-3 (idle diff ~7 < 阈值 10,
+      不粘键; 轻按第二击 (>=3 LSB) 立即触发)
+ 模拟验证 (test_retap_sim.py, 6 随机相位):
+   - old 恢复器: 第二击触发延迟 avg 330-513ms / 部分失败
+   - round46q: avg 2-3ms, 0 失败
+   - 构建: Nyanithm_round46q_retap_fix.uf2 (137216 bytes)
+   - 文件: src/hw_devices.cpp, test_retap_sim.py

### 202607310900
+ round45u: 修复slide移动中miss (slide过渡加速 + 空间过滤修复)
+   - 问题: slide移动过程中突然miss, 手指从cell A滑到cell B时B未及时触发
+   - 根因1(空间过滤): 空间过滤检查raw[m](硬件touch bit)判断是否有邻居
+     slide过渡时cell A的硬件bit=0(sticky维持输出), cell B被误判为孤立->+1确认周期
+   - 根因2(验证延迟): cell B需2次I2C验证(~5ms), 即使邻居活跃也不加速
+   - 修复1: 空间过滤改用prevStretched[](含sticky输出), slide过渡时cell A仍算邻居
+   - 修复2: 邻居在上一周期活跃时, 跳过第二次I2C验证(slide过渡加速)
+     cell B: 1次验证+1确认 = ~2.6ms (原: 2验证+2确认+1空间 = ~13ms, 快5倍)
+   - 效果: slide过渡延迟从~13ms降至~2.6ms, cell A的sticky(21-39ms)完全覆盖
+   - 安全性: 邻居活跃=手指在附近, 触摸极可能是真实的; 验证仍检查diff
+   - 含 round45t 全部(跨核竞态修复+Air确认+sticky+flick+baseline冻结)

### 202608011500
+ round46: 全链路诊断版 + 统一阈值变体 (v1.4.0)
+   - 跨核竞态彻底修复(seqlock): round45t 只修了 HID 路径(touchData[4]),
+     CDC 路径(touchData32[32]+airKeys[6], 即游戏实际使用的 DLL 轮询路径)
+     仍是无同步读写 -> DLL 1ms 轮询可能读到新旧混合帧, 造成游戏内
+     突发漏键/误触(monitor 5ms 采样看不到此 1ms 撕裂)
+     修复: Core0 在 updateInputState 首尾翻转 touchStateGen (odd/even),
+     Core1 的 CMD_GET_INPUT / CMD_DEBUG_CHAIN / HID 全部在偶数代次快照内拷贝
+   - 新增 CMD_DEBUG_TELEMETRY (0xC1): 游戏运行期间串口被 DLL 独占,
+     Core1 记录每次 CMD_GET_INPUT 响应的边沿事件+计数(served/rise/fall/
+     verifyFail/CDC字节数/Core0循环周期), 游戏结束后回读即可还原
+     "固件实际发给 DLL 的数据" (528B 固定报文)
+   - 新增 CMD_DEBUG_DIFF (0xC2): 42B 紧凑诊断 (6B raw + 36x diff),
+     115200 波特率下 ~3.7ms/帧, 支持 5ms 采样校准
+   - UNIFIED_THRESHOLD_V1=1: 所有电极统一使用全局 th_touch/th_release
+     (多数键为基准, 数值由 calibrate_thresholds.ps1 实测给出, 经 ConfigApp 写入;
+     原 round45b 逐键阈值表保留在 #else 分支, 改宏可还原)
+   - 固件版本: 1.3.1 -> 1.4.0
+   - 配套工具: calibrate_thresholds.ps1 (5ms 采样校准), monitor_chain3.ps1
+     (v3: 修 Write-Host -f bug + 5ms 采样 + 事件流 + 异常自动抓拍 + 遥测回读),
+     proxy/chuniio_nyanithm_proxy.c (32位日志代理DLL, 记录游戏钩子实际收到的
+     每帧 32 cell 数据), analyze_chain.py (三路对齐分析)
+   - 构建: Nyanithm_round46_unified_diag.uf2 (136192 bytes)

### 202608011605
+ round46b: 看门狗 + I2C超时 + 高速诊断优化 (v1.4.0)
+   - 实测发现: 校准后设备 I2C 总线偶发卡死 -> Core1 的 0xC2/0xBF(I2C 读取)
+     永久无响应, 而 0xC0/0xC1(无 I2C)正常; 卡死原因疑为运行时 I2C 总线
+     异常(线缆/供电/事务交错), 旧固件无任何恢复机制 -> 游戏输入冻结
+   - 修复1(硬件看门狗): watchdog_enable(2000ms), updateInputState() 每周期
+     喂狗; 任何 Core0 卡死(如 I2C 挂起) -> 2s 内自动复位恢复, 不再永久冻结
+   - 修复2(I2C 超时): i2c_port.cpp 全部事务改用 _until 5ms 超时版本,
+     总线挂起时单次事务最多阻塞 5ms(原先可能阻塞 1s+), 配合 WDT 自愈
+   - 修复3(0xC2 批量读取): 75 次单字节 I2C 读 -> 9 次批量事务(~7ms -> ~1ms),
+     使 5ms 采样成为可能; 新增 MPR121::readRegisters()
+   - 协议变更: 0xC0/0xC2 响应加 [0xAA][0x55] 同步头(46B/44B), 支持主机侧
+     流水线读取与错位自恢复(旧 monitor_chain2.ps1 将无法解析, 请用 v3)
+   - 实测主机瓶颈: Windows USB-CDC 驱动 ~16ms 接收延迟(0xC0 往返实测
+     avg22ms) -> 脚本改为后台读线程 + 4 深请求流水线, 理论采样率 ~5ms
+   - 构建: Nyanithm_round46b_wdt_i2c_fast.uf2 (137216 bytes)

### 202608011615
+ round46c: 配置模式 USB 独占修复 (v1.4.0)
+   - 问题: ConfigApp 断开/读写配置异常。根因: 配置模式下 Core0 的
+     handleCommand() 经 stdio_cdc 写响应时会调用 tud_task(), 而 Core1 的
+     multicore_entry 循环仍在无差别调用 tud_task() -> 双核并发驱动 tinyusb
+     (非线程安全) -> CDC 栈损坏 -> 命令响应乱码/丢失 -> 无法正常断开
+   - 修复: 配置模式(in_config_mode)下 Core1 不再调用 tud_task(),
+     USB 栈完全由 Core0 独占(stdio_cdc 内部已带互斥)
+   - 附带: CMD_DEBUG_TELEMETRY 528B 写循环加 500ms 上限, 主机中断读取时
+     丢弃剩余数据而不是让 Core1 永久卡死(CDC 全停)
+   - 构建: Nyanithm_round46c_cfgmode_usb_fix.uf2 (137728 bytes)

### 202608011630
+ round46d: 配置模式彻底重做 - Core1 独占 USB 栈 + Core0 直连 tud_cdc (v1.4.0)
+   - 回退分析(round46a/c 实测):
+     * round46a(双核都跑 tud_task): 配置模式下 Core0 的 stdio_cdc
+       (printf/putchar/getchar 路径, usb_device.cpp 126/131/174 行) 内部
+       调用 tud_task() 与 Core1 并发驱动 tinyusb -> CDC 状态损坏 ->
+       ConfigApp 命令乱码/断开异常
+     * round46c(配置模式 Core1 停 tud_task): 修掉双核竞争, 但 handleCommand
+       等待循环 while(tud_cdc_available()==0) 期间无人跑 tud_task ->
+       USB RX 事件无人处理 -> 主机命令永远进不了 FIFO -> 配置模式彻底卡死
+       (现场复现: 0xB4 后 CFG_READ 0/128B, GET_STATUS 死等)
+   - round46d 正确架构:
+     * Core1 永远独占 tud_task()(USB 栈唯一驱动者, 含配置模式)
+     * Core0 配置模式 I/O 全部改用新模块 src/config_cdc.cpp:
+       cfg_cdc_write/puts/printf/read_byte/read_bytes - 只调
+       tud_cdc_read/write/flush, 绝不调 tud_task(), 读写均带超时防卡死
+     * app_link.cpp handleCommand/getStatus 与 controller_config.cpp
+       setConfig/checkConfigBuf 的 getchar/putchar/printf 全部替换
+   - 保留: seqlock 竞态修复 / 统一阈值 / WDT 看门狗 / I2C 5ms 超时 /
+     0xC2 批量读取+同步头 / 0xC1 遥测(含 500ms 写上限)
+   - 构建: Nyanithm_round46d_cfgmode_fix.uf2 (138240 bytes)

### 202608011645
+ round46e: 配置模式回退 round45 前 stdio 机制 (v1.4.0)
+   - 背景: round46c(配置模式 Core1 停 tud_task, USB RX 饿死) 与
+     round46d(Core0 直连 tud_cdc, flush 与 Core1 tud_task 竞争) 均未解决
+     ConfigApp 卡死; 用户要求参考 round45 之前的固件代码重新整合
+   - 参考 backup_20260719_cdc_opt(round45 前, ConfigApp 验证可用):
+     * Core0 maindev_loop/handleCommand 用 stdio(getchar/putchar/printf)
+       处理全部 CDC 命令
+     * Core1 常驻 tud_task()(配置模式也跑), USB RX 由 Core1 驱动
+   - round46e 整合:
+     * app_link.cpp handleCommand/getStatus: 恢复 getchar/putchar/printf
+       (撤销 round46d 的 config_cdc 直连方案)
+     * controller_config.cpp setConfig/checkConfigBuf: 恢复 getchar/printf
+     * 删除 src/config_cdc.cpp + include/software/config_cdc.h
+     * multicore_entry 保持 tud_task() 常跑 + cdc_respond(in_config_mode
+       时自动让位) + hid_task - 与旧版配置模式行为一致
+   - 保留: seqlock / 统一阈值 / WDT / I2C 5ms 超时 / 0xC2 批量+同步头 /
+     0xC1 遥测(500ms 写上限)
+   - 说明: round46a 时期 ConfigApp"断开异常"的根因实为 I2C 总线卡死
+     (无超时, updateInputState 每次阻塞 1s+) 拖慢配置命令响应;
+     round46b 的 I2C 5ms 超时已解决该因素
+   - 构建: Nyanithm_round46e_cfgmode_stdio.uf2 (137728 bytes)

### 202608011650
+ round46f: 配置模式空闲自动退出兜底 (v1.4.0)
+   - 实测反馈: round46e 配置读写正常, 但 ConfigApp"断开连接"后设备仍可能
+     停在配置模式(handleCommand 不返回, 等待 CMD_EXIT=0xB9 或主机关闭)
+   - 兜底: handleCommand 等待循环 60s 无任何 CDC 命令 -> 自动 reboot()
+     回正常模式; 无论 ConfigApp 断开时是否发送命令, 设备都能自动恢复
+   - 说明: 正常操作 ConfigApp 时命令间隔远小于 60s, 不影响使用
+   - 构建: Nyanithm_round46f_cfgmode_idleexit.uf2 (137728 bytes)

### 202608011655
+ round46g: 配置模式命令日志 (诊断 0xCE) (v1.4.0)
+   - 实测反馈(round46f): ConfigApp 断开时显示"状态忙", 设备 LED 常绿
+     不退出配置模式; 60s 空闲超时未触发 -> 怀疑 ConfigApp 断开后仍在
+     周期性探测(每次刷新空闲计时)
+   - 新增: 配置模式下记录收到的每个命令字节到 256B 环形日志
+     (含未知命令); 诊断命令 0xCE 回读日志 [1B len][len 字节旧->新]
+     ConfigApp 不会发送 0xCE, 不影响其正常使用
+   - 用途: 精确定位 ConfigApp"断开"时发送的命令序列, 针对性修复
+   - 构建: Nyanithm_round46g_cmdlog.uf2 (138240 bytes)

### 202608011655
+ round46h: 修复 reboot() 永不生效 - ConfigApp 断开根因 (v1.4.0)
+   - 命令日志取证(round46g): ConfigApp 断开时确实发送 0xB9 (CMD_EXIT),
+     但设备收到后未重启 -> reboot() 无效
+   - 根因: reboot() = watchdog_reboot(0,0,10) 设置 10ms 复位窗口,
+     而 round46b 新增的 watchdog_update()(updateInputState 每周期调用)
+     持续刷新 WDT 计数器 -> 10ms 窗口永远被续命 -> 复位永不触发
+     配置模式下 handleCommand 等待循环每 10-15ms 调一次 updateInputState
+     (round45 前无 watchdog_update, 故 ConfigApp 断开一直正常)
+   - 同时解释了 round46f 的 60s 空闲自动退出也无效的现象
+   - 修复: watchdog_reboot(0,0,0) -> delay=0 直接置 WATCHDOG_CTRL_TRIGGER
+     立即复位, 不依赖计数窗口
+   - 构建: Nyanithm_round46h_reboot_fix.uf2 (138240 bytes)

### 202608011715
+ round46i: 修复 0xC2 批量读取失败产生 diff=255 假尖峰 (v1.4.0)
+   - 实测: 正式校准(60s idle, 6899 样本)出现 th_touch=257(p85 idle max 255)
+   - 分析 calib CSV: 仅 2 个异常样本(0.03%), 每个样本为某个 MPR 的 12 个
+     电极同时 = 255 -> 该 MPR 的 readRegisters 整批 I2C 读失败(偶发总线
+     错误/超时), 缓冲区残留未初始化栈垃圾 -> diff clamp 到 255 -> 污染
+     idle_max 统计
+   - 修复: MPR121::readRegisters 返回 bool, 失败时 memset 清零;
+     0xC2 处理器检查返回值, 失败的 MPR 输出干净全 0
+   - 构建: Nyanithm_round46i_diff_fix.uf2 (138240 bytes)

### 202608011745
+ round46j: 阈值完全统一 - 尊重 ConfigApp 值 (v1.4.0)
+   - 用户要求: 所有按键阈值统一恢复为 ConfigApp 应用的值, 不做任何
+     单键特殊处理 (右侧 slider 0-15 漏键问题先不改阈值)
+   - initMPR121: 去掉 th_touch 最低 6 / th_release 最低 4 的强制下限,
+     所有电极直接使用 ControllerConfig.th_touch/th_release (ConfigApp 值)
+   - 移除 round45n stuck-key boost: 不再运行时 setThresholdsForElectrode
+     修改单电极阈值 (boost 抬高 / 释放恢复全部删除); 保留 stuckStart
+     跟踪供 round45p baseline 冻结使用 (baseline 修复不改阈值)
+   - 设备当前配置 (ConfigApp 已写入): th_touch=10 th_release=9 dt=1/dr=1
+   - 分析结论 (chain_report): 传输层 251639 帧全收到, rise==press 计数
+     一致, 无 DROP/GHOST; 右侧 0-15 与左侧 16-31 按压时长分布一致
+     (p50 75ms vs 74ms) - 漏键待结合具体谱面/时刻继续定位
+   - 构建: Nyanithm_round46j_unified_th.uf2 (137728 bytes)

### 202608011830
+ round46k: baseline 主动防塌陷 + 释放后恢复 (v1.4.0)
+   - 实测反馈: slider 0-7 锁手交互/持续点击中突然释放, 且短时间无法
+     重新触发 -> 经典 MPR121 baseline 塌陷症状
+   - 机制: NCLF=127 使 baseline 在持续触摸中每 127ms 缓慢下降 1 LSB,
+     数秒/数十秒后 baseline 塌陷到 filt 附近 -> 硬件 touch bit 清零
+     = 意外释放; 塌陷的 baseline 残留使重新触发需要更重按压
+   - round45p 旧冻结的缺陷: 要求 rawTouch 位仍置位 + 等 diff<bth+2 才修
+     -> 意外释放发生时硬件位已清零, 冻结条件不满足, 从不干预
+   - round46k 双机制:
+     1) 主动防塌陷: 按住 >3s 的电极每 2s 无条件重置 baseline =
+        filt+bth+4 (不等塌陷; 释放检测不受影响 - filt 回升仍是释放信号)
+     2) 释放后恢复: 释放后 0.3-10s 窗口内每 1s 轮转检查 8 个电极,
+        idle diff < bth (塌陷残留) -> 重置 baseline, 下次触摸立即触发;
+        I2C 开销受控 (每轮最多 8 电极, 全 36 电极 ~4.5s 轮转)
+   - 构建: Nyanithm_round46k_baseline_fix.uf2 (138240 bytes)
+   - 文件: src/hw_devices.cpp

### 202607310800
+ round45t: 修复跨核竞态条件 (phantom touch + micro-dropout根因)
+   - 关键发现: monitor_chain2.ps1显示无触发, 但游戏内仍有幻影触摸和微断触
+     原因: touchData[0..2]在L483-485赋值(早), touchData32[]在L487-533赋值(晚)
+     Core1 HID报告可能在touchData[]更新过程中读取, 得到新旧混合数据
+     -> 某cell在新数据中按下但旧数据中未按下 -> 幻影触摸
+     -> 某cell在新数据中按下但相邻cell在旧数据中按下 -> 微断触
+     monitor读touchData32[](CDC路径), 不受影响 -> 看不到问题
+   - 修复: 将touchData[0..2]赋值移到touchData32[]之后(L538-540)
+     4字节在~4时钟周期内连续赋值, HID读到混合数据的概率极低
+     HID要么读到全部旧值, 要么读到全部新值, 不会混合
+   - 这是"monitor无触发但游戏有触摸"的根本原因!
+   - 含 round45s 全部(Air确认+slider门控+方向门控+air sticky)
+   - 文件: src/hw_devices.cpp

### 202607310700
+ round45s: Air确认机制 + slider门控 + 方向门控 (过滤低空swipe误触)
+   - 问题1: ToF错开未完美改善延迟
+   - 问题2: 手低空划过ToF传感器时意外触发Air (硬件位置设计问题)
+   - 解法1(Air确认): 新Air触发需N次连续在范围内检测
+     正常: 2周期(~5ms), 手在slider上: 3周期(~7.5ms)
+     过滤1周期的短暂swipe(手横向划过ToF光束)
+   - 解法2(Slider门控): 检测touchData32是否有触摸活跃
+     有触摸=手在slider上=低空swipe可能=需更多确认
+   - 解法3(Air sticky): 确认后维持不需重新确认, 直到手离开+stretch
+     类比touch的sticky机制, 避免ToF测量间隙导致Air闪烁
+   - 解法4(方向门控): 预测只在手上升时触发(approach>0)
+     横向swipe不会触发预测(距离不变或增加)
+   - 延迟分析: 确认增加~5ms, 预测补偿~5ms, 净延迟~0ms
+   - 含 round45r 全部(M2E4阈值+verifiedCount保留+ToF错开+sticky+flick+baseline冻结)
+   - 文件: src/hw_devices.cpp

### 202607310600
+ round45r: 修复静置误触 + sticky dip恢复时验证失败导致miss
+   - 问题1(静置误触): M2E4(key25)阈值7不够, 静置时仍有2次/10min误触
+     修复: M2E4阈值 7->10 (idle max=4, +6 margin, 真实触摸diff=20-50不受影响)
+   - 问题2(游玩miss/粘键): sticky dip恢复时verifiedCount被重置为0
+     -> 需重新验证 -> 弱信号/I2C时序错配导致验证失败 -> 真实触摸被过滤 -> miss
+     修复: 50ms内触摸过的电极, touch bit=0时不重置verifiedCount
+     -> touch恢复时verifiedCount仍=2 -> 跳过重新验证 -> 即时输出, 无miss
+     -> 50ms窗口覆盖最大sticky dip(18周期~47ms)
+   - 风险评估: 50ms内同电极的新触摸跳过验证
+     安全性: MPR121硬件阈值仍在, confirm仍在, 空间过滤仍在, 刚触摸过的电极baseline稳定
+   - 含 round45q 全部(ToF错开+预测+展宽+sticky+flick优化+baseline冻结)
+   - 文件: src/hw_devices.cpp

### 202607310500
+ round45q-final: ToF传感器错开启动 + 纯固件Air加速(不改JudgeTiming)
+   - 约束: 不修改JudgeTiming.ini(改判定窗口=作弊)
+   - 核心优化: 错开4个ToF传感器启动时间(各延迟5ms)
+     原始: 4传感器同时启动 -> 数据同时更新(每20ms) -> 平均延迟10ms
+     优化后: 传感器错开5ms启动 -> 数据每5ms更新一次 -> 平均延迟2.5ms
+   - 效果: ToF有效延迟从~10ms降至~2.5ms(降低75%)
+     配合预测(AIR_PREDICT_MM=10)补偿残余~2.5ms
+     总残余延迟: ~0-2ms (原~10-20ms)
+   - Air JCR窗口[0,75ms]不变, 大部分hit落在0-5ms(JCR左边缘)
+   - Early attack风险低: 预测over-compensation最多5ms < 1帧(8.33ms)
+   - JudgeTiming.ini已恢复原版
+   - 含 round45p 全部(sticky+flick优化+baseline冻结+自适应grace)
+   - 文件: src/hw_devices.cpp

### 202607310400
+ round45q: Air判定加速 - 预测+展宽补偿ToF 20ms延迟
+   - 问题: tap偏快(round45o优化~5ms) vs air偏慢(ToF固有~10-20ms延迟)的时序不对称
+   - 根因: VL53L0X最小时序预算=20ms(库注释L692明确),无法通过降低测量周期加速
+     air路径无任何补偿机制(无stretch/sticky/prediction),与tap路径不对称
+   - 解法1(预测): 跟踪heightData变化趋势,手接近检测范围15mm内且approach>=gap时预触发
+     补偿ToF测量延迟,平均提前~10ms触发air key
+   - 解法2(展宽): air key释放后保持15ms(AIR_STRETCH_MS),平滑ToF测量间隙的闪烁
+   - 效果预期: air判定延迟从~10-20ms降至~0-10ms,与tap路径基本对称
+   - 含 round45p 全部(sticky+flick优化+baseline冻结+自适应grace+零开销诊断)
+   - 文件: src/hw_devices.cpp

### 202607310130
+ round45p-update: chain监控零I2C开销 + monitor_chain2.ps1修复
+   - 问题: CMD_DEBUG_CHAIN每次调用读3次mpr.touched()=3次I2C,与主循环竞争spinlock
+     5ms轮询=200Hz×0.6ms=12%开销,用户反馈"诊断接口导致延迟极大触摸不可用"
+   - 优化: 新增hwTouch[3]数组,在updateTouch_v1中验证前保存硬件触摸快照
+     CMD_DEBUG_CHAIN改用hwTouch快照(零I2C读取),不影响主循环性能
+   - 修复: monitor_chain2.ps1的Write-Host -f格式化导致ForegroundColor绑定错误
+     (key>15时"25"被误认为ConsoleColor枚举值)
+   - 文件: src/hw_devices.cpp, src/chuni_io.cpp, monitor_chain2.ps1

### 202607310200
+ round45p-refine: 自适应sticky grace + 综合优化
+   - 优化: 自适应sticky grace, 短持(touchCount 15-49)用grace=8(释放~32ms)
+     长持(touchCount>=50, ~130ms+)用grace=15(释放~50ms, 最大容忍)
+     短持释放从50ms降至32ms(进入JUSTICE CRITICAL窗口±33ms内)
+     长持保持最大dip容忍(防断触)
+   - 完整防护链(round45n+o+p):
+     1. verify(2次I2C, 强信号跳过第二次) 2. confirm(1-3周期, 空间过滤+1)
+     3. dipGrace(3周期, touchCount>=3) 4. sticky(8-15周期, touchCount>=15)
+     5. STRETCH(3ms) 6. baseline冻结(每3s, 防塌陷) 7. 智能stuck-key(30s验证)
+   - 文件: src/hw_devices.cpp

### 202607310200
+ round45p: 超长Hold baseline塌陷防护 + flick时序优化(含round45o)
+   - 深度分析: MPR121 NCLF=127 在持续触摸中每127ms跟降baseline 1 LSB
+     30s跟降约236 LSB, 60s约472 LSB -> diff完全塌陷 -> 硬件touch bit清零 -> 断触
+     sticky机制能容忍dip但无法阻止baseline本身塌陷
+   - 解法: 每3秒检查持续触摸>5s的电极, 若diff<base_th+2则重置baseline
+     重置值 = filt + base_th + 4 (安全margin, 保证diff远超阈值)
+     I2C开销: 每3秒每电极~3次操作 = 可忽略
+   - 超长Hold(45s-1min)完整防护链:
+     0-5s: 正常检测 + sticky(15周期dip容忍)
+     5s+: baseline冻结(每3s检查重置) 防止diff塌陷
+     30s: 智能stuck-key(读diff验证, 真按不boost)
+   - 同时含 round45o 全部(flick时序优化: 强信号跳过验证+空间过滤减半+STRETCH回退)
+   - 含 round45n 全部(sticky+智能stuck-key)
+   - 文件: src/hw_devices.cpp

### 202607310100
+ round45o: flick音符时序优化 (减少检测延迟和抖动)
+   - 问题: flick音符出现ATTACK判定, 根因是检测管线延迟抖动 2-9.6ms
+   - 延迟来源: 2次I2C验证(~0.8ms) + confirm周期(1-3) + 空间过滤孤立cell(+2周期)
+     flick通常是单点触摸(孤立), 空间过滤+2导致额外3.2ms延迟
+   - 优化1: 强信号跳过第二次I2C验证 (diff>=th+6 -> verifiedCount=2, 省1周期~2ms)
+     非常强的信号几乎不可能是误触, 无需二次验证
+   - 优化2: 空间过滤条件化+减半 (+2->+1, 且强信号reqCycles==1时完全跳过)
+     强信号(diff>=th+4)极少误触, 不需空间过滤
+     孤立强信号flick: 1周期(原3周期), 减少抖动3.2ms
+   - 优化3: STRETCH_MS 5->3ms回退 (sticky已提供足够dip容忍, 更短stretch=更快flick释放)
+   - 优化后时序: 强信号flick=1周期~2ms(原3~5.6ms), 弱信号孤立=4周期~8ms(原5~9.6ms)
+   - 协同: round45n全部(sticky+智能stuck-key) + flick时序优化
+   - 含 round45n 全部
+   - 文件: src/hw_devices.cpp
### 202607301809
+ round45e: 卡键自动记录诊断(常驻,不干扰游戏)
+   - 目的: 诊断"触摸占用未释放/漏键"的根因(硬件 false touch vs 软件卡住)
+   - 机制: 持续触摸>1s 的电极,每2s自动记录 {m,e,elapsed,filt,base,diff,boost} 到环形缓冲(48条)
+   - CDC 命令 0xC1 读取缓冲(read_stuck_log.ps1),读取后清零
+   - 不影响正常游戏(只在异常长触摸时记录,常驻)
+   - 包含: round45d 全部(振幅分级+电极级阈值+纯STRETCH+stuck-key 10s加速)
+   - 诊断脚本: read_stuck_log.ps1 (含 cell 映射)
### 202607301824
+ round45f: 移除卡键诊断接口(恢复性能)
+   - 问题: round45e 诊断接口(filteredData/baselineData 的 I2C 读)导致延迟极大、触摸不可用
+   - 根因: 诊断记录在 updateInputState 主循环内做 I2C 读,与 touched() 竞争总线,拖慢主循环
+   - 解法: 完全移除诊断逻辑(stuckLog 缓冲 + 记录 + 0xC1 命令),回到 round45d 性能
+   - 逻辑等同 round45d: 振幅分级 + 电极级阈值 + 纯STRETCH + stuck-key 10s加速
+   - 诊断改用 monitor_chain2.ps1(非侵入式,事后采集)
+   - 文件: src/hw_devices.cpp, src/chuni_io.cpp
### 202607301902
+ round45g: 降低电极级阈值余量(+4->+2)修复 slider 19-26 漏键
+   - 诊断: chain2_log 显示性能恢复;用户反馈 slider 19-26 容易漏键
+   - 根因: electrodeBaseTouchTh 余量+4 过大,cell 19/20/21/23 阈值 10-12
+     挡住真实轻触(diff 8-12)
+   - 改进: 余量 +4 -> +2,保留 idle 噪声防护,放行轻触
+     M2E0:16->15, M2E3:12->10, M2E1/M0E5/M0E6:11->9, M1E7/M2E2:10->8
+   - 乱触防护: 振幅分级(弱信号3周期确认)补偿降阈值带来的噪声风险
+   - 备份: backup_pre_th_lower_20260730/ (含 round45f)
### 202607301956
+ round45h: 精准调阈值治静置乱触(基于 chain2_log_190718 静置诊断)
+   - 诊断: 静置5分钟,cell25(M2E4)6/8事件为乱触主源(全局阈值6太低),
+     cell21(M2E2)round45g降到8后偶发触发
+   - 改进: M2E4 新增电极级阈值7(原全局6,挡idle 6+);M2E2 8->9(挡idle 8+)
+   - 保留: 其余电极 round45g 阈值,振幅分级,纯STRETCH,stuck-key 10s
+   - 风险: M2E4=7放真实触摸20+/轻触8+;M2E2=9比原10仍低,治漏键
+   - 备份: backup_pre_th_lower_20260730/ (含 round45f)
### 202607302015
+ round45i: 加强软件 baseline 动态跟踪(治本,非阈值)
+   - 诊断: 静置10分钟乱触分散在5电极(M0E7/M2E4/M2E7/M2E6/M0E4),阈值6-9都触发
+     根因是 baseline 漂移导致 idle diff 偶发过阈值,非单一电极阈值问题
+   - 改进(非阈值): 加强已有 baseline 修正逻辑,让 idle diff≈0 从根源减少乱触
+     频率 1s->0.5s, 电极 4->8(9s->4.5s轮转), 门槛 diff>5->>3, adj diff>10?2:1->>6?2:1
+   - 优势: 治本(idle diff降),不调阈值(漏键不恶化),跳过触摸中电极(不影响真实触摸)
+   - I2C开销: 8电极*4I2C/0.5s ≈ 7ms卡顿/0.5s(1.4%),可接受
+   - 含 round45h 全部(电极级阈值+振幅分级+纯STRETCH+stuck-key 10s)
### 202607302019
+ round45j: 叠加方法C空间过滤(非阈值,治孤立噪声)
+   - 诊断: chain2_log 乱触全部是孤立单cell(无相邻),真实触摸通常1-3相邻cell
+   - 改进: confirm阶段检查同MPR相邻电极(e-1/e+1),孤立电极+2确认周期
+     真实多cell触摸不受影响,孤立噪声(短)被滤
+   - 组合: A(baseline加强,治本) + C(空间过滤,滤孤立) 双非阈值机制
+   - 风险: 孤立单cell真实触摸(手指尖小)+2周期(~6ms)延迟,可调+1
+   - 含 round45i 全部
### 202607302200
+ round45k: NCLF 63->127 治触摸中 baseline 塌陷(漏键根因)
+   - 深度分析: 用户报告严重漏键(突然释放/移动释放/释放后几秒无法触发)
+   - 根因: NCLF=0x3F(63, round32从127调低)过快,触摸时filt下降,baseline跟着下降
+     -> diff=baseline-filt减小 -> 突然release(现象1,2)
+     -> 释放后baseline偏低 -> 再次触摸diff不够th_touch -> 几秒无法触发(现象3)
+     -> NCLR=31慢回升 -> 几秒后baseline回升恢复
+   - 解法: NCLF 0x3F->0x7F(127),需127ms持续低filt才降baseline1
+     短/中触摸(<127ms)不塌陷baseline;长触摸慢降,不会突然release
+   - 乱触补偿: NCLF慢降可能让idle baseline偏高(idle diff大),
+     但A(软件baseline修正round45i)idle时调低baseline + C(空间过滤round45j)滤孤立噪声
+   - 三者协同: NCLF127治漏键 + A治乱触 + C滤噪声
+   - 含 round45j 全部(A baseline加强 + C 空间 + 电极级阈值 + 振幅分级 + 纯STRETCH + stuck-key 10s)
### 202607302208
+ round45l: 精细化 dipGrace 治断触(只对持续触摸容忍dip)
+   - 深度分析: round45k(NCLF127)后断触仍在,说明NCLF是次要因素
+   - 断触主因: 移除dipGrace后touchCount严格连续,移动时raw瞬间0->touchCount清零->重新confirm->断触
+   - 之前移除dipGrace因卡键(放大false touch),但精细化可避免:
+     dipGrace只对touchCount>=4(持续触摸,~12ms)启用,单点false touch(1-3周期)严格不享受
+   - 改动: 恢复dipGrace(DIP_TOLERANCE=2),但else段加 touchCount>=4 条件
+     持续触摸: dip时容忍2周期不清零touchCount(防断触)
+     单点false touch: touchCount<4,严格清零(防卡键)
+   - 协同: NCLF127(治baseline塌陷) + 精细化dipGrace(治touchCount断触) + A(baseline修正) + C(空间)
+   - 含 round45k 全部
### 202607302222
+ round45m: 治超长Hold/锁手释放(A自动校准误调 + stuck-key误boost)
+   - 深度分析: 用户报告超长Hold/锁手交互仍释放,提示"自动校准逻辑"
+   - 根因1(自动校准): A(baseline修正)每0.5s跳过rawTouch=1电极,但持续触摸中
+     filt波动致raw瞬间0 -> A误判idle -> 调低baseline -> diff减小 -> 释放
+     超长Hold累积几秒,baseline被调低,release
+   - 根因2(stuck-key): 10s boost阈值,真实长Hold(>10s)被误当卡键boost -> release
+   - 解法1: A用lastTouchedMs时间戳,2s内触摸过的电极不调baseline
+     (不只看单周期rawTouch,看2s触摸历史,避免误判idle)
+   - 解法2: stuck-key 10s->30s(CHUNITHM长音<5s,30s不误判真实Hold)
+   - 协同: NCLF127(baseline不塌陷) + A不误调 + stuck-key不误boost + 精细dipGrace + C空间
+   - 含 round45l 全部

### 202607302330
+ round45n: sticky touch机制 + 智能stuck-key检测(超长Hold判定) + 参数微调
+   - 核心问题: 断触根因 = 硬件touch bit在持续触摸中瞬时跌落3+周期
+     dipGrace=2耗尽 -> touchCount清零 -> 需重新verify(2次I2C)+confirm(2-3周期)
+     = 4-5ms延迟 -> 游戏判定miss + "几秒内无法触发"(baseline漂移导致verify失败)
+   - 解法1(sticky touch): touchCount>=15(约15ms持续)进入粘滞态
+     dip时不清零touchCount, 仅递减stickyGrace(15周期约15ms容忍)
+     touch恢复后touchCount仍在(>=reqCycles) -> 即时重新确认, 无延迟, 无miss
+     与dipGrace区别: 仅对真正持续触摸生效(门槛15), false touch(1-3周期)不享受
+     stickyGrace可远大于dipGrace(15 vs 3)而不增加乱触风险
+   - 解法2(智能stuck-key/超长Hold判定): 30s触发前读filteredData/baselineData
+     diff>=th_touch(真按,手指在) -> 不boost, 重置计时器(每30s检查一次)
+     diff<th_touch(卡键,手已走) -> boost释放
+     回答用户: 45s-1min真Hold -> 30s检查diff大 -> 不boost -> 60s再检查 -> 持续不boost
+   - 解法3(参数微调): DIP_TOLERANCE 2->3, dipGrace门槛 4->3, STRETCH_MS 3->5ms
+   - 协同: NCLF127 + A(baseline不误调) + sticky(核心断触修复) + 智能stuck-key + 精细dipGrace + C空间
+   - 含 round45m 全部
+   - 文件: src/hw_devices.cpp
   - 构建: Nyanithm_round46q_retap_fix.uf2 (137216 bytes)
   - 文件: src/hw_devices.cpp, test_retap_sim.py

### 20260801 round46r: 防塌陷与恢复器冲突修复 (v1.4.0)
+ 深入审查发现 round46q 引入的新冲突:
+   - 恢复器 (round46q) 写后 idle diff ~7 (>0)
+   - 防塌陷 held 窗口 (释放后 2s) 内检查条件 0 < diff < bth+8 (=18)
+     -> 恢复器写后 diff=7 满足条件 -> 防塌陷把 base 再抬到 filt+22
+     -> idle diff=22 >= 阈值 10 -> 硬件位置位 -> 粘键 ~1.5s
+     (对应用户历史反馈"某键突然出现触摸占用/未释放")
+ 修改 (src/hw_devices.cpp):
+   - 防塌陷写条件: 0 < diff < bth+8 -> bth <= diff < bth+8
+     * 下界 bth: 恢复器修好的 idle diff (7) 不再被防塌陷碰
+     * 写时 diff 是平滑滤波的局部最低点 -> 写后波动只会更高
+   - 模拟验证 (平滑随机游走波动模型, 更接近真实 MPR121 滤波):
+     deep/light/wobble 全部 0 释放 (round46m 旧写点在 wobble 下 115 次)
+   - 构建: Nyanithm_round46r_anticollapse_ok.uf2 (137216 bytes)
+   - 文件: src/hw_devices.cpp, test_anticollapse_sim.py
   - 构建: Nyanithm_round46r_anticollapse_ok.uf2 (137216 bytes)
   - 文件: src/hw_devices.cpp, test_anticollapse_sim.py

### 20260801 round46s: flick 释放延迟优化 (v1.4.0)
+ 深入分析用户反馈 "flick 音符出现 ATTACK 判定":
+   - flick 快速扫过 cell (接触 40-80ms -> touchCount 15-29)
+   - 旧 stickyGrace 分档: touchCount>=50 -> 15, 其余 -> 8
+     -> flick cell 释放延迟 21-26ms (grace 8 x 2.7ms + STRETCH 3ms)
+   - 关键洞察: baseline 塌陷需要数秒 (NCLF ~0.3 LSB/40ms), 短时触摸
+     (touchCount<30) 不可能发生塌陷 dip -> 不需要长 grace
+ 修改 (src/hw_devices.cpp):
+   - stickyGrace 三档: touchCount>=50 -> 15 (长按 dip 保护),
+     30-49 -> 8, 15-29 -> 4 (~11ms)
+   - flick/快速连击释放延迟 21-26ms -> 14ms (改善 ~40%)
+   - 静止长按不受影响 (>=50 保持 15)
+   - 构建: Nyanithm_round46s_flick_release.uf2 (137216 bytes)
+   - 文件: src/hw_devices.cpp
   - 构建: Nyanithm_round46s_flick_release.uf2 (137216 bytes)
   - 文件: src/hw_devices.cpp

### 20260801 诊断工具优化 (round46s-diag)
+ - monitor_chain3.ps1: 快照冷却 200ms -> 500ms
+   (每次快照突发 8 帧 CMD_DEBUG_ALL = ~40ms Core1 I2C 流量; 200ms 冷却
+   实测抬高固件 loop 尖峰到 25ms; 500ms 减半干扰)
+ - proxy/chuniio_nyanithm_proxy.c 审查: 纯转发+日志, 无逻辑问题
+ - 文件: monitor_chain3.ps1
   - 构建: Nyanithm_round46s_flick_release.uf2 (137216 bytes)
   - 文件: src/hw_devices.cpp

### 20260801 round46t: seqlock 自旋超时 (v1.4.0, 子智能体审查)
+ 子智能体 Socrates 审查输出路径 (chuni_io.cpp/usb_device.cpp):
+   - 确认 maindev_loop 为死代码 (无调用者), 游戏实际路径 handleCommand/
+     cdc_respond 有完整 seqlock 保护
+   - 其"updateAir 早退跳过关锁"分析经核实为误报 (早退只退出 updateAir
+     自身, 不影响 updateInputState 的关锁)
+   - 采纳其防御性建议: Core1 seqlock 自旋无超时 -> Core0 卡死 (WDT
+     复位前 ~2s) 时 DLL/HID 阻塞 2s
+ 修改 (src/chuni_io.cpp):
+   - CMD_GET_INPUT / CMD_DEBUG_CHAIN / HID (touch+air) 的 seqlock 读取
+     加 5ms 自旋上限, 超时后返回最近一次读到的状态 (不阻塞 Core1)
+   - 构建: Nyanithm_round46t_seqlock_timeout.uf2 (137216 bytes)
+   - 文件: src/chuni_io.cpp
   - 构建: Nyanithm_round46t_seqlock_timeout.uf2 (137216 bytes)
   - 文件: src/chuni_io.cpp

### 20260801 round46u: I2C 损坏样本防御 (v1.4.0)
+ 实测分析 (round46t, chain3_events_195656):
+   - 锁手测试: 所有按压 262-575ms 释放 (仍无长 hold), dip=0 (防塌陷正常)
+   - 快照 (detail_snapshot_195658) 揭示: 触摸中 (t0=0B3) 但 base 被写坏
+     (8/44/64, 正常 ~712) -> diff 全负 -> 触摸释放; 且首行 filt=0/base=0
+     (I2C 读取失败/损坏)
+ 根因:
+   - 诊断快照 (每 500ms 8 帧 x 36 电极 x 3 读 ~1700 读/s) + 恢复器/
+     防塌陷读取 -> MPR121 在高 I2C 负载下偶发返回损坏样本 (filt=1-4/37-64)
+   - 恢复器把损坏 filt 写入 baseline (写条件 filt>0 无法识别损坏)
+     -> base=8/44/64 -> diff 负 -> 硬件位清 -> 释放; 且新按需先修复 base
+ 修改:
+   - 固件 (hw_devices.cpp): 防塌陷 + 恢复器写前重读 filt, 两次差值 >3
+     LSB 视为损坏样本 -> 跳过该周期写入 (防写坏 baseline)
+   - monitor_chain3.ps1: 快照 8 帧 -> 4 帧 (I2C 突发减半)
+   - 构建: Nyanithm_round46u_i2c_doublecheck.uf2 (137216 bytes)
+   - 文件: src/hw_devices.cpp, monitor_chain3.ps1
   - 构建: Nyanithm_round46u_i2c_doublecheck.uf2 (137216 bytes)
   - 文件: src/hw_devices.cpp, monitor_chain3.ps1

### 20260801 round46v: 防塌陷写点抬高 (v1.4.0)
+ 实测分析 (round46u, chain3_events_202645):
+   - 锁手测试: p50=156ms (点击正常), max=2567ms (长按 4 倍于 round46t)
+   - 但长按仍不稳: cell 29 按住 2.5s, raw 从 1.2s 起持续掉线 2.4s
+     (dip ongoing 618/1227/1850/2426ms), game 保持后最终释放 -> 断触
+   - 系统性排查"快速释放"候选机制:
+     [已修] 写入频率过高 (46l) / 恢复器冲突 (46r) / I2C 损坏样本 (46u)
+     [本轮] 防塌陷写点 [10,18) 太靠近硬件判定边界 (置位 10/释放 9):
+       写后 diff=22 塌到写点 10-12 才写回 -> 写前 diff 贴近边界
+       -> ±1-2 LSB 噪声即抖动 (raw 位 flutter) -> 长按塌穿
+ 修改 (src/hw_devices.cpp):
+   - 防塌陷写窗口 [bth, bth+8) -> [bth+2, bth+10) (=12-19)
+   - 写后目标 bth+12 -> bth+14 (=24)
+   - 数学验证: 写后 24 塌 4 LSB/500ms -> 1000ms 处 16 (写) -> 写前最低
+     16±3 波动 = 13 > 置位边界 10 -> 稳定; 恢复器 idle diff 7 < 下界 12
+     不冲突
+   - 构建: Nyanithm_round46v_anticollapse_boundary.uf2 (137216 bytes)
+   - 文件: src/hw_devices.cpp

### 20260803 round47b-patch: 审查风险修复 (v1.4.0)
+ 背景: round47b 全固件审查发现 3 个非阻塞风险 + 2 个注释瑕疵
+ 修复:
+   1. 删除 maindev_loop 死代码 (chuni_io.cpp L133-205): 未被调用,
+      含无 seqlock 读 touchData32; cdc_respond 替代; chuni_io.h 声明同步删除
+   2. filt==0 验证后门加固 (hw_devices.cpp 验证层): 旧代码直接信任硬件
+      (verifiedCount++); 改为重读 filt2, 仍==0 则跳过(不验证不拒绝,等下周期),
+      filt2!=0 则走完整验证; 防 I2C 连续失败绕过验证层
+   3. seqlock 5ms 超时撕裂读修复 (chuni_io.cpp):
+      - hid_task 两处: 超时 break->return (跳过本周期 HID, 游戏保持上一帧)
+      - cdc_respond CMD_GET_INPUT: 先读 tmpSlider[32] 临时缓冲, seqlock 成功
+        才 memcpy 到 inputState; 超时保留上次 inputState (无撕裂数据)
+      - CMD_DEBUG_CHAIN 保持 break (诊断用途, 撕裂可接受)
+   4. 注释修正: "4.5s rotation"->"2.25s rotation (36/8*0.5s)"
+   5. STRETCH_MS 注释修正: "covers main-loop peak"不准确 -> 改为
+      "smooths normal-speed releases, does NOT cover main-loop stalls"
+ 自审 (A-E): 全部通过
+   A maindev_loop 无残留引用(仅注释); prevInputState/prevAirState 仍被 cdc_respond 使用
+   B filt2==0 不卡死(每周期重试); filt2!=0 验证与 else 分支一致
+   C hid_task return 安全(1ms无HID->游戏保持上一帧); tmpSlider 32B 无栈溢出
+   D 注释与代码一致; chuni_io.h 与 cpp 一致
+   E ninja 全绿; UF2 137216->137728 = +512B (新增逻辑+section对齐)
+ 备份: backup_round47b_patch_20260803_011155/ (hw_devices.cpp.bak, chuni_io.cpp.bak)
+ 构建: Nyanithm_round47b_patch.uf2 (137728 bytes, 8/3 01:17)
+ 文件: src/chuni_io.cpp, src/hw_devices.cpp, include/software/chuni_io.h

### 20260803 round47b-patch-review: Gibbs 审查修复 (v1.4.0)
+ Gibbs 子智能体审查发现 1 个中风险 + 1 个低风险建议:
+   C.3(中): CMD_GET_INPUT 超时路径 air 变量不一致
+     局部 air(撕裂读)用于遥测和 prevAirState, 而 inputState.air(上次好值)用于发送
+     修复: 超时 break 前加 air = inputState.air; 使三者一致
+   B.2(低): filt2!=0 的 else 分支缺少快速通过路径注释
+     修复: 加注释说明有意差异(I2C 不稳定时不走快速通过)
+ 重新构建: Nyanithm_round47b_patch.uf2 (137728 bytes, 8/3 01:30)

### 20260803 round47b: NCLR 回退 31 + bl_reg 合理性校验 (v1.4.0)
+ 背景: round47 (NCLR=14) 实测 20min 静置 M2 芯片 5 次突发 RAW 触发
+   (M2E0 x3 / M2E3 / M2E4, 各 140-171ms), 全部被验证层过滤 (GAME 0 次),
+   但游戏中手接近可能突破 -> 用户实测 slider 17 (M2E0) 频繁误触
+ 根因 (Maxwell 分析 + 数值验证):
+   - NCLR=14: 上升跟踪 14ms/LSB, idle 噪声尖峰 (~14ms) 即可推高 baseline
+   - NCLF=127: 下降极慢 (127ms/LSB) -> 推高后 diff 长期滞留 (棘轮效应)
+   - "上升最快 + 下降最慢" = round34 当年改 31 要根治的组合
+   - round47 改 14 的理由 (释放后回升快) 已被 FDLT=0xFF 冻结取消:
+     触摸期 baseline 不塌陷 -> 释放后无回升需求
+ 修改:
+   1. src/mpr121.cpp L59: NCLR 0x0E -> 0x1F (恢复 round34 已验证值,
+      需 31 个连续样本 ~31ms 才推高 baseline, 短尖峰推不动)
+   2. src/hw_devices.cpp 自动校准 A: 新增 bl_reg 合理性校验
+      (bl_reg 与 filt>>2 相差 >15 LSB 视为损坏读, 跳过写入;
+       防 I2C 损坏样本 base=8/44/64 写坏 baseline)
+ 审查 (Plato): A 副作用通过 (触摸走下降方向不涉及; FDLT 冻结下无
+   恢复需求; 漂移跟踪富余 3 个数量级); B 校验正确 (数学上不可能误杀,
+   blDiff∈[-1,12]<15; 损坏样本必然拦截); C 残留风险通过 (棘轮消除后
+   idle diff<=8<TH=10, RAW 预期归零); D/E 构建通过 (固件字节级验证
+   NCLR=31 已烧录, 代码增量仅 24 字节)
+ 构建: Nyanithm_round47b_nclr31.uf2 (137216 bytes, 8/3 0:24)
+ 待实测: ① idle 20min RAW 触发应 0 (对照 round47 的 5 次);
+   ② 长按 5s baseline 漂移 <5 LSB; ③ 释放后 30ms 再触 + 连续 10 次
+   长按无累积下漂; ④ 游戏回归 (滑键/flick 释放边沿 <=10ms)

### 20260802 monitor_chain3: 触发事件实时终端报告 (脚本)
+ 用户要求: 所有检测脚本中"触发"信息直接报告在终端, 日志生成不变
+ 修改 (monitor_chain3.ps1): Log-Event 函数在写事件 CSV 的同时,
+   实时 Write-Host 到终端, 按事件类型着色:
+   - RAW_UP (绿) / RAW_DOWN (灰) / GAME_UP (青) / GAME_DOWN (灰)
+   - HOLD_END (黄) / RAW_FILTERED (红) / GAME_NO_RAW (黄)
+   - HOLD_RAW_DIP (红) / LONG_HOLD (品红) / 其他 (灰)
+   格式: [   12345ms] 类型 cell N [detail]
+   CSV 事件日志逻辑完全不变 (ts_ms,type,cell,detail)
+ 其他检测脚本现状: monitor_chain.ps1 / monitor_chain2.ps1 已有
+   触发行显示 (S 行 RAW/GAME); monitor_all.ps1 / monitor_detail.ps1
+   已有 TOUCH/HOT 显示; analyze_chain.py 为事后分析脚本 (输出报告
+   文件, 非实时) 不改
+ 验证: PowerShell Parser 语法检查通过; cell=-1 (SNAPSHOT) 边界
+   由 `$cell -ge 0` 保护

### 20260802 round47-review: 安全审查修复 (v1.4.0)
+ 智能体审查 (Aristotle) 结论: 无内存越界/无共享态竞态/无 WDT 风险/
+   无编译残留/寄存器值与厂商推荐 (esp-idf) 一致; 两项防御性修复:
+   - hw_devices.cpp 自动校准 A: 加 `if (bl_reg == 0) continue;`
+     (I2C 读失败返回 0 时禁止写入 baseline=0, 避免 2-3s 伪触摸锁定)
+   - mpr121.cpp writeBaselineRun: 入口加 `if (e > 11) return;`
+     (防 e>=13 写入 MHDR 等滤波寄存器 / 地址回绕)
+ 假设数值验证 (Python 模拟): FDLT 冻结后长按 60s diff 恒定 20 不释放;
+   未冻结则 1.4s 塌穿 (与旧实测吻合); NCLR=14 + 自动校准 A 平衡点
+   diff≈0 无误触; STRETCH_MS=5 覆盖 300-500Hz 采样; sticky 12 周期
+   对 I2C 错误 dip 充分
+ 重新构建: Nyanithm_round47_hwfreeze.uf2 (137216 bytes, 22:28)
+ 待实测: ① 长按 5s baseline 漂移 <5 LSB; ② 释放后 30ms 内再触;
+   ③ 连续 10 次长按无累积下漂; ④ idle 10 分钟误触计数;
+   ⑤ flick 快速谱面释放边沿 <=10ms

### 20260802 round47: 硬件触摸期冻结 + 软件单写者 (v1.4.0)
+ 背景: round46j-fix1 实测长按 5-10s 强制释放 + 阈值升高 + 缓慢恢复。
+   根因定论: (1) MPR121 触摸期间 baseline 继续跟踪 (NCLF 塌陷),
+   而 NHDT/NCLT/FDLT=0x00 未启用硬件触摸期冻结; (2) 软件三机制
+   (自动校准/冻结/恢复器) 共用 writeRegister() 的 Stop->Run 全芯片
+   中断, 互相打架 + 每次写都打断测量 -> stuckStart 一帧清零 ->
+   冻结 5s 失明 -> 塌穿释放。
+ 方案 (彻底重构 baseline 软件层):
+   A. 硬件触摸期冻结 (src/mpr121.cpp):
+      NHDT 0x00->0x01, NCLT 0x00->0x10, FDLT 0x00->0xFF
+      (AN3891/esp-idf 确认 FDLT=0xFF 触摸期间禁用 baseline 跟踪)
+      -> 长按期间 baseline 恒定, 永不塌陷, 永不释放
+      NCLR 0x1F->0x0E (恢复 hw_v1 早期已验证值, 释放后回升快一倍)
+   B. 新增 MPR121::writeBaselineRun(e, val): Run 模式直写 0x1E+e,
+      不做 Stop->Run (无中断)
+   C. 软件层删除冲突源 (src/hw_devices.cpp):
+      删除 stuckStart 跟踪 / 冻结块 (round45p+46j-fixA) /
+      释放恢复块 (46j-fixB) - 全部由硬件冻结替代
+      自动校准 A 保留并改造: 改用 writeBaselineRun (无中断),
+      新增 per-electrode 2s 写后冷却, 仍只降不升 (idle 防误触)
+   D. 参数微调: STRETCH_MS 3->5ms (覆盖主循环峰值 25-60ms),
+      STICKY_GRACE_SHORT 8->12 (锁手容忍 ~32ms)
+ 结果: 运行期软件层只剩一个 idle 校正写入者, 无 Stop->Run 中断,
+   无机制间冲突/竞态; 触摸检测/确认/过滤链 (验证层/振幅分级/
+   空间过滤/sticky/seqlock) 全部只读保留
+ 构建: Nyanithm_round47_hwfreeze.uf2 (137216 bytes)
+ 文件: src/mpr121.cpp, src/hw_devices.cpp, include/device/mpr121.h
+ 备份: round46 系列源码 -> ..\backup_round46_series_20260802_001009\
+ 待验证: FDLT=0xFF 在 v1 硬件实测是否冻结 (置信度高, esp-idf 默认);
+   若长按仍塌陷则加 Run 直写冻结兜底

### 20260801 round46j-fix1: 冻结增强 + 低频释放恢复 (v1.4.0)
+ 背景: 用户实测 round46j 相对稳定只有漏键; 以 46j 为基线 (46j-restore
+   已重建源码), 修复漏键两个根因
+ 根因1 (长按塌陷追不上): round45p 冻结写后 diff=14, NCLF=127 下
+   635ms 塌到释放线 9, 但冻结周期 3000ms -> 锁手/长 hold 2-3s 塌穿
+ 根因2 (快速二击无恢复): 释放后 baseline 残留高位 -> 第二次轻触
+   diff 不够 -> 漏键; 46j 无恢复器
+ 修改 (src/hw_devices.cpp, 冻结块):
+   A. 冻结周期 3000->1000ms; 触发条件 diff<bth+2 -> <bth+6 (12->16);
+      写后目标 filt+bth+4 -> filt+bth+10 (14->20)
+      验证: 写后 diff=20 塌到 9 需 ~1400ms > 1000ms 周期, 追得上
+      I2C 开销: 每 1s 检查 held>5s 电极 (~1-5 个), 可接受
+   B. 新增低频释放恢复: 每 1000ms 检查释放 100-1000ms 的电极 (8 个
+      轮转批次), diff<bth 时写 filt+bth-3 (idle diff~7), 带 filt2
+      双读防坏样本; 频率为 round46q 恢复器的 1/10, 无 stale-filt 竞态
+ 保留: 46j 其余全部 (验证层/sticky 两档/空间过滤/50ms 窗口/统一阈值)
+ 构建: Nyanithm_round46j_fix1_freezerel.uf2 (139264 bytes)
+ 文件: src/hw_devices.cpp
+ 注意: 本版保留 46j 的 Stop->Run 冻结写入 (每 1s/held 电极), 若实测
+   仍有锁手断触, 下一步改为 Run 模式直写 baseline (跳过 Stop->Run)

### 20260801 round46j-restore: 回退到 round46j 基线 (v1.4.0)
+ 背景: 用户实测 round46j (Nyanithm_round46j_unified_th.uf2) 相对稳定,
+   只有漏键问题; round45j_restored 仍有意外漏键; 决定以 46j 为基线
+ 方法: 从 backup_round46v_20260801_215853 (46v 源码) 反向回退
+   46k~46v 的触摸链叠加改动 (共 13 处 R1-R13), 保留 round46~46j 基础
+ 回退清单 (src/hw_devices.cpp):
+   R1: slide 加速门槛 diff>=th+2 改回 neighborWasActive (46l)
+   R2: 验证保留窗口 100ms -> 50ms (46p)
+   R3: sticky 三档 -> 两档 (46s)
+   R4-R6: AIR_PREDICT_MM 15->10, 恢复 approach>=gap 条件 (46o)
+   R7: 自动校准跳过窗口 10s -> 2s (46l)
+   R8-R9: 校准门槛 bth+2 改回 diff>3, 恢复写 0 防护 (46l/46m)
+   R10: 防塌陷 500ms 窗口块 -> round45p baseline 冻结 (3s 周期, >5s
+        长按, diff<bth+2 时写 filt+bth+4) + stuckStart 跟踪 (46k~46v)
+   R11: post-release 恢复器 100ms 块删除 (46q~46u)
+ 保留 (round46~46j 基础): seqlock touchStateGen, WDT, I2C 5ms 超时,
+   0xC1/0xC2 诊断, UNIFIED_THRESHOLD_V1=1 统一阈值 (ConfigApp 值),
+   initMPR121 无强制下限, g_verifyFail 遥测
+ chuni_io.cpp: 保留 46t 的 seqlock 5ms 有界自旋 (仅 Core0 卡死时生效,
+   正常运行时等待 <1ms, 不改变触摸判定; 严格回退为无界自旋反而
+   会让 Core1 在卡死时阻塞 2s)
+ 验证: 全量重建 (touch 所有源文件强制 ninja 重编), UF2 = 138752 字节
+   (原始 46j = 137728, 差 1024B 为编译时间戳/SDK 工具链差异, 行为等价);
+   13 处回退全部 Select-String 验证通过, 无 lastCollapseCheck/
+   lastReleaseCheck/relCheckStart/boostLevel 残留
+ 构建: Nyanithm_round46j_restored.uf2 (138752 bytes)
+ 备份: 46v 源码备份保留在 ..\backup_round46v_20260801_215853\
+ 说明: 46j 基线不含防塌陷/恢复器/sticky 三档/100ms 验证窗口;
+   保留 round45p 冻结 (3s 周期) 作为唯一长按塌陷防护
