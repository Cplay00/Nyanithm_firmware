### 20260907 round88b: 修复 0xBA 烧录轮询 I2C 读形状误用（P0，真机复现）

- **根因（真机日志确认）**: 1.6.5 首版烧录序列中 CTRL_CMD 轮询/STATUS/ERR/复位恢复读全部写成了 `i2c_write(nostop=true) + i2c_read(nostop=true)`。`i2c_read` 的末参是 RP2040 SDK 的 **nostop**（保持总线不发 STOP），不是"阻塞"语义——该组合在整个会话中从不产生 STOP，CY8CMBR3116 无法按标准组合事务应答，轮询永远读不到有效值 → 误判 `CTRL_CMD timeout, burn aborted.` → **不发软复位** → 新配置停在 RAM 未生效，触摸行为错乱（真机复现：烧录报 timeout 但随后的 verify ok 反而通过，即 128B 已写入 RAM 且 0x0C 可读，仅命令完成判定失败）。
- **修复**: `production_mode.cpp` 新增 `mbrReadReg()` 统一走 `i2c_write_read`（原子 write+restart+read 组合事务、末尾 STOP，与 verify/0xC6 读回/驱动数据路径同形，round75/80 真机验证过的形状）；烧录轮询、前置忙检查、STATUS/ERR 判错、复位恢复探测、`detect3116` 全部切换到该形状。全仓扫描确认无其他 `nostop=true` 读残留。
- **验证**: 编译通过（UF2 SHA1 DC219041…）；round50 理论迁移测试 117/117。真机回归见配套面板 round88b 条目。


- **版本**: `NYANITHM_FW_VERSION` 1.6.4-beta1 → **1.6.5**（用户指示：正式版号，不再标 beta）。
- **新增 0xC7 MBR3116 单传感器调试通道**（仅配置模式受理，需固件 ≥ 1.6.5）:
  - 协议（`nyanithm_shared.h` CMD_MBR3116_DEBUG 注释为唯一真相源）: 主机分两次 write——先 `[0xC7]`，再 2B 载荷 `[addr][sensor]`（与 0xC6/CFG_SET 双写铁律同源）。地址白名单 0x40-0x44（0x37 工装地址不受理），sensor 0-15。
  - 固件流程严格对齐 TRM §1.5.79 SENSOR_ID: 写 `SENSOR_ID(0x82)=sensor` → 泵 tud_task+watchdog 等待一个扫描周期（40ms，不阻塞 sleep）→ 突发读调试区 `0xDB..0xE7` 13 字节 → 校验 `SYNC_COUNTER1(0xDB)==SYNC_COUNTER2(0xE7)` 且 `DEBUG_SENSOR_ID(0xDC)==sensor`（防撕裂/陈旧帧）→ 回 11B 二进制 `[0xC7][addr][sensor][sync][cp][diffL][diffH][baseL][baseH][rawL][rawH]`（全小端）。失败回文本行 `3116 debug fail`。
  - 用途: diff 与悬空高度单调相关（实测 2mm≈255 饱和、10mm≈0），配合面板寄存器悬浮窗实时读数做悬空带定标；diff 走 16-bit 全量程，不再受 0xB1 的 8-bit 裁剪。
  - `hw_devices.h` 补 `extern MBR3116D/E`（v2 双主控 0x43/0x44 对象此前仅 cpp 内部可见）。
- **0xBA 烧录序列官方化修正**（`production_mode.cpp program_cy8cmbr3116_custom`，逐条对标 TRM/Datasheet）:
  - 删除"连写两次 0x02"违例（TRM §1.5.80: CTRL_CMD 非零时写入行为未定义），改为**单次写入**。
  - 以**轮询 0x86 归零**判命令完成（官方语义: 设备完成命令后自动清零），上限 500ms（Datasheet NVM 写 ~220ms），期间泵 tud_task+watchdog；取代旧盲等 10ms。
  - 完成后读 `CTRL_CMD_STATUS(0x88)` bit0 判错；出错再读 `CTRL_CMD_ERR(0x89)` 报具体码（0xFD Flash 写失败 / 0xFE CRC 不符），**出错绝不发 0xFF 软复位**（评审建议采纳）。
  - 软复位后轮询 I2C 恢复（读 0x51==addr，最长 200ms）再返回，verify 读回不再撞启动期 NAK（评审建议采纳）。
  - 写入前检查 CTRL_CMD==0，非零（前命令未完）放弃本次烧录并打印。
- **配置说明**: 用户决策「模板表不动」——6 张内置模板（`cy8cmbr3116_cfg.h`）保持原样；抗悬空参数组（ATH_EN=0/FT=200/HYS=25/NNT=80/NT=80/LBR=50/debounce=2/ARST=20s）由面板侧预设下发，随 config.bin 保存链路落 NVM（见面板 round88 条目）。
- **验证**: 编译通过（UF2 SHA1 E2C8EE33…）；round50 理论迁移测试 117/117 PASS；Mock 0xC7 应答与面板全链路见面板条目。**0xC7 实读与烧录序列真机验证待用户在场**。


- **按键背景板透明根因修复**: 修复输入测试轮询开启（默认「原始结果」模式 0xC5 level 2）后按键背景板（`background: var(--c-surface-2)`）变为透明完全消失的严重缺陷。
  - **根本原因**: 在 `rawSession`（原始结果）模式下，未触摸时通道读数 `v` 为 0（MPR121）或底噪 ~128（MBR3116）；此前逻辑无条件计算 `op = Math.min(1.0, Math.max(0.0, v / 100.0)).toFixed(2)`（MPR121 算得 `0.00`）或 `op = on ? ... : "0.35"`（MBR3116 算得 `0.35`），并直接赋予 `tc.style.opacity = op`。导致所有未激活按键的内联样式被设置为 `style="opacity: 0.00;"`（MPR121）或 `style="opacity: 0.35;"`（MBR3116），覆盖了 CSS 规则，使整个按键物理背景板完全透明隐形或半透明变淡。
  - **修复逻辑**:
    1. **未激活状态（`!on`）规范化**: 无论在「原始结果」还是「模拟结果」模式下，只要按键未处于激活态，必须保证 `tc.style.opacity = ""`（清空内联 opacity），恢复 CSS 默认的 100% 不透明实心背景板，同时正常显示当前通道底层物理读数或空白。
    2. **按键高亮亮度线性映射**: 仅在按键被触碰激活并加上 `.active` 类时，才根据读数或压力赋予内联 `style.opacity`：
       - **MPR121**: 0~100 线性提升亮度，触碰激活保底 0.20 亮度，达到 100 时为 1.00 满亮（游戏标准 128 二值判定直接 1.00 满亮）。
       - **MBR3116**: 依照原始触摸值 0~255 线性变化（`Math.min(1.0, Math.max(0.0, v / 255.0))`），达到 255 满亮 1.00（游戏标准 128 二值判定直接 1.00 满亮）。
    3. **停止测试清理**: 暂停输入测试或断开连接时，主动调用 `clearInputDisplay()` 重置所有 32 键的文本、高亮与内联透明度，确保待机状态整洁一致。
  - **发布部署**: `ControlPanel_v1_refit_dev/build.py --release` 同步部署至 `Nyanithm_firmware_hw_v1/ControlPanel_v1.html`（经自动断言 `devDiag == 0`）；仿真测试 `_dev_tools/verify_input_test_opacity.py` 全部通过，理论迁移测试 117/117 全过。

### 20260907 round86c: MPR121 输入测试触摸亮度 0-100 线性映射

- **MPR121 输入测试亮度 0-100 线性映射**: 在输入测试中，针对 MPR121（27寸）按键，背景亮度（opacity）按 `Math.min(1.0, v / 100.0)` 映射；0~100 线性提升亮度，触摸值达到 100（及以上，包含游戏标准 128 判定）时背景亮度达到最大 1.00 满亮；未激活键保持暗色；MBR3116 保持 0~255 量程。

### 20260907 round86b: 长按连接瞬间弹窗 + 输入测试模拟结果完全对齐游戏真实输入

- **长按连接设备即刻弹窗**: 移除松手才弹出的滞后体验与 `if (!lastPort)` 拦截导致 null 报“设备连接失败”的致命缺陷；500ms 到点直接在定时器中弹出系统设备选择列表；取消选择静默返回不报错误。
- **输入测试「模拟结果」对齐游戏真实呈现**: 固件 level 1 与常规游戏模式统一：未触发判定按键恒为 0（不高亮）；触发判定按键呈现游戏实际输入（未开启真实压力时为标准 128 二值，开启时为归一化动态压力）。
- **输入测试「原始结果」**: level 2 直报物理层读数，供底噪观测与硬件调试；校准向导改走 level 2 原始物理采样，避免受触发门限影响。
- **双机实刷验证**: COM1 (27寸) 与 COM6 (32寸) 均已刷入 UF2 CDA38A2B，模拟结果静置 32 键全 0 验证通过。

### 20260907 round86: 面板 1.1.1-beta5——12 项 UX 重组（校准统一路由 + MBR 区收缩 + 分区锁定）+ 固件 1.6.4-beta1（0xBC 普通模式探测）

- **固件（1.6.4-beta1, UF2 71A928B8…）**: 0xBC 硬件探测下放普通模式（chuni_io.cpp `cdc_respond`，与配置模式 app_link.cpp 处理器共存）。同 6 字节帧；差异：普通模式**不扫 ToF mux 通道**（Core0 updateAir 独占 mux 通道状态，探测后归位会打断 ToF 轮询），tofCount 按 hwVer 推导并置 flags bit2=1 标记「推导值」（面板显示「按机型」防误读为实测）；mprMask/mbrMask/mux 在位为实扫。Core1 阻塞最坏 ~1.7s 安全：i2c_port 自旋锁与 Core0 触摸扫描串行（round80 0xBE/0xBF 先例），Core0 持续喂狗。版本串 `NYANITHM_FW_VERSION`+`pico_set_program_version` →1.6.4-beta1；chuni_io.cpp 补 `hardware/watchdog.h`。round50 迁移测试 117/117。
- **面板 12 项（用户逐条反馈清单）**:
  1. MBR 高级配置默认不可达→校准入口统一到「一键校准阈值」（项 7 合并解决）。
  2. **分区锁定**: 未进配置模式时四个 config-section（通用设置/参数调节/高级参数/MBR）整体 `config-locked`（opacity .45+grayscale+pointer-events:none），进入后解除；控件级 disabled 门禁不变。
  3. **附加延迟数显缺失根因修复**: HTML id 拼写错误 `rgLatencv`→`rgLatencyv`（少 y），`$(slider.id+'v')` 查找落空致 odometer 从未创建（Playwright 实证复现）。
  4. MBR 参数标题英文注释（THRESHOLD/SENSOR_DEBOUNCE/SENSITIVITY）移入 `slider-label-en` 副标题。
  5. **参数区按主控显隐**: `data-mpr-only`（触摸/释放阈值、触发/释放消抖——dt/dr 固件只写 MPR setDebounce）与 `data-mbr-only`（芯片阈值 THRESHOLD、触摸/释放消抖 SENSOR_DEBOUNCE、灵敏度 SENSITIVITY、悬空抑制）由 updateTouchUi 按 ckMbr3116 切换；AIR/亮度/延迟/ToF 恒显。
  6. **普通模式硬件探测**: 固件 1.6.4-beta1 后连接握手即自动 0xBC（`normalModeProbe`），「硬件探测」信息卡连接即填充；旧固件静默失败回退配置模式探测；探测判定 hwVer 与 0xBD 不一致时以探测为准提示。
  7. **校准统一路由**: 「一键校准阈值」点击→重新探测→路由：MPR 在位→既有统计校准；MBR 在位→悬空阈值校准向导（intro 三屏文案动态填充，采样走环形倒计时 running 屏，峰值/建议阈值走 result 屏，废弃 confirm()/alert()）；双芯片在位（罕见串扰）按 ckMbr3116 裁决。探测在每次点击时刷新（设备可换）。
  8. 校准按钮移至导入/导出行下一行全宽（`.btn-cal-full`）+魔棒矢量图标+hint 更新双路由说明。
  9. 「退出并重启」confirm() 弹窗→按钮内两段式「继续？」（3s 复原，与保存并重启一致；setEnterExitBtnLabel 支持 overrideText）。
  10. **连接串口单击/长按分流**: 单击=自动回连上次设备（localStorage `nyanithm.lastPortIdx` 记录枚举序索引；单设备直连）；长按 600ms=强制弹系统选口列表（pointerdown 定时器，600ms 内抬起按单击）。注：同 VID/PID 双设备 Web Serial 无稳定唯一 ID，跨刷新记忆按枚举序尽力恢复。
  11. **INPUT TEST 状态栏 sb3 大数显按主控切换**: 标签 TH TOUCH↔THRESHOLD（sb3en/sb3en2），数值源 rgThTouch↔mbrThreshold；updateStatusBar 互斥守卫（3116 下 rgThTouch 动画不回写 sb3 防串台；mbrThreshold change→sb3 联动）。
  12. **MBR 区收缩+全芯片联动**: 阈值滑条迁入参数调节区、消抖/灵敏度迁入高级参数区（均「同步调整所有目标芯片」）；预设按钮移除（功能并入滑条）；新增「同步烧录全部芯片」（逐片 0xBA 双写+300ms 间隔；仅全部表 trusted 可用——0xC6 读回/已烧录=trusted，模板/导入/手改=untrusted，拒绝用模板覆盖芯片 NVM 实值）；每芯片表缓存 `mbrTableCache`+脏标记 `mbrDirty`；切换目标芯片有未烧录修改→confirm 询问立即烧录或放弃（放弃回退模板）；mbrSection 只留 目标芯片/从芯片读取（读回即置 trusted）/完整寄存器表。
- **验证**: 编译通过（UF2 246784B SHA1 71A928B8）；round50 117/117；Mock 全链（Playwright/Edge）：id 修复→odometer 2 位、锁定 4 区→配置后 0 区、MPR 显隐/3116 显隐翻转、sb3 TH TOUCH(006)↔THRESHOLD(128/160 联动)、MPR 校准两轮→报告→应用→自动进配置、MBR 路由（补丁探测帧 MBR-only）→悬空采样(峰值160)→建议 200→确认→进配置→回填 mbrTh=200+gate=130、退出两段式「继续？」、release devDiag=0。
- **真机（双机均刷 1.6.4-beta1）**: COM1=27寸 hw1 MPR×3 / COM6=32寸 hw2 3116@0x40-42，普通模式 0xBC 探测全过（flags bit2 推导标记确认）；0xC5 level 2/0 回显双机 PASS；0xBD 版本串 1.6.4-beta1 双机确认；COM6 配置读回 gate=165 xorOK、0xC6 回读 THRESHOLD=132/DEBOUNCE=0x1/CRC A71A 正常。**交互项（校准路由实际触发/寄存器区操作手感/长按选择器）未经用户在场验证**。

### 20260907 round85: 面板 1.1.1-beta4——原始/模拟报送切换 + 悬空截止校准 + SENSITIVITY 滑块 + 真机回归

- **输入测试「原始结果/模拟结果」切换按钮**（0xC5 level 2/1）: 默认「原始结果」=固件 0xC5=2（pressureSnap 原值直报、免 MPR ×2）；点击切「模拟结果」=0xC5=1（round80 报送语义）。`setRawReportMode(level)` 替代布尔版（旧 `setRawReport(on)` 保留为校准向导兼容入口，恒走 level 1）；RAW= 回显按 0/1/2 正则解析；格子 title 标注「原始/模拟」；原始档 sensorMax 恒 255。门禁 `rawLevelFwSupport`（cmpVersion≥1.6.3-beta1）：旧固件按钮隐藏不发送、`setRawReportMode` 自动回退 level 1。会话态不落 localStorage，断连/重连复位。
- **悬空截止校准**（MBR3116 配置区按钮）: 普通模式采样（0xC5=2+0xB1 帧流取全板原始 diff 峰值 P；0xC2 对 3116 无效故不走），芯片阈值=clamp(1.25×P, 128, 200)（对齐 Infineon「阈值=80% 信号量」逆向；datasheet 合法区间 31-200）；联动回填配置页悬空抑制滑块→130（噪声兜底位，主闸门移交芯片阈值）；确认后自动进配置模式回填 mbrTable+rgMbrGate，提示「应用于手台」(0xBA 烧 NVM) +「保存并重启」(0xB5) 两步持久化。峰值<60 判为无效采样拒绝回填。
- **MBR 三件套**: ①阈值滑块上限 220→200（datasheet 合规修正）；②新增 SENSITIVITY 滑块（0x08-0x0B，每字节 4 传感器×2 位字段写入器 `mbrWriteSensitivity`，保留同字节其他传感器位；0=0.1pF 最灵敏→3=0.4pF 最迟钝；hint 注明本机 0x40+ 出厂已 0.4pF 无上调余量）；③rgMbrGate hint 改为「主闸门=芯片阈值，本门=噪声兜底 ~130」；抗悬空预设文案随动。0xC6 从芯片读取与 SENSITIVITY 滑块值联动（`syncMbrFields` 显示 byte0 sensor0 档位）。
- **Mock 升级**: 版本串两处→1.6.3-beta1；`rawReport` 存 level（0/1/2，SET handler 未知值保持现态，与真机同构）；inputFrame 增 level==2 免 ×2 分支；0xC4/0xC5 回显 RAW=0|1|2；悬空截止校准 Mock 全链走通（含峰值过低防护路径）。
- **版本清扫**: KNOWN_V1_FW+="1.6.3-beta1"、PANEL_VERSION=1.1.1-beta4、build.py 标题 beta2→beta4 陈旧串修正。
- **真机回归（COM1=27寸 hw1 MPR / COM6=32寸 hw2 三片3116，均刷 1.6.3-beta1 UF2 2CF09582…）**:
  1. **0xC5 level 双档**: COM1 level=2 空闲原始 diff ≤10、level=1 同帧 ×2 后 ≤20（×2 报送差异实证）；0xC4 正确回显 RAW=1/RAW=0。COM6 level=2 原生直报（MBR 两档等价）确认。
  2. **gate 现状勘定**: COM6 gate=200 持久化在场（字节90），空闲最强悬空键 tc1/3/5 原始 diff 110-122 < 芯片阈值 128+滞回 → BUTTON_STAT 不置位 → 0xC0 链路 hw/raw/out 全零，游戏路径无穿透。**用户报告的"游戏内悬空触发"未在本次窗口内复现**（需用户在场复测：多次悬停扫描 0 事件，判定用户当时不在设备旁）；gate 已按新语义调至 130 并 §8 回归 PASS。
  3. **§8 保存回归**: COM6 gate 200→130 → CFG_SET `check ok` → CFG_SAVE → 0xB9 重启 → 回读字节90=130 xorOK=True。
  4. **0xC6 芯片回读**: 0x40 阈值 132（自定义表）、0x41/0x42 阈值 128、SENSOR_EN=0x0FFF、SENSITIVITY=0xFF（0.4pF 满档，SENSITIVITY 滑块无上调余量的实测印证）。
  5. **遥测基线**: COM1 loopMin=941us/loopMax=7270us、COM6 1052/5480us（<1/3 周期预算，I2C 负载健康）；两台 verifyFail 全零。
- **待用户配合的交互验证**: ① COM6 悬停复现（游戏误触发高度，hoverscan/0xC0 双通道）+ 手掌滑动 slide 连续性 A/B（gate=130 vs 0）；② COM1 tc8-10 轻点 tapscan 判据（峰值谱 vs verifyFail[15/11/16]，空闲态 tc8-10 峰值 0-3 干净）；③ 游戏内手感 A/B。
- Mock 全链验证（?mock=1）: 连接→握手→1.6.3-beta1 门禁→轮询 level2/1 切换（×2 差异实时可见）→校准全链→配置模式 MBR 滑块/校准按钮门禁。

### 20260907 round84: 固件 1.6.3-beta1——0xC5 三级报送（原始/模拟）+ MBR slide 连续性（车道锚定 gate 豁免）

- **背景（用户实机反馈 round80-83 交付后的三问题）**: ① 3116 悬空抑制（mbrTouchGate=200）在游戏内仍见悬空触发——勘察确认游戏/HID 路径全部走验证后 touchData32，gate 生效无疑；根因是近距悬空（≤2mm）diff>200 且 0.5mm 饱和 255，物理上与实触不可分，纯软件门无法根治，主闸门须上芯片侧 FINGER_THRESHOLD（本轮先行交付 slide 连续性与报送基建，芯片校准随 round85 面板落地）。② 3116 掉 slide（手掌糊板判定丢失）——gate 预检（v2:542/v1:939）位于邻格快路径之前，slide 过渡弱边缘（diff∈[baseK,gate)）被一并拒掉。③ 压力报送需「原始结果/模拟结果」双视角。
- **0xC5 三级报送**: `rawReportMode`(bool)→`rawReportLevel`(u8)：0=关、1=模拟（round80 语义：MPR ×2 clamp255）、2=原始（pressureSnap 原值直报免 ×2；MBR 原生 0-255 两档相同）。rawModeActive()=level≠0||gameRaw；0xB1 替换层 level==1 走 ×2、level==2 直通；0xC4/0xC5 回显 `RAW=0|1|2`（会话级优先，无会话时 gameRaw 折算 1）；未知载荷保持现态（旧面板 0/1 兼容不变）；断连/进配置模式清零挂钩沿用（usb_device.cpp:71-75、chuni_io.cpp 配置模式入口）。gameRaw（cfg2 bit1）分支未动。
- **MBR slide 连续性——已确认邻居锚定 gate 豁免**（v1/v2 双镜像，评审裁定弃用「同芯片任一电极」方案）：新增 `prevLaneStretched[32]` 游戏车道空间上一周期拉伸位快照（拉伸后位投影，v2 经 V2_LANE_M/E 于 prevStretched 更新后、v1 于共享拉伸段后仅 MBR 模式投影）+ `MBR_NEIGHBOR_GATE_MARGIN=40`。MBR verify gate 预检处：车道 k±1（经 laneTable，跨芯片边界天然覆盖）上一周期有已确认输出且 rejectTh>baseK+40 时，rejectTh 下放为 baseK+40。下放后进入与 gate=0 完全相同的 fast/skip/档位分类链；gate=0 零变化；悬空带首格永无已确认邻居，无法自我锚定。3116 电极物理乱序（v2 布局 tc1/2 与 tc15/16 同芯片），同芯片判定会致「按住一侧、另一侧悬空穿透」，故必须用车道空间。MPR 分支本轮不动（先诊断）。
- **版本**: 1.6.3-beta1（`NYANITHM_FW_VERSION` + `pico_set_program_version`；cmpVersion 对「≥」门禁天然成立，弃用字面 1.6.1-beta3 与不可区分的 1.6.2-beta2）。API_LEVEL 0x10 不变。
- **验证**: 编译通过（UF2 246272B，SHA1 2CF09582…）；round50 迁移测试 **117/117**（新增 S29 车道锚定豁免+S30 无锚悬空带保持拒绝+S31 下放 floor 语义+S32 报送 level 1/2；S29 断言按既有 neighborWasActive skip 路径语义修正）。review-agent 只读复查 P0/P1=0（采纳：更新日志补条目、2 处 rawReportMode 注释残留清理；备案：锚定车道悬空防护降级至芯片阈值+滞回≈140 属设计取舍，真机 A/B 列入 round85 验证清单）。**未经真机验证**——芯片校准联动、0xC5 level2 端到端、锚定手感 A/B 待实机。

### 20260906 round83: 0xC6 响应截断修复（实机定位）+ round80-82 实机验收进展

- **0xC6 截断修复（固件，app_link.cpp）**: 实机（COM6，32寸 v1）定位 0xC6 回读仅 86B 到达主机后停滞——`putchar`×128 + 单次 `stdio_flush` 只把第一个 EP 包（64B）送出，TX 环剩余部分无人泵出（CFG_READ 未暴露是因尾部 printf 文本顺带泵出）。修复：改 `tud_cdc_write` + 循环 `tud_task()`/`tud_cdc_write_flush()` 直到 128B 全部送出。复测 128B 单次 0.06s 完整到达。
- **实机验收（两台 v1：COM1=27寸 MPR121 / COM6=32寸三主控 MBR3116，均刷 1.6.2-beta1，UF2 SHA1 539B602E…）**:
  1. **刷写链路**: `0xBB+0xA5` 合并单笔写 → 1.2s 进 BOOTSEL → UF2 写盘 → 原端口重枚举，两台各一次成功（pyserial 台架，round78e 形态）。
  2. **§8 保存回归（COM6）**: gate=200 → CFG_SET `check ok. config changed.` → CFG_SAVE → 0xB9 重启 → 回读字节 90 = 200 且 xor 有效——**mbrTouchGate 跨重启持久化通过**。
  3. **0xC6 芯片回读（COM6）**: 0x40 芯片 128B 完整回读（THRESHOLD=0x84=132——用户芯片此前已烧过自定义表≠内置模板 128，实证「从芯片读取」先看再改的价值）；0x43（v1 布局无此芯片）正确回文本 `3116 read fail`。
  4. **遥测**: 0xC5 会话 + 0xB1 帧流 COM6 ~180fps；COM1 空闲噪声 ×2 尺度 ≤28（raw ≤14，与 round47b 噪声谱吻合）；COM6 键 1/2/27/29 空闲差值 69-120（悬空敏感键，gate=200 生效压制中）。
  5. round50 迁移测试 101/101（drain 修复后复跑）。
- **待物理手感验收（需人工）**: ① 32寸 悬空距离 A/B（gate=200 vs 0）② 折返 flick/slide 手感 ③ 27寸 MPR flick 快路径（raw≥50）手感 ④ 面板端到端（Web Serial）真机连接。

### 20260906 round80: 固件 1.6.2-beta1——悬空抑制门 + 强信号快路径（折返漏检）+ MPR 压力 ×2 报送 + 0xC6 配置回读

- **背景**: ① v1 3116 悬空触发：芯片 NVM 阈值 0x0C=128 使 BUTTON_STAT 在 PCB+亚克力上方 ~4mm 即置位，而固件验证基线（80）比芯片还松，悬空直通（用户实测 4mm 开始触发、0.5mm 饱和 255）。② 折返 flick 漏检：旧版芯片位当周期直出（flick 全过、slide 漏），现版差值验证+2 周期确认+孤立电极 +1 惩罚（hw_devices.cpp v1:944 / v2:585）使孤立强 flick 首次输出 ~2 周期 ≈5.4ms，1-2 扫描周期的折返触点在确认前熄灭；MPR 分支同构。③ MPR 压力∝接触面积（用户实测手指 30-50、手掌 ~130，原生到不了 255），旧游戏模式 ×4 硬饱和破坏线性。
- **配置结构**（nyanithm_shared.h，128B 总长/xorSum/cfgVer 0x02 不变）: `reserved[37]` 首字节改为具名 `mbrTouchGate`（字节偏移 90，面板同步）——MBR 悬空抑制门：0=完全旧行为（向后兼容）；>0 时 MBR 新触摸 ON 验证门 = max(verifyBaseK, mbrTouchGate)，仅作用触摸判定（release/sticky 不变），v1（hw1/hw2）与 v2（hw3/hw4）双布局同样生效。slide 过渡信号同受此门约束；gate≥200 时快路径有效阈值同步抬到 gate 值（max 语义，预期）。
- **强信号快路径**: MBR diff≥200（`MBR_FLICK_FAST_TH`，真按压量级、~1.6x 于 4mm 悬空信号）且非 I2C 重试读 → 置 `fastOk`，确认循环对快触**豁免孤立 +1 惩罚**→ 首周期输出（恢复旧版零延迟语义）；MPR 快阈值 `max(50, sw_th+4)`（raw，=报送尺度 ×2 后 100，覆盖实触手指/手掌，噪声谱 diff≥14 有 ≥3.5x 裕度）同机制。**40ms 释放反弹守卫**: 上次确认后 40ms 内的 stale fastOk 不豁免，防止松手残余信号二次触发。SKIP≥300 邻格路径、STRONG/MEDIUM/WEAK 档位、CONFIRM/STRETCH/STICKY 全部不变（弱/中信号过滤精度不变）。
- **MPR 压力 ×2 报送**（chuni_io.cpp）: 面板实时压力（0xC5 会话）与游戏 raw 模式（cfg2 bit1）统一 min(raw×2, 255)——替换游戏模式旧 ×4；MBR 原生 0-255 不变；缩放仅在报送层，pressureSnap/0xC2 调试仍为原始值。**行为变化**: MPR 用户压力值减半且保持面积线性（手掌 255 饱和、手指 60-100）。
- **新增 CMD_READ3116CONFIG (0xC6)**（仅配置模式）: 载荷 [addr]（**主机必须分两次 write：先 [0xC6] 再 [addr]**，与 CFG_SET 双写铁律同源），白名单同 0xBA；成功回 128B 二进制（0x00-0x7F 原值，0x7E/0x7F CRC 不重算），失败回文本 `3116 read fail\n`。固件侧 4×32B 分块读（对齐 round75 verify 的 write-stop+read 事务形态，规避 128B 单次突发风险），块间泵 tud_task+喂狗。
- **版本**: 固件 1.6.2-beta1（`NYANITHM_FW_VERSION` + `pico_set_program_version`）。0xC6 与 mbrTouchGate 面板侧按 cmpVersion≥1.6.2-beta1 门控（round81）。
- **验证**: 编译通过（UF2 245248B，SHA1 9C44FA99…）；理论迁移测试更新至 **101/101**（新增 round80 场景 S21-S28：MBR 快路径首拍/边界/199 不快、40ms 反弹守卫双边界 39ms/42ms、悬空门拒 150 过 250、gate=0 回归、MPR 快路径 60/40、逐键 48+4 不被绕过、×2 报送钳 255；S1/S2/S9/S12/S14 按新契约调整并注明）。SecReviewer 只读复查 Approve-with-fixes（P0/P1=0；采纳 P2-1 注释矛盾修正、P2-2 双写契约注记、P3-1 裕度数字、P3-2 slide 门约束注记、P3-5 注释出处）。**未经真机验证**——gate/flick 联动调参、×2 报送实测、0xC6 回读、保存回归（§8）待实机。

### 20260831 round79: 输入轮询管线优化（补记）

- 面板输入轮询循环解除与串口事务锁/渲染的刚性耦合（配置读取/保存/心跳执行时立即让步，错误路径区分让步与退避）；固件 Core1 seqlock 自旋超时 5ms→1ms、HID 键盘回报间隔 5ms→1ms（1000Hz）。UF2 随 round80 基线构建（SHA1 1215B663…）。

### 20260831 round78f: 固件 1.6.1-beta2 / 面板 1.1.1-beta2 发布——RPI-RP2 句柄持久化与零弹窗全自动写盘

- **背景**: round78e 解决进入 BOOTSEL 后，用户实测发现 BOOTSEL 盘符出现后浏览器不自动刷入（Edge 没有在 BOOTSEL 挂载后弹出目录选择框）。根本原因是 Chromium 的 `showDirectoryPicker()` 安全模型：仅在用户点击手势发生后约 5 秒内有效，BOOTSEL 设备重启挂载（通常需 4~6 秒）后激活已过期，导致弹窗被直接拦截抛出 `NotAllowedError`。
- **面板修复（RPI-RP2 目录句柄持久化 + 零弹窗自动写盘）**:
  1. **句柄持久化存储**: 首次选择并校验通过的 RPI-RP2 目录句柄（File System Access API）自动存入 IndexedDB（`nyanithm_v1_storage:rp2_dir_handle`），下次刷写直接读取复用。
  2. **激活期内权限验证**: 点击「开始刷写」瞬间（激活最 fresh 阶段）立即尝试取出句柄并完成 `queryPermission` / `requestPermission`；首次刷写无存储句柄时，在点击瞬间提前唤起选择框（与刷写命令发送并行），用户点选后直接入档。
  3. **零弹窗自动刷入**: 固件进入 BOOTSEL 模式后，面板全自动轮询 `INFO_UF2.TXT` 就绪，无需任何弹窗直接将 UF2 镜像写入 RPI-RP2，写完后自动重新连接串口并比对版本，实现全自动无人值守刷机闭环。
  4. **版本发布升级**: 控制面板版本更新为 `1.1.1-beta2`（标题、页头徽标、Known FW 白名单全面更新）；固件版本更新为 `1.6.1-beta2`（`CMakeLists.txt` / `nyanithm_shared.h`）。
- **验证**:
  1. 固件编译构建 `Nyanithm_v1.6.1-beta2`（UF2 大小 242176 字节，SHA1 `7DCCBB93...`）。
  2. 理论迁移测试 `test_round50_migration.py` 43/43 测试用例全部通过（覆盖 68 个断言）。
  3. 控制面板 Release 版构建（`devDiag == 0`，SHA1 `18402352...`）。
  4. Electron 真实台架 Mock 回归测试全链路通过（解析 1.6.1-beta2 -> 进 BOOTSEL -> 自动模拟写盘 -> 自动重连验证版本通过）。

### 20260831 round78e: 刷写确认改合并单笔写 + 35s 周期重发——Electron 真实 Chromium 台架定位宿主栈扣写

- **背景**: round78d（30s 武装窗 + 0xCD）后用户复测仍「✗ 设备未进入 BOOTSEL」。本次建成 **Electron 真实 Chromium 串口台架**（`_dev_tools/round78e_electron/`，session `select-serial-port` 自动授权，无 chooser UI），端到端驱动部署版面板对真机刷写，两次完整复现用户失败时序。
- **定位（台架页面侧 write 捕获 + OS 侧 COM/盘符地面真相）**: 面板在**同一毫秒连发三笔单字节写** `[0xBB][0xA5][0xC3]` 后，三笔全部被宿主栈扣住不达设备（>11s，面板报错；一轮 60s 内从未送达直到配置模式自动退出重启，另一轮 closePort 后 ~2s 才送达触发迟到 BOOTSEL——与用户"设备最终躺在 BOOTSEL"现象一致）；而**所有间隔发出的单笔写（0xB3/0xBC/0xB4）均瞬时到达**。Chromium 源码级调研（独立调研报告：blink `serial_port_underlying_sink` → `serial_port_impl` → `serial_io_handler_win` 四层）证实写路径无任何批处理/延迟定时器、`writer.write()` resolve 仅代表字节进入 mojo 管道、`port.close()` 走 `PurgeComm(PURGE_TXCLEAR)` 是**丢弃而非冲刷**——"报错后 closePort 冲出滞留字节"的旧设计假设不成立。
- **面板修复（`runFlashSequence` 重写）**: ① 0xBB+0xA5 **合并为一次 write**（一个 chunk → 一次 WriteFile → 一个 USB OUT 包，WICG/serial #166 维护者确认的"立即发完"正解；与台架实测所有能瞬时到达的单笔写同形态），删除尾随 0xC3 冲刷写（重复引入连写形态）；round76b"必须分两笔写"结论仅适用旧阻塞确认窗，固件 round78c 起状态化确认对合并/分离写均兼容。② 验证窗 11s→**35s**（覆盖固件 30s 武装窗），期间**每 1.5s 重发一次合并对**——台架实测第一笔合并写仍可能被扣 ~1.6s（与 0xBC DETECT 探针时长吻合），重发的那笔独立单笔写瞬时送达即触发 boot；每笔收到的 0xBB 亦重置配置模式 60s 自动退出计时，长窗内设备不会自行掉线。③ 等待期间**绝不 closePort**；35s 到期才报错并保留「手动选择盘符写入」。
- **台架侧工程记录**: Electron ≥26 `select-serial-port` 签名为 `(event, portList, webContents, callback)`（旧 5 参签名会静默失败）；portList 的 `vendorId/productId` 可能是字符串需 `Number()` 化；`requestPort()` 无条件走 chooser，自动化授权须走 session 事件或企业策略 `SerialAllowUsbDevicesForUrls`（`getPorts()` 免 UI）。
- **验证**: ① **Electron 台架端到端全链路 PASS**（真 Chromium + 真机）：合并写 t=2633 → 重发 t=4245 送达 → 56ms 后设备断开 → 5.97s RPI-RP2 出现 → 台架自动写入 UF2 → 7.9s 设备带 round78d 固件回连 COM1，面板进入「已进入 BOOTSEL」阶段，全周期 ~10s（守卫加入后复跑 PASS，时序一致）。② Mock 严格终态回归 PASS（MockPort 合并写即消费 → BOOTSEL 模拟断开 → 终态「已进入 BOOTSEL」3.94s）。③ 固件零改动（round78d be8db90 原样），round50 触摸回归豁免（触摸管线未触及）；`?mock=gen2` 门禁未触及豁免。release 面板部署双树 SHA1 E473EA33…（devDiag=0）。
- **只读审查（SecReviewer）**: ✅ Approve 无 P0/P1。采纳 1×P2 + 1×P3：P2=窗内固件软重启（配置 60s 自动退出）会被"端口消失"误判为 BOOTSEL 成功——新增判别守卫（真 BOOTSEL 下 CDC 口不以同 VID/PID 重枚举；+2.6s/+6s 两次采样 `getPorts()`，回归且持续在线 → 判软重启报错不弹盘符框；回归后又消失=滞留字节迟到触发真 BOOTSEL，继续成功路径；Mock 的 getPorts 每次发新假端口，门控跳过）；P3=deny 文本检测改在断开判定之前（断开清理会清 readBuffer）。P3 备案未改：重发写无独立超时（35s 死线在循环顶部，写挂起时 UI 停留"正在进入 BOOTSEL"）、失败路径 closePort 后不再自动重连（提示语已引导手点连接）、Mock 无法复现宿主扣字节病理（真机回归已由台架覆盖）。

### 20260831 round78d: 武装窗 30s + 0xCD 刷写诊断——覆盖 Chrome 滞留直到关口才冲出的最坏时序

- **背景**: round78c（10s 武装窗）后用户复测仍「✗ 设备未进入 BOOTSEL」。0xCE 命令日志取证：设备端 `BC B4 BC BB A5 C3`——0xBB 与 0xA5 **均已到达设备**但未 BOOT，说明 BB→A5 在设备端的间隔超过 10s（过期 deny）或 boot_flashing 静默失败，二者无法从既有痕迹区分。
- **固件**: ① 新增 **`CMD_FLASH_DIAG (0xCD)`** 事后诊断：`[0xCD][code][rc][gap u32 LE]`，code=0 无/1 已尝试 boot/2 窗口过期 deny/3 杂字节 deny，rc=flash_safe_execute 返回码，gap=设备端 BB→确认/超时的毫秒数——任何静默失败此后可精确归因（正常模式与配置模式都有该处理器）。② **武装窗 10s→30s**：Chrome 的滞留时长不可控（实测 >5.5s，且 `closePort` 冲出积压意味着"最坏情况下 0xA5 在面板放弃之后才送达"），30s 窗保证关口冲出的迟到确认依然触发 BOOTSEL。rc 记录进 boot_flashing 两次尝试。
- **面板**: 验证窗 5.5s→11s（覆盖 closePort 前的滞留释放），失败路径保留「手动选择盘符写入」并提示设备可能迟到进 BOOTSEL。
- **验证（真机，Built Aug 31 2026 02:12:21，UF2 242176B SHA1 C999DBBB…）**: `round78c_window30_test.py` 12s 迟到确认 → 13.2s 进 BOOTSEL（旧 10s 窗会 deny）；`round78c_closeflush_test.py` 模拟"面板 11.5s 放弃关端口→冲出滞留 0xA5"最坏时序 → 13.5s 进 BOOTSEL；0xCD 诊断实测 `code=3 deny-wrongbyte gap=301ms` 与注入场景一致；round50 68/68。release 面板部署 SHA1 1193C72D…（devDiag=0）。Playwright 实机串口台已搭（`_dev_tools/round78c_pw_harness.js`，持久 profile 预置授权），chooser 预置授权格式待 Chromium 源码核实后启用。

### 20260831 round78c: 0xBB 确认改状态机——10s 武装窗、0xA5 任意时刻落地即触发

- **背景**: round78b（尾随 0xC3 冲刷写）后用户实测仍「✗ 刷写被固件拒绝」——0xA5 确认字节被 Chrome 串口栈滞留**超过 5s**（旧阻塞窗全灭），冲刷写也不可靠（Chrome 的滞留并非"下一笔写必冲出"）。
- **修复（固件，双模式）**: 0xBB 从"阻塞等确认"改为**状态机武装**——`flashingArmed` 开启 10s 窗口后立即返回（正常模式不再阻塞 GET_INPUT，配置模式不再阻塞控制台）；0xA5 无论何时落地（10s 内）都立即触发 BOOTSEL（正常模式经 `pending_flashing` 交 Core0）；期间其它字节 → 立即 deny（配置模式该字节按旧确认读语义吞掉，正常模式继续分发）；超时后下一个字节触发 deny 文本；宿主重试 0xBB 重新武装。round51 防误触强度不变（非 0xA5 不启动）。删除 `readCdcPayload(5s)` 阻塞等待与正常模式 2s 阻塞等待。
- **面板**: 失败路径保留「手动选择盘符写入」入口并提示——`closePort` 会冲出 Chrome 滞留字节，设备可能在报错后"迟到"进 BOOTSEL，此时手动路径仍可用。
- **验证（真机，Built Aug 31 2026 01:19:56，UF2 240640B SHA1 930D403F…）**: `_dev_tools/round78c_statemachine_test.py` 全过——n-delay（正常模式 0xA5 迟到 4s → 5.2s 进 BOOTSEL，旧固件 2s 即 deny）、c-delay（配置模式 0xA5 迟到 6s → 进 BOOTSEL，旧固件 5s 即 deny）、deny（0xBB+杂字节 → 立即 deny 文本）、expire（10.5s 静默 → 精确 deny + 设备存活）、retry（双 0xBB 重武装 → BOOTSEL）；round50 68/68；postflash 3/3。release 面板部署 SHA1 D7DB1EB6…（devDiag=0）。Chrome 真机时序由用户实测确认（rig 无法复现 Chrome 批处理）。

### 20260831 round78b: 面板刷写再修——0xA5 确认字节被 Chrome 串口栈滞留，尾随 0xC3 冲刷写

- **现象**: round78 后用户实测仍失败——面板 3s 重启验证报「✗ 设备未进入 BOOTSEL」，报错关闭端口后设备才"迟到"地断连进 BOOTSEL（用户所见"异常断连"）；INPUT TEST 的 API LEVEL 显示 0xb0（响应流错位旁证）。
- **根因（机制链闭环）**: Chrome Web Serial **没有 flush 语义**——最后一笔 write 的尾字节滞留在浏览器串口服务/驱动缓冲里，直到下一笔主机写或 ~4s 惰性超时才真正发出。面板流程 [0xBB][0xA5] 两笔写后不再有写 → 0xA5 滞留 → 固件确认等待期内设备不动 → 面板 3s 验证窗超时报错 → `closePort` 冲出积压字节 → 设备此时才进 BOOTSEL。pyserial `write()` 无 flush 也 51ms 内送达（rig 实测），**rig 永远复现不出该时序**；round76b 的"双写修复"只是让 [0xA5] 从"永远被扣"变成"~4s 迟到"，恰在固件 5s 确认窗边缘，故此前一直偶发。round78 的"先验证重启再弹框"把窗口矛盾显性化（3s 验证窗 < 4s 滞留），从偶发变必现。
- **修复（仅面板）**: ① `runFlashSequence` 在 [0xBB][0xA5] 之后**补发一笔尾随 0xC3（CFG_KEEPALIVE）冲刷写**——"下一笔写冲出上一笔滞留字节"是 round76b 已实证的物理；0xC3 配置模式=静默心跳、正常模式=无分支忽略，滞留亦无害；尽力而为（设备若在两写间重启离开，writer 已置空，try/catch 兜底）。② 重启验证窗 3s→5.5s，且循环内检测到 `flashing denied` 文本即提前退出精确归因（正常模式 deny ~2s / 配置模式 ~5s 均可捕获）。
- **验证**: Mock 全链路（双写+0xC3→模拟断开→wentAway→bootsel-manual）通过；Mock 顺带暴露并修复"设备在 [0xA5]/[0xC3] 间离开导致 writer 置空抛 未连接 误报"竞态。round50 无固件改动免跑。**Chrome 真机时序验证待用户实测**（rig 无法复现 Chrome 批处理行为）。release 面板部署 SHA1 0F14B3C2…（devDiag=0）。

### 20260830 round78: 刷写引导修复——正常模式受理 0xBB + BOOTSEL 失败不再静默 + 面板重启验证

- **现象**: round76b 修复后实机仍偶发「面板刷写流程走完但 RPI-RP2 盘符不出现」。
- **根因（真机复现定位）**: ① **正常模式 0xBB 被静默忽略**——只有配置模式 Core0 `handleCommand` 受理 0xBB，正常模式 Core1 `cdc_respond` 无此分支；面板 `btnStartFlash` 的门槛是面板自身 `configMode` 标志，一旦与设备失同步（60s 空闲自动退出重启、保存后重枚举等），0xBB+0xA5 落空、设备纹丝不动，面板却照常弹盘符框。诊断脚本 `_dev_tools/round78_bootsel_repro.py`：正常模式双写 → 全静默无重启（复现）；配置模式双写 → 1.2s 进 BOOTSEL（对照）。② **`boot_flashing()` 忽略 `flash_safe_execute` 返回码**——SDK 在跨核 lockout 握手失败时跳过擦除并返回错误，旧代码照样 `reboot()` → 回到正常固件 → 无盘符（第二静默路径）。③ 实验发现：`rom_reset_usb_boot(0,0)`（bootrom 规范入口）**从 Core1 直接调用会把 Core1 卡死**（真机复现：USB 栈冻结、无盘符、无复位），已弃用该路径并回退到已验证的 Core0 擦除向量表机制。
- **固件修复**: ① 正常模式受理 0xBB：Core1 有界 2s 读确认字节（泵 `tud_task`），0xA5 → 置 `pending_flashing` 交 **Core0** 执行擦除（Core0 调用方 + Core1 lockout victim = 已验证路径；Core1 不得自行执行），否则回 `flashing denied` 文本；0xA5 确认要求（round51 防误触）双模式保留。② `boot_flashing()` 检查返回码：失败重试一次（1000ms 窗），仍失败**留在固件内不重启**（正常模式 Core0 不 printf，防 round51 单驱动竞争），主机侧「未检测到重启」验证据此给出明确报错。
- **面板修复**: ① `runFlashSequence` 盲发改为**重启验证**——发完 0xBB+0xA5 后轮询最多 3s 等「端口自行消失」（readLoop 意外断开处理置 `connected=false`），确认设备真的重启才弹盘符框；超时则给出具体错误（读缓冲含 `flashing denied` 时提示"刷写被固件拒绝"），不再无提示地打开空盘符弹窗。② **Mock 确认链路重写（round76b 遗留缺陷）**：旧实现把同 write 的 0xA5 也当"武装"字节、跨 write 的第二笔 [0xA5] 落到 `unknown command`——确认链路从未走通；现按 cfgSetPending/mbrPending 同模式顶部 pre-check（`flashingArm`/`flashingConsume`），0xA5 触发 BOOTSEL 模拟断开（`dev.closed`），denied 后剩余字节回到命令分发。
- **审查轮加固（SecReviewer 只读复查，结论采纳 2×P2 + 同型顺手修，其余 P3 备案）**: ① `boot_flashing` 失败重试路径在两次 1000ms lockout 握手等待间插入 `watchdog_update()`——否则该"留在固件报错"路径自身会击穿 2s 看门狗复位、把静默失败原样带回；② 正常模式 0xBB 确认等待改减法回绕安全比较（49.7 天 uptime 后 `now+2000` 直接比较会坍缩为 0）；③ 同文件同型顺手修：`CMD_SET_RAW_REPORT` 100ms / `CMD_SET_LED` 500ms 有界等待同样的回绕隐患。P3 备案未改：normal 模式擦除失败时宿主无字节反馈（面板笼统报错可接受）、deny 确认窗 2s 内 GET_INPUT 停服务（有界取舍）、错位流 0xBB+0xA5 相邻即刷写的既有暴露面扩展（round51 同等，DLL/面板流不受影响）、Mock 确认窗 5s 对齐配置模式值（Mock 流程走配置模式路径，语义正确）。
- **关键发现补充（真机）**: rom 入口从 **Core0（配置模式路径）调用完全正常**（0.5s 进 BOOTSEL）——卡死仅限 Core1 调用，坐实「擦除必须 Core0 执行」的路径约束；Core1 直调的卡死态需物理重插恢复。
- **验证**: 固件 UF2 240640B SHA1 F4AD644C…；`test_round50_migration.py` 68/68；Mock 刷写全链路（选 UF2→校验→自动进配置→双写→模拟断开→重启验证→盘符阶段）通过；release 面板部署 SHA1 3CC9623C…（devDiag=0）。**真机验收全过**（COM1，Built Aug 30 2026 23:23:35）：正常模式 0xBB+0xA5 → 1.17s 进 BOOTSEL（本次修复新能力）；配置模式 → 1.16s（回归）；正常模式 deny（0xBB 无确认）→ 2.0s `flashing denied` + 设备存活；配置模式 deny → 5.0s `read timeout`+`flashing denied`（round76b 语义保持）；postflash 3/3（0xBD 版本 / 0xB1 轮询 11 帧 / 0xB4 配置 xor-valid）；BOOTSEL↔UF2 回刷全周期 ×4。

### 20260830 round77: 版本发布——固件 1.6.1-beta1 / 控制面板 1.1.1-beta1

- **固件**: `NYANITHM_FW_VERSION` 与 `pico_set_program_version` 升至 **1.6.1-beta1**（0xBD 版本串与 NYANFW1 签名随宏自动更新）。1.6.0→1.6.1 累计内容：round75（0xBA/0xBB 有界读取+地址白名单+烧录校验）、round76/76b/76c（LampArray DTR 抑制、刷写双写+5s 确认窗、0xBC 探测喂狗）。
- **面板**: 确立版本标识 **Panel v1.1.1-beta1**（`PANEL_VERSION` 常量），显示于页头品牌区徽标 + 页面标题；门禁白名单 `KNOWN_V1_FW` 加入 `1.6.1-beta1`；Mock 版本串同步。`cmpVersion` 对带后缀补丁段按 0 处理，`1.6.1-beta1` 等价 `1.6.0` 通过全部 ≥1.6.0 功能门禁。
- **验证**: 固件 UF2 239616B SHA1 9813B9DB…（Built Aug 30 2026 21:37:36）真机刷入，0xBD 报 `FW:1.6.1-beta1`、输入轮询与配置回读正常；Mock 全链路（标题/徽标/门禁/版本门禁）；round50 68/68。面板 SHA1 见 git。

### 20260830 round76c: 真机全链路回归——0xBC 门禁探测看门狗复位隐患修复 + 面板门禁超时扩容

- **背景**: 设备在手，按 AGENTS §8 对 round74-76b 全部改动做真机回归（`_dev_tools/round76c_realtest.py`，字节级复刻面板流程）。
- **真机回归通过**: 配置保存→0xB9 重启→回读 **128/128 字节一致**（§8 保存回归，覆盖 app_link.cpp 改动）；刷写双写 0.3-0.5s 进 BOOTSEL + UF2 回刷全周期 ×3；0xBB 拒绝路径（5s 窗超时 + denied）；0xBA 地址白名单拒绝（0x00 → "not allowed. burn aborted."）；0xB1 轮询 33B 帧界零残留；0xC5 压力会话开/关；0xB2 五色扫描无卡死；0xC3 静默语义；0xBD/0xB0。**特征化**: 单笔 129B CFG_SET 在当前固件仍复现尾包扣压 → round63i 双写铁律保持必要。
- **发现并修复（P1 级既有隐患，非近期回归）**: `app_link.cpp` 0xBC 门禁探测在 handleCommand 内执行 ~15 次 `findI2CDevice`（缺席设备 10 次重试 × I2C 事务超时）+ mux 通道扫描，**真机实测耗时随总线状态在 1-6s 波动**，而阻塞期间 `updateInputState()` 停喂 2s 看门狗 → 越线即复位、面板表现为"连接不上"。修复: 探测循环内逐次 `tud_task()` + `watchdog_update()`（`readCdcPayload` 同款模式）。面板门禁 `readConfigStream` 本有 500ms 轻推 + 总超时设计，真机数据支持总超时 5s→8s（本轮回随面板发布）。
- **残留（需目标硬件）**: 0xBA 真实 3116 烧录（本机 mbrMask=0 无 3116 芯片，白名单与协议路径已验）；DTR→LampArray 抑制的视觉确认；游戏内 DLL 字节序。
- **验证**: 真机 COM1 全套件 15/17（2 个 FAIL 为主机侧 usbser 写扣压测试伪影——0xBC 应答帧已逐字节证实正确 `[BC 07 00 04 01 01]`，面板轻推模式不受影响）；round50 68/68。固件 UF2 239616B SHA1 BDAF7216…；release 面板 SHA1 E3EDB3F1…（devDiag=0）。

### 20260830 round76b: 修复面板刷写无法进入 BOOTSEL——0xBB/0xA5 双写 + 确认窗 500ms→5s

- **现象**: 控制面板「更新固件」无法让实体设备进入 BOOTSEL，RPI-RP2 盘符不出现。
- **根因（真机 A/B 定位）**: 面板将 0xBB 与 0xA5 合并为单笔 2 字节 write；usbser 把 0xA5 尾包扣住不发，直到下一笔主机写才被"冲"出来（真机实测迟到 ~4s，与 round63i CFG_SET 尾包物理同源）。round75 之前的无界 `getchar()` 会无限等待，该宿主行为从未暴露；round75 的 500ms 有界确认窗将其变成确定性失败：固件 500ms 内看不到 0xA5 → `flashing denied` → 永不进 BOOTSEL。诊断脚本 `_dev_tools/round76_bootsel_diag.py`：场景 A（单笔写）真机复现 `read timeout at byte 0 of 1` + `flashing denied`；场景 B（两次独立写）0.5s 内进 BOOTSEL。
- **修复**: ① 面板 `runFlashSequence` 改两次独立 `writeBytes`（round63i 铁律），MockPort FLASHING 处理器同步状态化（`flashingPending` + 5s 超时，跨 write 等确认字节）；② 固件 0xBB 确认窗 500ms→5000ms（`readCdcPayload` 全程泵 `tud_task()`+`watchdog_update()`，容忍 ≤5s 的尾包扣压；超时仍 denied，不 wedge）。
- **影响**: 正常刷写路径面板日志不变；行为差异仅在错位/扣包流下确认窗更宽容。新面板 + 旧固件（500ms 窗）组合同样修复（双写即时到达）。
- **验证**: 真机 COM1（fw 1.6.0 Built Aug 30 2026 20:34:45）：双写 0.5s 进 BOOTSEL，UF2 经 RPI-RP2 回刷恢复并复验两轮；Mock 全链路（选 UF2→校验→自动进配置→双写→无 denied/unknown→盘符阶段）；`test_round50_migration.py` 68/68。固件 UF2 239616B SHA1 D0CAAF22…；release 面板 SHA1 97E597D5…（devDiag=0）。

### 20260829 round76: 修复 LED「无论如何只显示蓝色」——DTR 在位抑制 LampArray 抢写 + 面板主题默认色收敛

- **现象**: v1 3116 三主控用户报告灯带无论选择什么颜色最终都只显示蓝色。
- **根因**: 固件中唯一能覆盖用户所选颜色的机制是 Windows 动态照明（HID LampArray）：`chuni_io.cpp` standby 超时在 CDC 静默 5s 后落 `game_connected=false`，`lamp_array_apply` 立即放行 Windows 按其配置色（系统强调色默认为蓝）重刷整条灯带；cfg2 bit0 默认 0 = LampArray 默认开启。面板 LightTest 单帧 0xB2 之后只要输入轮询停止/断开/设备重启，5s 后必被刷回 Windows 色；固件此前无法区分"面板仍握着端口"与"主机已离开"。帮凶：round71 面板亮色主题 accent `#0D44D4`（蓝）在页面加载即覆盖 LightTest 取色器默认值、主题切换还会主动向设备推帧。已排除：固件全树无硬编码蓝（rainbow 蓝色扇区为死代码）；面板/固件 0xB2 字节序一致（均 b,r,g）；WS2812 GRB 输出正常。
- **修复**: ① `chuni_io.cpp` standby 超时改为仅当 DTR 已断开（`!tud_cdc_connected()`）才回落 `game_connected=false`；DTR 仍高（面板/游戏 DLL 握着端口）则刷新 `connected_time` 继续抑制 LampArray，端口真正关闭后才恢复 Windows 接管（保留桌面氛围灯用途）；宿主崩溃不关口则抑制持续到重插。② 面板 `openPort` 打开后显式 `setSignals({dataTerminalReady:true})`（try/catch 包裹），不依赖浏览器默认行为；MockPort 补 no-op。③ 面板 round71 主题跟随收敛：新增 `ledColorTouched`（预设/取色器/自定义发送时置位），用户选过颜色后主题加载/切换不再覆盖取色器、不再主动推帧。④ 动画运行中取色从静默忽略改为日志提示「先停止动画」。⑤ 审查轮加固（SecReviewer 只读复查，无 P0/P1）：`setSignals` 失败不再静默（记 warn 日志）；`closePort` 关闭前显式 `setSignals({dataTerminalReady:false})` 兜底（不依赖 OS 反断言 DTR，防"关闭面板后动态照明永不恢复"）；动画中取色警告按会话去重（`ledAnimWarned`，`stopLedAnim` 复位）防拖拽取色器刷屏；亮色主题下初始取色器 stale 对齐（主题 IIFE 应用已存主题后补一次 `syncLedDefaultColor(false)`）。
- **影响**: 面板/游戏会话期间动态照明全程让位（原先事务间隙 5s 即抢写）；无主机开端口时行为与旧版一致。DTR 判定依赖宿主打开端口后拉高 DTR（面板已显式设置；游戏 DLL 依赖其持续 0xB2 流量，逻辑不受影响）。
- **验证**: 编译通过（UF2 239616B，SHA1 EF890529…）；`test_round50_migration.py` 回归通过；面板 Mock 全链路通过；release 面板部署 SHA1 3D0E439F…。**未经真机验证**——「面板开着时 Windows 不抢写 / 关闭面板 5s 后恢复接管」需真机确认；终端用户自诊断：白光测试显示白色=链路与字节序正常（转查 Windows 设置→个性化→动态照明或勾选面板「关闭 Windows 动态照明」）；白光异常=字节序/面板来源问题（核对面板版本）。

### 20260829 round75: 修复 0xBA/0xBB 无界 getchar() 看门狗复位，3116 烧录链路加固

- **现象**: v1 3116 三主控用户在面板修改 3116 配置并保存时串口报 "The device has been lost"、设备复位；阈值无法烧录、经常连接不上、游戏内触摸/抬手卡住（坏状态芯片令 i2c0 事务吃满 5ms 超时、Core0 循环退化 50-80ms/周期，且 MBR 管线无卡键恢复层）。
- **根因**: `src/app_link.cpp` 的 0xBA（1+128B 载荷）与 0xBB（0xA5 确认字节）用无界 `getchar()` 读取——底层 busy-wait 不泵 `tud_task()`、不喂看门狗。载荷中途断流（主机停滞，或单次 130B 写被 Web Serial/USB 层拆 [64][64][2] 后尾包在单缓冲 OUT 未重武装时被 NAK）即令 Core0 永久卡死，`updateInputState()` 停喂，2s 硬件看门狗复位、USB 重枚举。round63c 已在 0xB7 上修过同类问题，0xBA/0xBB 漏修；round63i 双写铁律未覆盖 0xBA。复位若落在芯片 NVM 写入期间（datasheet 最长 ~120ms，原序列仅盲等 10ms）可致 3116 配置半写入损坏。
- **修复**: ① 新增 `readCdcPayload()`（`tud_cdc_read()` + 每迭代显式 `tud_task()`+`watchdog_update()`，总时限超时回错误行并中止），0xBA 载荷 1s / 0xBB 确认 500ms（超时=flashing denied）；② 0xBA 增加 I2C 地址白名单（0x37/0x40-0x44），错位垃圾流不再能以任意地址编程；③ 新增 `verify_cy8cmbr3116_burn()`：烧录软复位后有界轮询芯片恢复（典型 ~300ms，最坏 ~600ms，泵狗）并回读阈值寄存器 0x0C 与写入值比对，追加 `verify ok / verify mismatch / verify skipped (chip busy)` 诊断行。
- **影响**: 正常烧录路径响应行序不变（"programing 3116 chip"/"done" 仍在原位，末尾多一行 verify 诊断，面板兼容）；超时/错位流从"2s 复位"变为"错误行+继续运行"。旧面板 + 新固件组合会在 done 后多出一条未读诊断行（各事务入口均先清接收缓冲，不污染后续事务）。配套面板改动（0xBA 双写、烧录校验行展示、意外断开自动重连、按钮代号清理）另行部署；旧固件下仅更新面板无法避免复位，最终用户需同时刷新固件。另：Mimosa git 门禁对 `tools/elf2uf2.py` 的 argv 写入模式为模式级硬拦（argv 化、白名单收敛、字面量文件名写盘均不被认可），经决定自本轮起 `tools/` 整体移出版本控制、仅保留本地（.gitignore 同提交）；本地 tools/elf2uf2.py 最终形态为**无 argv 的 stdout 过滤器**（固定 build 目录内 Nyanithm.elf/Nyanithm.uf2 字面量文件名、UF2 二进制走标准输出，脚本内零文件写入；build_firmware.ps1 以 Start-Process 重定向原样落盘），转换输出与原版逐字节一致（UF2 239104B 复现验证），随本地文件继续生效。
- **验证**: 编译通过（UF2 239104B，SHA1 DDCDB365…，最终构建）；`test_round50_migration.py` 回归通过；面板 Mock 全链路通过；**未经真机验证**——真机 0xBA 三芯片烧录、保存并重启、断开自动重连回归待硬件在手。

### 20260829 round74: 修复 USB 字符串描述符未按 UTF-16 扩宽导致的设备名乱码

- **现象**: Chrome Web Serial 串口选择器里 COM 口旁设备名显示为 CJK 乱码（iProduct "Nyanithm Controller" 被主机解码为 "祎湡瑩浨䌠湯牴汯敬r" 一类）。
- **根因**: `src/usb_descriptors.c` 的 `tud_descriptor_string_cb` 对 STRID_MANUFACTURER / STRID_PRODUCT / STRID_HID_KBD / STRID_HID_LAMP / STRID_CDC 五个字符串用 `memcpy` 把 ASCII 字节直接拷入 `uint16_t` 缓冲区，未扩宽为 UTF-16-LE；小端下两个 ASCII 字符并成一个码元，主机解读为 CJK 码点（离线复现："Catium"→"慃楴浵"），且 bLength 声明 chr_count 个码元、实际只写入 ceil(n/2) 个，尾部还暴露静态缓冲区残值。
- **修复**: 新增 `ascii_to_utf16le()` 逐字节扩宽辅助函数替换五处 `memcpy`；LANGID（两字节对本就构成 0x0409）与 SERIAL（`board_usb_get_serial` 按 UTF-16 写入）分支不变；bLength = 2*chr_count+2 现与实际写入码元数一致；最长字面串 21 字符 < 32 码元上限，无越界。
- **影响**: 仅 USB 枚举字符串显示变化；VID/PID、HID/CDC 功能、面板连接与保存自动重连（按 VID/PID 过滤）不受影响。hw_v1 与 cplay 两变体 `usb_descriptors.c` 保持字节级一致。
- **验证**: 双变体编译通过（UF2 各 238080B，SHA1 2D73AEFA… / 89F8989F…）；py 脚本按主机视角复现旧路径解码为乱码、新路径正确；选择器/设备管理器显示效果需重刷 UF2 后真机确认（未经真机验证）。

### 20260828 round72: 控制面板多项易用性与动画/几何缺陷修复 (72+72b+72c+72d)

- **1. 倒计时数字残缺/移出 ring 裁剪容器**: 校准向导 `.cal-ring-num` 从带 absolute + radial-mask 的 `.cal-ring` 容器内移到 `.cal-ring-wrap` 兄弟级, 解决数字被 mask 切割且无法 flex 居中的视觉残缺。
- **2. Odometer 旋转数显重写 (幽灵9 / 9↔0 几何 / 双写杀死)**:
  - 几何修复: 12 格条带 `[9][0..9][0]`, 静止位对齐 `translateY(-(k+1)em)`; 尾部 0 副本端点由 -12em 修正为 -11em(72b, 解决飞出空白后闪现 0);
  - 双写保护 (72c): `setOdometerValue` 在同目标落点时不触发 `odoFinalize`, 解决逐键气泡通用+专用双 input 监听对同值连写导致 9↔0 wrap 动画当帧被杀死的缺陷;
  - 首次进配置模式同步 (72d): `smoothSliderTo` 程序化动画落地时广播 `notifySliderSettled` 刷新阈值展示、警告文案与橙红变色, 解决 unpack 窗口内格子显示旧默认值 6/4 与警告缺失的问题;
  - HTML 警告 span 归位: `thTouchWarn`/`thReleaseWarn` 及 `dtWarn`/`drWarn` 移入所属 `slider-row`, 消除附加延迟下方错位。
- **验证**: release 面板双树部署 SHA1 `36522802…` (devDiag=0); Playwright mock 逐帧采样证实 9↔0 双向 28-40 帧连续过渡、无空白闪现; 注入 13/12 diff 验证首次进配置格子/警告/变色全部落地对齐。

### 20260827 round71: 控制面板易用性重构 (向导/格子阈值/亮度映射/语义高亮/弹窗动画)

- **1. 一键校准向导弹窗**: 三屏向导 (intro 规则说明 / running 环形倒计时 + 取消 / result 确认并开始修改); Mock 下自动加速便于测试。
- **2. 配置模式 INPUT TEST 格子阈值展示**: 进配置模式后格子事件驱动显示当前生效的触摸/释放阈值(全局或逐键覆盖), 统一跟随主题 accent 颜色; 退出恢复基线。
- **3. GROUND SLIDER 格子亮度映射**: 按传感器原生量程(MPR121=63, MBR=255)线性映射透明度, 恒定 128 判定帧特例直达最高亮度 1.0。
- **4. 视觉与交互**: 低强调文字提亮 +15%; 核心按钮语义高亮(断开=红, 固件刷写=橙警示, 保存重启=绿); 更换 USB 插头与芯片刷写图标; 通用弹窗退出动画助手 `closeModalAnimated`; Light Test 默认色跟随主题。
- **验证**: release 构建双树部署, 真机 7/7 链路通过, review-agent 终审「可发布」。

### 20260827 round73: 1.5.3→1.6.0 全量审查收尾 (P1 基线锁存竞态修复 + 构建时间一致性保障)

- **背景**: 用户要求审查 1.5.3(bbfd92d)→1.6.0(工作区)全部固件改动并构建 hw_v1;且今后每次构建必须保证产物内嵌构建时间与系统时刻一致。范围=9 文件 +411/-77(round63f/j 已提交部分 + round64-71 工作区部分),双通道复核(agent 深审 + 全量人工通读)。
- **[P1] chuni_io.cpp updateGameRawBaseline 冷启动死锁存**: 原实现"首次命令时锁存 cfg2 bit1 基线",但 Core1 在 main()->initUSBDevice() 即启动并跑 cdc_respond(),早于 boot_mode 的 readConfig()——首次锁存读到的必然是全零 ControllerConfig,cfg2 bit1(游戏侧压力映射持久基线)在所有冷启动下永不生效。修复:删除 gameRawInited 锁存,改为每次 cdc_respond 逐值重算 `gameRawEnabled = !!(cfg2 & CFG2_BIT_GAME_RAW_SLIDER)`——附带收益:保存 cfg2 后无需重启即反映到基线(0xC5 会话语义不变)。**§2 同步**:cplay 树 chuni_io.cpp 同补丁。
- **[P3] GET_INPUT seqlock 超时路径撕裂快照门控(chuni_io.cpp)**: rawModeActive 替换块原先无条件执行,seqlock 超时(>5ms)时 tmpPressure 可能是栅栏切换中途的新旧混合,违背"超时保留 last-good"约定。新增 frameFresh 标志(仅 commit 成功置位),替换块改 `if (rawModeActive() && frameFresh)`;cfg3 持帧/超时路径原样保持上一服务帧。**§2 同步**:cplay 树。
- **已知缓办(P3,仅诊断面)**: raw 模式下 inputState.slider 持有压力值,cfg3 的 changed 判定(mempr vs 二进制 tmpSlider)恒真 → cfg3 附加延迟近似常数化、0xC1 riseCnt/fallCnt 按压力过零计数。不影响 DLL 字节契约与二进制模式;待实验模式反馈后统一重设计。
- **构建时间一致性保障(tools/build_firmware.ps1 双树)**: 每次构建前自动扫描 src/include 全部引用 __DATE__/__TIME__ 的编译单元(chuni_io.cpp/app_link.cpp,未来新增自动纳入)刷新 mtime 强制 Ninja 重编——此前增量构建会让 UF2 带着陈旧的 Built: 时间戳。新增 _dev_tools/check_fw_buildtime.py 校验产物(bin 中存在今天日期+当前时刻±10min 配对的字面量;pico-sdk 运行时区自带的日期字面量属 SDK 缓存日,如实列出不参与判定)。
- **审查结论(A-F 六域)**: 配置结构体(offsets/xorSum/128B static_assert/1.5.3 存量全零=继承全局兼容/sanitize 钳位)/看门狗(setConfig 泵+喂狗、C5 载荷等待有界)/USB-CDC 时序(RX256 vs 载入流/0xBD 串/stdio_flush)/触摸等价性(V1/V2 lane 表逐项验算、全零默认与 round50 数值等价、钳位只紧不松)均无问题;dispatch/Flash 路径未动。
- **验证**: 迁移测试 68/68;hw_v1 构建 UF2 SHA1 04AAD4EB98A32F71C4E92AD3681F65738495F827;ninja 日志证实 app_link.cpp 零改动亦被时间戳刷新触发重编;check_fw_buildtime.py = FRESH(delta 223s)。真机建议:刷入后 0xBD 应报 Built:当日时刻;若启用 cfg2 bit1,保存后重启前后各发一次 0xC4 确认 RAW=1(新旧行为分界点)。

### 20260827 round70c: 面板三项 (实时压力开关语义 + 校准报告 Toast + 配置容量确认)

- **1. 「压力报送&压力映射」→「实时真实压力&压力映射（实验）」** (body_main.html 标签 + startInputPolling 语义): 选中开关时同样发 0xC5 会话开关(0xC5 是面板会话、cfg2 bit1 是持久基线, 两者独立)——INPUT TEST 选中时显示实时真实压力, 不选中时显示默认 0/128 判定帧。停止测试即关会话开关(0xC5 0), 持久基线不受影响。
- **2. 一键校准报告改 HTML Toast(确认修改/取消)**: 校准完成不再 alert, 弹 Toast 展示建议全局 th_touch + 独立抬升键列表; 「取消」/× 直接关闭不做改动; 「确认修改」自动进入配置模式——calPendingBackfill 保护下 unpackConfig 不再用设备旧值覆盖回填(含全局 th_touch/th_release 滑条, 补 round67 遗漏), 用户点「保存并重启」即应用报告值。Toast 组件: body_main.html 新增 #calToastWrap + head_patch.css .cal-toast 样式 + app_ui.js showCalToast/escapeHtml。
- **3. 配置容量确认**: 128B 布局 thTouchKey[32](offset 26-57)+thReleaseKey[32](58-89)+reserved[37]+xorSum 已满, 独立阈值已存下但无余量; 暂不扩展 cfgVer(保持 0x02), 未来若加新字段可考虑双配置文件方案(独立阈值放第二文件)。
- **验证**: release 面板双树部署 SHA1 5beff691… (devDiag=0, calToastWrap=2, 实时真实压力=3); Mock 全链路: 校准 Toast 显示(建议 th_touch=13 + tc5:27/tc17:19) → 确认修改 → 自动进配置模式 → 表单回填 rgThTouch=13/rgThRelease=12(设备旧值未覆盖) → 无 JS 错误(仅 favicon 404)。真机 INPUT TEST 选中开关时的 128 空闲噪声为 MPR121 diff 物理特性(非 128 填充), 已按用户确认的「选中=实时真实压力」语义交付。
- 注: 自本轮起不再自动构建 cplay 固件变体(独立按键阈值已可由用户配置)。

### 20260826 round70b: API 回退 0x10 + 面板 0xC5 门禁改按固件版本 (真机 128 根因)

- **背景**: 实机 INPUT TEST 恒报 128。深查定论: (a) 工作区未提交改动把 NYANITHM_API_LEVEL 抬到 0x11——用户铁律: API 与硬件版本绑定(一代=0x10), 升级等于抛弃老设备, 绝对不允许; (b) 设备 Flash 内固件(08-26 15:24 编译)早于 round66, 无 0xC4/0xC5/pressureSnap, 对 0xC4/0xC5 零响应——须重刷; (c) 面板部署版 gate 为 api>=0x11, 真机 API 0x10 永不发 0xC5 → 永远 128。
- **固件**: 双树 NYANITHM_API_LEVEL 0x11 → 0x10(0xC4/0xC5/pressureSnap 均不依赖 API 数值, 保留)。新固件 0xBD 应报 FW:1.6.0 API:0x10。
- **面板**: rawReportSupported 门禁 api>=0x11 → fw>=1.6.0(能力门禁, API 不再作命令开关); 删除 app_core.js 死常量 API_LEVEL=0x11; 一键校准文案「API 0x11+」→「固件 1.6.0+」。
- **验证**: 双变体编译通过(cplay SHA1 5043AABD… / hw_v1 E21E0AE3…); 迁移测试 68/68; release 面板双树部署 SHA1 41b89505… (devDiag=0, 0x11 残留=0); Mock 全链路通过; **真机待重刷后复测**。

### 20260826 round70: 面板逐键UI收尾 (移除网格 + 气泡复用滑条样式 + INPUT TEST 始终真实压力)

- **1. 移除参数调节区逐键网格**: perKeySection 32 格总览区块(HTML/JS/CSS 含 .pk-cell)整体删除; 仅保留单行提示「配置模式下点击 INPUT TEST 按键格子可编辑该键阈值」(perKeyHint, 配置模式+固件支持时显示)。逐键编辑入口收敛为 INPUT TEST 格子点击。
- **2. 气泡 UI 与参数调节完全同构**: pkBubbleModal 改为复用参数调节区 slider-row 结构——触发阈值/th_touch (per-key)、释放阈值/th_release (per-key) 各含 slider-label(中文)+slider-label-en(英文)+slider-value(odometer 滚动数字)+slider-track(滑条)+field-hint; 数显走既有 initSliderAnimations odometer 路径(2 位), 新增 setPkVal() odometer 感知写值(兼容"继承"文本)。
- **3. INPUT TEST 始终报真实压力(根因修复)**: 此前仅手动点「开始测试」会发 0xC5 会话开关, 连接/保存重启重连/刷写重连三条自动路径不发——真机 GET_INPUT 只能回 1.5.x 二进制帧(按下 128/松开 0), 表现为「无论开关如何 INPUT TEST 只报 128」。新增 startInputPolling() 统一入口(自动开 0xC5 压力会话 + 启动轮询), 替换 connectBtn/保存重启重连/刷写重连/校准向导四处重复启动逻辑; cfg2 bit1 游戏映射开启时仍不覆盖(显示帧中映射值, round69 语义保留)。
- **验证**: 面板 release 构建(devDiag=0); Mock 全链路: 连接即显示真实压力(tc1:当前 7-11 / 阈值提示)、气泡开合/滑条联动/恢复继承/保存重启重连后压力保持、gen2 门禁 3×ToF 拦截; 迁移测试 68/68; 无 JS 错误(仅 favicon 404)。已部署两树 ControlPanel_v1.html(SHA1 28eb781f53c9b846e5c18f3b26b48b5d002ec7fc)。
- 真机待办: 连接后自动开压力会话在真机的 CDC 时序回归(§8 保存链路); 逐键值真机读回一致性。

### 20260826 round69: 面板四项改进 (API门禁放宽 + 逐键气泡UI + 压力报送&映射单开关)

- **1. API 门禁放宽**: 面板 enterConfigMode 的 API 检查从 `api !== 0x11` 硬拒改为 `api < 0x10` 才拒——API 0x10(1.5.x)/0x11(v1.6) 均属一代固件, API 只是硬件/协议标识, 不应挡旧固件连接。autoHandshake 的 `lvl >= 0x10` 视为匹配(不再红字), rawReportSupported 仍按 `api >= 0x11`(功能门禁)。
- **2. 逐键副标题去版本号**: `per-key (v1.6)` -> `per-key`(body_main.html)。
- **3. INPUT TEST 点击弹气泡(UI 整合)**: 逐键编辑器从右侧面板迁入 INPUT TEST 交互——配置模式下点击任意按键格子(tc1-32)弹出模态气泡(复用 .flash-modal 动画), 内含触发/释放滑块(复用参数调节区样式)+ 恢复继承/关闭按钮; 新增关闭过渡动画(.closing 反向 fadeOut/fadeUpOut)。右侧 perKeySection 保留 32 格总览网格, 移除内嵌编辑器。
- **4. 压力报送&压力映射单开关(固件双树)**: cfg2 bit1 语义升级——开启后游戏侧(GET_INPUT, chuniio_nyanithm.dll 消费)报**线性归一化 0-255 映射值**: 按下=映射压力(MPR diff ×4 clamp 255, 因其原生尺度窄 1-63; MBR DIFFERENCE_COUNT 原生 0-255 原样透传), 释放=0; 面板会话(0xC5 0x01)不受影响仍报真实原始压力。面板 INPUT TEST 在 bit1 开启时不自动发 0xC5 1(避免会话开关覆盖游戏模式填充), 直接显示帧中映射值(与游戏所见一致); 新增 perKeyGameRaw 运行时状态(pack/unpack 同步)。面板复选框文案改为「压力报送&压力映射(实验)」, Mock inputFrame 按 cfg2 bit1 模拟映射分支。
- **验证**: 双变体构建通过——hw_v1 UF2 236544B SHA1 D146D12D4CC5300EB7A623E7CCECAB16F9DD1D45, cplay UF2 237056B SHA1 C90C84A12FE452C7E0E9D9F61073C47B5D3C420A; 迁移测试 68/68; 面板 Mock 全链路: 进配置→点击 tc1 弹气泡(键 tc1)→关闭动画→勾选 ckGameRaw(点 label)→保存并重启→重连→INPUT TEST 显示映射值(50,53,42,24,26,10,10,9 非0/128 二值); 面板 release 已部署两变体(devDiag=0, IDENTICAL)。
- 真机待办: MPR ×4 映射系数需真机 A/B(实际 diff 分布校准); 游戏内 DLL 对 0-255 连续值的消费行为验证; API 0x10 旧固件连接回归。

### 20260826 round68: 游戏内压力报送 (cfg2 bit1 实验性, 双树)

- **背景**: v1.6 高阶目标——让 DLL/游戏侧直接消费压力值(部分游戏/工具对非零字节语义未验证, 标记实验性, 默认关)。二值兼容: 按下键报 max(1,pressure)、释放报 0, 与旧 0/128 帧在布尔语义上等价。
- **协议**: cfg2 bit1 = CFG2_BIT_GAME_RAW_SLIDER(0b00000010), sanitizeConfig 掩码扩展(与 bit0 LampArray 并列); 0xC5 0x00 改为回落到 cfg2 基线(不再硬回二值), 0xC4 查询报 active(rawReportMode || gameRawEnabled)。
- **固件**: 开机首次命令时 updateGameRawBaseline() 惰性同步 cfg2→gameRawEnabled(重启自动恢复); GET_INPUT 填充分支: rawReportMode(面板会话)=全 lane 原始压力, gameRaw(基线)=按下 max(1,pressure)/释放 0, 双关 = 二值帧与 1.5.x 逐字节等价。updatePressureSnap 条件同步(两者任一开即快照)。gameRawEnabled 定义在 hw_devices.cpp(round66 rawReportMode 同模式), chuni_io.cpp 仅 extern。
- **面板**: 高级参数新增"游戏内压力报送(实验)"复选框 ckGameRaw, packConfig/unpackConfig 读写 cfg2 bit1, 与逐键阈值同门禁(fw>=1.6.0, 旧固件置灰+清勾选, 防 sanitize 静默丢弃); BIT_GAME_RAW_SLIDER 常量入 app_core.js。Mock 的 0xC4/0xC5 同步 active 语义。
- **验证**: 双变体构建通过——hw_v1 UF2 236544B SHA1 DCC1D1AA24007EC6AFCFB0E8BC040301FEF6B665, cplay UF2 237056B SHA1 33E547D6686A1590EDEAF4ADC322494EA7C43268; 迁移测试 68/68; 面板 Mock 全链路: 进配置模式→勾选 ckGameRaw→保存并重启→自动重连(0xC5 active 语义)→重进配置模式读回勾选保持; 面板 release 已部署两变体(devDiag=0, IDENTICAL)。修复了 gameRawEnabled 在 chuni_io.cpp 与 hw_devices.cpp 的重复定义(round68 开发中引入, 双树各删除定义, 保留 hw_devices.cpp 唯一定义)。
- **四方审查修复**(review-agent + 双子代理交叉核对, 2026-08-26): (1) P1 cplay `updatePressureSnap()` 误嵌 `else{updateAir()}` 块内→IR 对射组件在位时压力快照永不执行, 已移出与 hw_v1 一致(无条件调用), cplay 重编译 SHA1 9162DB04; (2) P1 面板校准向导 round67 回填值在"进配置模式→保存并重启"路径被 unpackConfig 设备旧值覆盖→新增 calPendingBackfill 保护标志(回填置位/保存成功清除), 保证校准结果能落盘; (3) P2 校准 MBR 建议值低限用 rng.min(129) 而非 1, 不再生成 ≤128 的无效收紧建议(计划 D4.1); (4) P3 校准自动开启轮询时同步"停止测试"按钮状态。以上修复后面板 release 重建重部署, 迁移测试 68/68 复验通过。
- 真机待办: 游戏内压力报送需真机 HID 回归(0/非零 布尔语义等价性), DLL 直接消费压力值是否正常未验证; 与逐键阈值联动后的手感回归(§8); 校准向导"校准→保存"链路真机走查(本轮 Mock 逻辑验证 + 代码路径核对)。

### 20260826 round67: 面板压力模式 + 逐键阈值一键校准向导 (纯面板, 已部署)

- **压力模式**: 输入测试开启时自动发 0xC5 0x01(压力上报), 停止/断开时 0xC5 0x00; 固件 API>=0x11 门禁(0xAF 握手值), 旧固件自动禁用。renderInput 已按 0-255 渲染透明度/数值, 开启即得压力可视化; 每格叠加当前生效阈值提示(title: 当前值/阈值, 逐键或全局)。
- **一键校准向导**(移植 calibrate_thresholds.ps1 两阶段统计法): btnCalibrate 在设备状态栏(普通模式可见, 连接+非配置模式+API>=0x11 启用)。Phase 1 静置 20s 采每键 idleMax/直方图 → Phase 2 划全滑条 15s 采 touchMin/命中 → 建议: 全局 th_touch = clamp(p85(idleMax)+2, 1-63)(MPR)/钳 129-255(MBR); 异常键(idleMax+2>全局)设 thTouchKey = clamp(idleMax+2), 释放 = th-1(MPR)/继承(MBR); touchMin 缺失键提示"可能失灵"。结果回填逐键网格 + 全局滑条, 弹窗摘要, 用户确认后走"保存并重启"。
- **Mock**: MockPort 实现 0xC4(回 RAW=0|1
)/0xC5(状态化切换); inputFrame 按 rawReport 区分: 关=高斯扫(普通演示), 开=静止 idle + 坏键偏置(键5 +14 / 键17 +6), 保证校准 Phase1 idleMax 真实、坏键可命中; Mock GET_VERSION API 串同步 0x11。
- **验证(Mock 全链路)**: 连接→握手 API 0x11→开始测试(0xC5 自动开, title 阈值提示出现)→一键校准(Phase1 idleMax≈10-25 坏键偏高→建议全局 13; tc5 建议 27/idleMax=25, tc17 建议 19/idleMax=17)→弹窗→accept→进配置模式逐键网格显示 tc5 阈值 27/tc17 阈值 19/其余继承 13=回填正确。渲染无关改动仅 dev 版, --release 已部署两变体 ControlPanel_v1.html(devDiag=0, 两文件 IDENTICAL)。
- 真机待办: 压力数值与 0xC2 交叉验证; 一键检测在真实坏键设备建议值有效性; 保存回归(§6.3)。

### 20260826 round66: 逐键压力实时报送 — pressureSnap 管线 + 0xC4/0xC5 会话开关 + API 0x11 (固件, 双树)

- **背景**: v1.6 进阶目标——面板需要全键实时压力值做可视化与一键阈值检测向导; 游戏内则维持二值语义。会话态开关保证仅在控制面板连接时开启, 关闭零开销。
- **协议**: 新命令 CMD_GET_RAW_STATUS(0xC4)/CMD_SET_RAW_REPORT(0xC5, 跟1B 0/1), 应答文本行 RAW=0|1
(风格同 0xB8); API level 0x10 -> 0x11。仅普通模式(Core1 cdc_respond)生效。
- **压力快照(Core0)**: rawReportMode(volatile bool, Core1 置位)为真时每 5ms(PRESSURE_SNAP_INTERVAL_MS 分频, 与主循环解耦)批量读 diff 写 pressureSnap[32](钳 0-255): MPR 3 片 filtered+baseline 6 事务≈1ms(0xC2 同款读法); MBR 每片 1 次 0xBA(32B = 16 路 diff), v1 三片/v2 两片。写入在 touchStateGen 同一 seqlock 写侧, 关闭时零 I2C 开销。
- **GET_INPUT 33B 帧**: 帧长不变; rawReportMode 开时 32 slider 字节由压力值替换(seqlock 快照段同刻拷贝 tmpPressure, 与 touchData32 无撕裂), touchData32/HID/4k/6k/lamp 内部二值消费完全不动; cfg3 延迟的 memcmp 变更检测在压力模式下天然每帧触发(行为可接受, 文档化)。
- **生命周期**: 置位=0xC5 0x01; 清除=0xC5 0x00 / Core1 主循环检测 !tud_cdc_n_connected(0) / CMD_CONFIG_MODE 移交 Core0 前——三处兜底, 满足"仅连接控制面板时开启"; 重启默认关。
- **版本**: API 0x11 已入 nyanithm_shared.h 双树; FW 版本本轮不变(1.6.0)。
- **验证**: 双变体构建通过——hw_v1 UF2 236544B SHA1 A78F93657651E8F2EC8850097DD555F5F75C38B1, cplay UF2 237056B SHA1 59CD4A48A76CCA581000790F24AD137680FB3D74; 迁移测试 68/68(S19 二值语义已覆盖)。
- 真机待办: 压力数值与 0xC2 交叉验证; 0xC1 telemetry 确认 raw 开启时主循环周期预算(MPR +6 事务/5ms); 拔线/进配置/重启后 0xC4 查询确认自动关; 一键检测在真实坏键设备上的建议值有效性(§6.3)。

### 20260826 round64: v1.6 逐键阈值 — 配置字段 + 三消费点逐键化 + golden 等价测试 (固件, 双树)

- **背景**: 部分已售设备个别 slider 键电容压值系统性偏高(空闲噪声大、易误触), 现行固件只有一对全局 th_touch/th_release, 压制坏键会牺牲好键轻触手感(round45b 编译期硬编码表早被 round46 UNIFIED_THRESHOLD_V1=1 关闭)。v1.6 改为逐键可调, 配置由面板精确设置。
- **配置布局(D1, 不扩容)**: controller_config 保持 128B, cfgVer 仍 0x02; reserved[101](偏移26-126) 拆为 thTouchKey[32](26-57, lane 0-31) + thReleaseKey[32](58-89) + reserved[37](90-126), xorSum 仍在 127。**0 = 继承全局**, 旧配置(reserved 全 0)加载后行为与 1.5.3 逐字节等价, 零迁移。新增 static_assert(sizeof==128) 防偏移漂移。
- **sanitizeConfig**: 逐键钳位——0 保留; MPR 变体(hwVer1/2 且 cfg0.b1=0)非 0 值钳 1-63; MBR 变体钳 1-255; MBR 上 thReleaseKey 强制 0(无 per-sensor 释放寄存器)。
- **hw_devices.cpp 三消费点逐键化**(v1-MPR / v1-MBR / v2-MBR 三套独立逻辑):
  1. initMPR121 硬件层: electrodeBaseTouchTh/electrodeBaseReleaseTh 改为查 laneTable 配置(0=继承), cplay 保留 round47d M2E0=14 硬编码覆盖(仅当配置为 0 时生效, 顺序: per-key > M2E0 > global)。释放阈值逐电极写 0x42。
  2. updateTouch_v1 MPR 分支: sw_th/verify_th 从循环外单值改为 sw_th_k[3][12]/verify_th_k[3][12], 振幅分级(sw_th+6/+4/+2)相对逐键基点。
  3. v1-MBR 与 v2-MBR: 常量 80/120/200/300 改为 base_k + {0,40,120,220}, base_k 默认 80, 全 0 时与 1.5.3 精确等价; 逐键值经 laneTable 反查。
- **laneTable**: (m,e)->lane 反向表在 initHwDevices 构建(buildLaneTable), v1/v2 布局独立(镜像 touchData32 赋值), 三套消费点共用; 阈值热路径 O(1) 查表。
- **版本**: NYANITHM_FW_VERSION 1.5.3 -> 1.6.0(nyanithm_shared.h + CMakeLists pico_set_program_version 双处, 两变体); UF2 签名 NYANFW1;1.6.0;var 自动随之。API level 保持 0x10(本无新命令, round66 升 0x11)。
- **测试**: tools/test_round50_migration.py 新增场景 15-20: S15 全 0 继承等价性 golden(MPR/MBR 判定序列逐周期一致)、S16 单键抬升(其余键不变)、S17 MBR 分级等价(base 80 还原 80/120/200/300)、S18 sanitize 钳位矩阵、S19 二值语义 vs 未来压力替换、S20 cplay M2E0 软验证隔离; 模拟器 TouchPipeline 增加 per_key 参数(0=继承)。原 43 场景全绿, 共 68/68 通过。
- **验证**: 双变体构建通过——hw_v1 UF2 235008B SHA1 D1807113E53E4DBC90406CA48045A19E18918B64, cplay UF2 235520B SHA1 246DD87C84A72A0FBAB6001FFC61858AECCC2FD2; static_assert 128B 编译期通过。
- **修复后复验**: 交叉复查发现并修复 3 处——(1) cplay M2E0=14 硬编码被软验证层误用, 新增 electrodeBaseTouchThSoft 隔离(软层 per-key/global, 全 0 与 1.5.3 周期等价); (2) MBR 逐键 <80 会放宽软门, 软件层 max(80, perKey) 保证只收紧; (3) static_assert C 分支改为 struct controller_config。测试增加 S20(M2E0 软验证隔离)共 68/68。
- **同步铁律**: controller_config.cpp/nyanithm_shared.h/hw_devices.cpp/CMakeLists 四文件两变体语义一致, 行为差异仅 cplay 既有 M2E0=14 与 spatial penalty +2(预先存在, 保留)。
- 真机待办: 异常键 A/B(1.5.3 全局 vs 1.6.0 逐键抬升), 配置读改存重启回读一致(含逐键字节), dumpCfgCmdLog() 0xCE 回读, HID 游戏内手感回归 + CDC 诊断双验(§6.3)。

### 20260824 round63j: 保存并重启后自动重连 (纯面板)

- **背景**: round63i 修复后保存已走通(真机 log: CFG_SET check ok → 配置已保存 → 设备正在重启), 但重启后设备重新枚举, 面板只调 closePort() 就停在"未连接", 不会自动连回(自动重连此前只存在于刷写流程的 startReconnectWatch, 且与 flash UI 强耦合)。
- **修复**: 新增独立的 `startSaveRebootReconnect()` 计时器(15s 上限), 保存并重启后每秒轮询 `navigator.serial.getPorts()` 找 VID 0xCAFE/PID 0x4009, 找到即走普通连接路径(openPort + autoHandshake + 输入轮询), 与刷写流程的 reconnectWatch/热插拔监听互不干扰; `handleUnexpectedDisconnect` 时停止该计时器防残留。
- 验证: 面板 Mock 实测保存并重启后 1s 内自动重连(串口已打开 → 保存并重启完成,已自动重连 → 握手成功 → 设备信息), UI 回到普通模式已连接; 三处 HTML 一致 devDiag=0。
- 真机待办: 刷新面板 HTML 后保存并重启, 确认自动重连。
### 20260824 round63i: 真机 A/B 实测定根因 — CFG_SET 必须拆成两次 write (0xB7 单独, 再 128B) (纯面板)

- **真机实测(2026-08-24, cplay 固件 Built Aug 24 20:49:44 / SDK 2.2.0 / VAR cplay)**: 用 Python+pyserial 直连 COM1 复现 ConfigApp 协议, 捕获设备每字节响应:
  - A) 单次 `write(0xB7+128 共 129B)` → `read timeout at byte 65 of 128. config read aborted.`(失败, 与面板此前各版现象一致)
  - B) 两次 `write([0xB7])` + `write(cfg 128B)`(ConfigApp `serialib::writeChar(0xB7)`+`writeBytes(cfg,0x80)` 逐字节对齐) → `read 128 byte. check ok. config changed.` 且 `CFG_SAVE → saveConfig result: 0`(成功)
  - B 连测 3 次全过(currentPage 8/9/10)。A/B 同一台设备、同一固件, 差异只在主机发送方式。
- **根因**: 单次 129B write 在 Web Serial/USB 驱动层打成 `[64][64][1]` 三个包, 第二个包(byte 65 起)撞设备端单缓冲 OUT 端点未及时重武装而 NAK; 拆成两次 write 后, 固件 handleCommand 先吃掉 0xB7(单独 1B 包), setConfig() 从干净状态读 128B 载入流, 逐包重武装, 稳定成功。round63f/g/h 的"分块/单突发"方向都不对, 关键是**0xB7 必须独立成包**。
- **修复**: `doSaveConfig` 改为 `await writeBytes(new Uint8Array([CMD.CFG_SET]))` + `await writeBytes(cfg)` 两次独立 write, 无 sleep。CFG_SET 响应仍用纯 `waitForLine(3000)`(绝不用 0xBC 轻推)。
- **诊断保留**: `window.dumpCfgCmdLog()`(0xCE 命令日志回读)继续可用。
- 验证: 真机两写模式 3/3 成功; 面板 Mock 实测保存走通(CFG_SET check ok → 配置已保存 → 设备正在重启), 无 unknown command 风暴; 三处 HTML 一致 devDiag=0。
- 真机待办: 刷新面板 HTML 后保存并重启, 确认 CFG_SET 走通。
### 20260824 round63h: 对照 ConfigApp 反汇编修正保存时序 + 0xCE 命令日志诊断 (纯面板)

- **根因反转**: round63f/g 分块发送仍复现 `read timeout at byte 65`。反汇编原版 ConfigApp.exe(serialib 路径)确认它保存配置的方式是 `writeChar(0xB7)` + `writeBytes(config, 0x80)` 两次连续写、129 字节一次灌入、无分块无间隔,真机可稳定保存——而 round63g 的 64B 分块 + 60ms 间隔制造了固件 setConfig() 读取的空窗(设备端 RX ring 每读空一包需 tud_task() 重新武装 OUT,分块把载入流拆成 3 个 USB 包,中间空窗在旧固件 stdin getchar 路径上表现为 byte 65 卡死)。
- **修复**: `doSaveConfig` 改回与 ConfigApp 逐字节一致的 `await writeBytes(pkt)` 单次突发(0xB7+128 一次送出,无 sleep),CFG_SET 响应仍用纯 `waitForLine(3000)`(绝不用 0xBC 轻推,防止注入载入流)。
- **新增诊断**: 面板暴露 `window.dumpCfgCmdLog()`(控制台调用),回读固件 0xCE 命令日志(配置模式下收到的最近 255 个命令字节),判定设备实际收到多少字节——用于排查载入流截断。Mock 同步实现 0xCE 同构转储。
- 验证: 面板 Mock 实测保存走通(CFG_SET check ok → 配置已保存 → 设备正在重启), 无 unknown command 风暴; 三处 HTML 一致 devDiag=0。
- 真机待办: 仅刷新面板 HTML 后保存并重启, 确认 CFG_SET 走通; 若仍失败, 控制台调 `dumpCfgCmdLog()` 回读设备实际收到的命令字节。
### 20260824 round63g: 面板 CFG_SET 分块发送, 兼容旧固件 RX=64 (纯面板)

- **背景**: round63f 把固件 RX ring 提到 256, 但真机仍报 `read timeout at byte 65`——确认设备跑的还是旧固件(RX=64): `_cdcd_itf` 结构体 584B 只在 RX=256 时成立, 而 byte 65 卡死是 64B ring 的精确签名。
- **修复(纯面板, 不依赖固件刷新)**: `doSaveConfig` 把 CFG_SET 载入流(0xB7+128=129B)分块发送(每块 ≤64B, 块间 60ms 间隔), 让每个 USB 包独立到达, 固件 setConfig() 逐块读完 128 字节。旧固件(RX=64)与新固件(RX=256)都兼容。CFG_SET 响应仍用纯 `waitForLine(3000)`(绝不用轻推, 防止 0xBC 注入载入流)。
- **Mock 修正**: MockPort 的 onHostWrite 改为跨 write 累积 CFG_SET 载入流(真实固件 setConfig 就是一次读 128 字节, 分块对固件透明), 载入流未凑满 128 时后续 write 全部作为续段累积。
- 验证: 面板 Mock 实测保存走通(CFG_SET check ok → 配置已保存 → 设备正在重启), 无 unknown command 风暴; 三处 HTML 一致 devDiag=0。
- 真机待办: 仅刷新面板 HTML(无需重刷固件)后保存并重启, 确认 CFG_SET 走通。

### 20260824 round63f: 提升 CDC RX ring 至 256 + 面板 CFG_SET 不再轻推 (固件+面板, 双树)

- **真机复现**: 刷入 round63e 后保存, 得到 `read timeout at byte 66 of 128`。前 64 字节(第一个 USB 包)读入, 第 65+ 字节(第二个 USB 包)始终到不了设备。
- **根因(经 TinyUSB 源码分析)**: `CFG_TUD_CDC_RX_BUFSIZE=64` 时, CDC RX ring 只能装一个 64B USB 包。`_prep_out_transaction` 只在 ring 空闲 ≥64B 时才重挂 OUT 端点; ring 满后下一个 OUT 事务被 **NAK**, 直到应用 `tud_cdc_read()` 排空并重挂。129B 载入流(0xB7+128)跨两个包, 面板一次 write 与固件排空之间存在主机 NAK 重试时序竞态 → 第二个包迟迟不达。
- **另一处面板 bug(0xBC 注入)**: 面板 CFG_SET 响应用 `waitForLineNudged`, 超时后发 0xBC"轻推"。但设备此刻仍在 setConfig() 载入流读取中, 0xBC 会被**当成第 65 个配置字节注入并错位整个配置**(真机日志 read timeout at byte 65/66 正对应此)。
- **修复**:
  1. 固件: `CFG_TUD_CDC_RX_BUFSIZE` 64→**256**(双树 tusb_config.h)。整段 129B 载入流连续入 ring, TinyUSB 每包后自动重挂 OUT, 消除 NAK 竞态。
  2. 面板: `doSaveConfig` 的 CFG_SET 响应改用纯 `waitForLine(2500)`(不轻推); 载入流仍一次 `writeBytes` 发送。CFG_SAVE 阶段设备已回命令循环, 轻推保留(安全)。
- 验证: hw_v1 固件编译链接通过(SHA1 BD5EEA10…), cplay 通过(SHA1 39C65E04…); 面板 Mock 实测保存走通(CFG_SET check ok → 配置已保存 → 设备正在重启), 三处 HTML 一致。
- 真机待办: 重刷固件(RX 256)+ 更新面板后保存并重启, 确认 CFG_SET 走通且无 unknown command 风暴。

### 20260824 round63e: CFG_SET 改用 tud_cdc_read 直接读载入流 (固件, 双树)

- **真机复现**: 刷入 round63d 后保存, 得到 `read timeout at byte 65 of 128. config read aborted.`。前 64 字节(第一个 USB 包)读入成功, 第 65 字节起(需要拉取第二个 USB 包)卡死。根因: getchar_timeout_us() 内部 busy-wait 不泵 tud_task(), 且 stdio 的 in_chars 共用 stdio_usb_mutex(与输出竞争)。CFG_TUD_CDC_RX_BUFSIZE=64, 129 字节载入流跨越两个 USB 包: 读空 64B FIFO 后必须靠 tud_task() 拉取第二个包才能续流, stdio 路径在此卡死。
- **修复**: setConfig() 改为直接 `tud_cdc_available()/tud_cdc_read()` 读载入流 + 每次循环显式 `tud_task()` 泵 USB + `watchdog_update()` 喂狗 + 1s 总宽限(读到 128 字节为止)。与 normal-mode cdc_respond 同机制(已被真机验证), 完全绕过 stdio 互斥与 busy-wait。
- 验证: hw_v1 固件编译链接通过(SHA1 3459D1F1…), cplay 通过(SHA1 06037040…); 双树 `controller_config.cpp` 一致。
- 真机待办: 重刷后保存并重启, 确认 CFG_SET 走通(check ok)且无 unknown command 风暴。

### 20260824 round63d: 修复 CFG_SET 首字节超时 + "unknown command" 风暴 (固件, 双树)

- **真机复现**: 刷入 round63c 后保存, 得到 `read timeout at byte 0 of 128. config read aborted.` + 约 137 行 `unknown command...`。根因: round63c 用 `getchar_timeout_us(10000)` = **10ms**, 参数单位是微秒。真机上首个配置字节到达可能超过 10ms(USB 传输 + 前序 getStatus 文本输出与 stdio_usb 互斥竞争), 导致 byte 0 就超时中止; 随后 128 个配置字节被 handleCommand 逐个当作独立命令解码 → "unknown command" 风暴。
- **修复**: `setConfig()` 读取循环改为每字节 500ms 总宽限 + `getchar_timeout_us(50000)`(50ms 内无数据再轮) + `PICO_ERROR_NO_DATA`(瞬时空)继续重试, 仅持续超时才中止; 每字节仍显式 `tud_task()` 续流 + `watchdog_update()` 防看门狗复位。正常 USB 续流下 128 字节在 ~50ms 内读完, 500ms 只是安全上限。
- 验证: hw_v1 固件编译链接通过(SHA1 719E76B1…), cplay 通过(SHA1 019A3FEE…); 双树 `controller_config.cpp` 一致。
- 真机待办: 重刷后保存并重启, 确认 CFG_SET 走通(check ok)且无 unknown command 风暴。

### 20260824 round63c: 修复 CFG_SET 128 字节载入流看门狗复位 (固件根因, 双树)

- **真正的保存失败根因(固件侧)**: 面板保存时发 CFG_SET(0xB7 + 128 字节 = 129 字节), 但 TinyUSB `CFG_TUD_CDC_RX_BUFSIZE = 64`。旧 `setConfig()` 用 `getchar()` 循环读 128 字节——`getchar()` 底层 `stdio_get_until` 只 busy-wait 轮询 `in_chars`, **不泵 `tud_task()`**。前 64 字节从 FIFO 读出后, 剩余 65 字节卡在主机 USB 栈, 设备侧因不泵 USB 而永远收不到 → `getchar()` 永久阻塞 → `updateInputState()` 不再运行 → **2s 硬件看门狗直接复位设备**(表现为面板"等待行响应超时" + "device has been lost" + 设备重启)。与心跳、0xB8 探测无关(本次真机复现进入配置模式后仅 9s 即保存, 心跳尚未触发, 探测已通过)。
- **修复**: `setConfig()` 循环内每字节 `tud_task()`(让 TinyUSB 从主机续拉剩余字节) + `watchdog_update()`(防复位) + `getchar_timeout_us(10000)`(读超时打印 "read timeout at byte N" 并中止, 不再无限阻塞)。包含 `hardware/watchdog.h`。
- 验证: hw_v1 固件编译链接通过(SHA1 6E248790…), cplay 通过(SHA1 9307C65C…); 双树 `controller_config.cpp` 一致。
- **必须重刷固件**: 面板无法绕过旧固件的 64B FIFO + 不泵 USB 限制, 旧固件上 CFG_SET 必然卡死看门狗复位。请刷入新版 UF2 后重试保存并重启。

### 20260824 round63b: 存活性探测改 0xB8 + 0xBC 探测帧固件显式刷出 (面板+固件)

- **0xBC 作为存活性判别不可靠(真机复现)**: 面板 round63 用 0xBC 探测设备是否仍在配置模式, 但真机在配置模式下(进入仅 8s, 未到 60s 自动退出)误报"设备已离开配置模式"。根因: 0xBC 返回的是 6 字节纯二进制帧、无换行结尾, 单独发送时真机 stdio 通道不可靠(仅当其后的 printf 文本带出时才有效; enterConfigMode 的 [0xBC, CFG_READ] 因后续 read...\n 而成功)。
- **面板判别命令改用 GET_STATUS(0xB8)**: 配置模式返回换行结尾的 "get status...\n"(与面板状态查询同一机制, 真机已验证可靠), 普通模式 cdc_respond 无 0xB8 分支(静默丢弃)。probeConfigAlive() 读 "get status" 行后按时间窗(~250ms)排空状态转储, 防止 ToF 数值行污染随后 CFG_SET 的响应读取。
- **固件 0xBC 探测帧显式刷出**: app_link.cpp CMD_DETECT 分支末尾加 stdio_flush(), 使单发 0xBC 也能立即收到完整 6 字节帧(面板现在不依赖它, 但任何独立发送 0xBC 的宿主都受益)。
- 验证: 面板 Mock 实测 保存并重启 正常(0xB8 探测通过 → CFG_SET check ok → 配置已保存 → 设备正在重启 → 断开), 无"已离开配置模式"误拦, 无 CFG_SET 响应污染; 双树固件 app_link.cpp 编译链接通过(hw_v1 SHA1 490EF555…, cplay SHA1 8FCF23E6…)。
- 真机待办: 真机再次执行 保存并重启, 确认不再误报"已离开配置模式"且正常重启。

### 20260824 round63: 修复"保存并重启"失败 + 二级确认文案简化 (纯面板)

- **保存并重启失败根因(并发竞态)**: 配置模式心跳 CMD_CFG_KEEPALIVE(0xC3, 每 30s)此前直接 `writeBytes` 发送, 不经过串口事务锁 withSerialLock。若恰在 doSaveConfig 发送 CFG_SET 的 128 字节载入流的窗口内命中, 0xC3 会在导线层被固件 setConfig() 读进配置块, 使整块错位; 残留的一个配置字节随后被当作下一条命令解码, 可能触发 0xB9(重启)/0xBB(刷写) → 表现为"等待行响应超时" + "device has been lost"。修复: 心跳改为走 `withSerialLock`, 排到当前事务之后, 绝不穿插进载入流。
- **保存前设备存活性探测**: 新增 `probeConfigAlive()`(后经 round63b 改为 0xB8, 见下)。保存 CFG_SET 前先确认设备仍处于配置模式, 否则提示"设备已离开配置模式(可能超过 60s 自动退出或已重启), 未写入", 阻止把 128 字节写到已退出的设备(避免误触 0xBB/0xB9)。
- **二级确认文案简化**: 保存并重启 / 恢复默认 的二次确认按钮文字由"确认保存并重启? / 确认恢复?"统一改为"继续？"。
- **灯光写串行化**: sendLedColors(0xB2 单向写)也纳入 withSerialLock, 避免与输入轮询的 GET_INPUT 请求在导线层交错, 保证字节读取器能辨识 33 字节输入帧边界。
- 验证: 三处 HTML 一致 devDiag=0; 构建通过; Mock 实测 保存并重启 正常(CFG_SET check ok → 配置已保存 → 设备正在重启 → 断开), 二次确认显示"继续？", LED 写无错误, 无 JS 控制台错误。
- 真机待办: 真机长时间(>60s)配置会话下保存一次, 确认不再出现"设备已离开配置模式"误拦与"等待行响应超时"; 观察 keepalive 串行化后 30s 心跳不再打断命令事务。

### 20260824 round62: Log 固定置于左侧栏、设备状态下方 (纯面板)

- Log 不再使用网格 dock/自适应: 从 .app-main 网格拆出, 直接放入 .test-column(flex column)内、设备状态(panel-tof)之后, 作为左栏子项; 宽度 100% 撑满左栏, 高度紧随内容
- JS: initLogAutoDock 仅清除历史 dock 类/内联 gridColumn(移除把 Log 移入 .app-main 的 DOM 操作); CSS: .panel-log 由 grid-column 改为 width:100%
- 验证: 三处 HTML 一致 devDiag=0; 测试 43/43 + 25/25 + 5/5; DOM 无重复 id(mainLog/btnClearLog 各1); Mock 实测 Log parent=test-column, prev=panel-tof(设备状态), Log 宽 929=左栏宽 929, left=22=左栏 left=22, 位于设备状态下方
- 真机待办: 真机布局下 Log 左栏位置与设备状态间距复核

### 20260824 round61: 移除设备信息窗口 + 固件版本点击弹详情 + 信息格自适应 (纯面板)

- 移除下方"设备信息(固件/编译)"宽窗口(metric-cell wide + devInfoVal), 功能转为: 点击"固件版本"卡弹出完整设备信息弹窗(infoDetailModal, 显示 0xBD 原始 info 串); 指针悬停高亮(cursor pointer + hover 变 accent 色)
- 信息四格(固件版本/API硬件/构建变体/硬件探测): 允许自动换行/自动增高(white-space normal + word-break), grid-auto-rows 1fr 保证四格高低对齐; 修复"硬件探测"长文本显示不全的问题
- 移除 devInfoVal 元素与 JS 引用, 完整 info 由 devInfo.raw 承载供弹窗展示
- 验证: 三处 HTML 一致 devDiag=0; 测试 43/43 + 25/25 + 5/5; DOM id 无缺失; Mock 实测点击固件版本弹详情(FW/API/Built/SDK), 四格等高 57px, devInfoVal 已移除, 状态提示与按钮正常
- 真机待办: 固件详情弹窗在真机 0xBD 返回串下的展示; 四格换行观感复核

### 20260824 round60: Log 固定左栏 + 开屏/页头文字调整 (纯面板)

- Log 窗口: 弃用高度差 dock 迁移(受 grid stretch 拉平/阈值影响效果不稳定), 简化为固定位于左栏下方, 宽度与左栏一致。CSS .panel-log grid-column 1/2; JS 仅保留把 Log 移入 .app-main 网格(确保 grid-column 生效) + 清除旧 dock 类, 不再做任何实时高度差判断
- 开屏打字机: 副标题 "For Gen 1" 扩写为 "CONTROL PANEL FOR GEN 1" (INTRO_SUBTITLE 常量 + base 硬编码文本)
- 页头品牌: "CONTROLLER GEN 1" 字体增大 font-size 14px→24px, 渲染高度 26px 与左侧矢量 logo(brand-logo) 高度一致
- 验证: 三处 HTML 一致 devDiag=0; 测试 43/43 + 25/25 + 5/5; Mock 截图确认 Log 固定左栏(宽929=左栏宽929, x=22=左栏x=22), master-text 高26=logo高26
- 真机待办: 真机布局下 Log 左栏位置与页头字体观感复核

### 20260824 round59: 品牌文字改为 Gen 1 + Log dock 规则重新设计 (纯面板)

- 开屏动画副标题 "CONTROL PANEL FOR MASTER ver. / HW_V1" 改为 "For Gen 1" (INTRO_SUBTITLE 常量 + base 硬编码文本; 打字机效果)
- 顶部品牌图标+文字 MASTER 图片替换为文本 "CONTROLLER GEN 1" (.header-master .master-text, 保留 logo-filter 风格)
- Log dock 规则重新设计(用户确认: 普通模式全宽, 差异明显才收窄): 判定改用两栏内容自然高度(scrollHeight, 不含 grid stretch), 阈值放宽到 90px。普通模式左栏(INPUT TEST)比右栏(配置)高约 60-76px < 90 → Log 保持全宽; 仅当一栏内容明显高于另一栏(≥90px, 如右栏配置区真正暴涨)时才 dock 到较矮栏下方(左高→右栏, 右高→左栏 dock-left)
- 拆解过程: 此前 15px 阈值 + grid stretch 拉平两栏容器导致 Log 恒贴右栏/判定失效; 重新实测几何后定此为更稳健规则
- 验证: 迁移 43/43 + 逻辑 25/25 + UF2 5/5; 三处 HTML 一致 devDiag=0; Mock 实测普通模式 Log 全宽(1357px), 顶部品牌 CONTROLLER GEN 1, 开屏 For Gen 1
- 真机待办: 开屏动画副标题与顶部品牌观感复核; 真机右栏配置暴涨时 Log dock 到左栏的验证

### 20260824 round58d: Log 初始全宽 2s 后实判 + 右栏展开增高判定 (纯面板)

- 初始等待 2s: Log 默认全宽(规则①), 2s 内用 dockingEnabled 门控抑制 resize/MutationObserver 触发的重测(避免初始化补布局过早误 dock), 2s 后放开并做首次实时高度差判断
- 右栏展开增高判定: 判定沿用"两栏内容自然高"比较(dockLeft = 左栏scrollHeight > 右栏panel-body scrollHeight)。右栏展开折叠区后 scrollHeight 增大, 一旦超过左栏即 dock 到左栏(dock-left)。实测 mock 下右栏全展 1556 < 左栏 1615(右栏仍矮), 故 Log 停在右栏符合"占较矮一栏"规则; 真机右栏内容更多时会正确移左栏
- 验证: 三处 HTML 一致, devDiag=0; 迁移 43/43 + 逻辑 25/25 + UF2 5/5; Mock 实测 2s 内全宽(1357px) → 2s 后 dock 右栏(444px)

### 20260824 round58c: Log 初始全宽 + 页面就绪后实时判高度差 (纯面板)

- Log 初始保持全宽(规则①默认态), 不再在脚本启动时立即测量; 改为统一等 window.load(页面资源/字体/图片/折叠布局真正就绪)后再做首次实时高度差判断(resize / MutationObserver 继续实时更新), 避免布局未稳过早误 dock
- 规则确认: ①两栏差≤15px → Log 全宽放最下; ②两栏差>15px → Log 占较矮栏宽放该栏下方(实测右栏矮 59px, Log dock 右栏下, 宽对齐右栏)
- 验证: 三处 HTML 一致, devDiag=0; 测试 43/43 + 25/25 + 5/5

### 20260824 round58b: Log 窗口 dock 修复——移入 app-main 网格 + 内容自然高度判定 (纯面板)

- 根因一(布局): build.py 合并后 .panel-log 实际落在 <body> 下而非 .app-main 网格内, grid-column 定位失效, Log 恒占整行 block。修复: initLogAutoDock 启动时把 .panel-log appendChild 到 .app-main 末尾, 使其成为真正的 grid 子项
- 根因二(判定): 原用 getBoundingClientRect().height 比较左右列, 但两列容器被 grid stretch(isotensive)拉至同高, 掩盖真实可视高度差。改按"内容自然高度"判定: 左列用 .test-column.scrollHeight, 右列用 .panel-config .panel-body.scrollHeight(排除 header/padding 与 stretch 干扰)
- 效果: 右列 panel-body 内容 1556 < 左列 1615(差 59px > 15), 触发 dock 到右列下方, Log 宽度收窄至与右列一致(实测 462px, 对齐右列起点 x=983), 不再是全宽横贯底部
- 保持既有约束: 仅 2 列布局且差值 >15px 才 dock; 单列/窄屏保持全宽
- 验证: 迁移 43/43 + refit 逻辑 25/25 + UF2 5/5; Mock 实测(dock 后宽度对齐右列、resize 稳定、无 JS 错误); 截图确认 Log 位于右列下方
- 真机待办: 真机布局下 dock 观感复核

### 20260824 round58: 禁用控件灰度 + 导入/导出修复 + 刷写自动进配置 + 文案 (纯面板)

- 全局禁用灰度: 任意 disabled 交互控件(按钮/滑块/复选框/数字输入/下拉)文字变灰 + opacity 0.7 + not-allowed 光标; 未连接设备时全部交互控件禁用(仅"连接串口"保留可用作为连接入口, "更新固件"未连接时禁用)
- 文案: "配置读写需先进入配置模式, 保存后重新插拔生效" 中 "重新插拔" 改为 "重启"(文字属性不改)
- 导入/导出修复: 此前被限制在配置模式内导致普通模式"失效"; 现放宽为已连接即可用。导入只填表单(不写设备); 导出未进配置时导出当前表单构建值(含 cfg2/cfg3), 配置模式下导出设备读取值
- 刷写自动进配置: 点击"开始刷写"时若未进配置模式, 自动先调用 enterConfigMode()(含门禁/读取/心跳)进入再续跑刷写流程; 抽出 enterConfigMode() 供按钮与刷写复用
- 按钮结构: 抽 enterConfigMode() 辅助函数, "进入配置模式"按钮重构为委托调用
- 验证: 迁移 43/43 + refit 逻辑 25/25 + UF2 5/5; Mock 实测(禁用灰度 opacity 0.7/灰色/not-allowed, 未连接全禁用, 连接后导入导出可用, 保存并重启普通模式禁用, 文字重启生效)
- 真机待办: 刷写"未进配置自动进入"真机全流程; 导入导出在真机普通模式的表单回填

### 20260824 round57: Log 摆放修正 + 工具栏精简 + 配置模式心跳 (面板 + 固件)

- Log 摆放修正: 仅 2 列布局(app-main 双列)且左右高度差 >15px 时才 dock 到较矮列; 单列(窄屏)不再解析 grid-column:2/3(此前落在不存在的列上导致恒占全宽), 保持全宽。并强化 dock 时宽度收窄到目标列
- 工具栏精简: 移除最上方"保存设置"按钮; "连接串口/更新固件"两按钮均分拉伸填满一行
- 按钮禁用: 进入配置模式前"保存并重启/恢复默认"置灰不可点(进入后可用); "保存设置"并入"保存并重启"(二级确认)
- 导入/导出配置按钮移到"进入配置模式"按钮区下方(独立 cols-2 一行), 高级参数区仅保留 cfgVer/xorSum 展示
- 配置模式心跳(新功能, 固件+面板): 固件新增 CMD_CFG_KEEPALIVE=0xC3, handleCommand 内静默分支(仅重置 60s 自动退出计时器 lastCmdMs, 不回显避免污染流); 旧固件收到会回 "unknown command"(面板容忍)。面板进入配置模式后每 30s 发一次 0xC3, 退出/断开时停止
- Mock 适配: mock 串口对 0xC3 静默无响应
- 验证: 迁移 43/43 + refit 逻辑 25/25 + UF2 5/5; 双树固件编译含 keepalive(hw_v1 SHA1 3CBE96AC..., cplay SHA1 2B9B52D4...); Mock 全流程(按钮禁用/切换/心跳 32s 无污染且配置模式保持/Log 宽窄屏)
- 真机待办: keepalive 在真机配置模式下验证 60s 自动退出被抑制; Log dock 真机观感

### 20260824 round56: 一代面板交互重构 - 按钮合并 / 固件弹窗 / 文字与 UI 调整 (纯面板)

- 按钮合并: 工具栏"读取设置"并入"进入配置模式"(进入时即读配置); "保存设置"保留在工具栏(读取/保存/恢复三合一整理); "退出并重启"并入"进入配置模式"按钮--普通模式显示读取图标+进入配置模式, 成功进入后切换为重启矢量图标+退出并重启; "恢复默认"移到原按钮位
- 固件刷写: 取消内嵌折叠功能区, 改为独立弹窗 UI(更新固件按钮直接打开): 文件选择/解析信息/警告勾选/手动盘符写入/自动刷写/状态行全部在弹窗内; 点击"开始刷写"即最终确认(不再二次弹窗)
- 普通模式直读: 连接握手后从 0xBD 版本串直读 hwVer 并同步到硬件版本选择器(仅未进入配置模式时, 避免覆盖表单)
- 文字修改: hwVer 按钮 "v1/v2" 改为 "三主控/双主控"(如 27寸 三主控); 备注补充 v1 支持 MPR121 与 MBR3116; "导出/导入 config.bin" 改为 "导出配置/导入配置" 并加矢量图标
- 文字 UI: 所有备注/说明/副标题/小字增大字号(至 fs-sm/fs-md) 并统一透明度 40%; 警告及以上文字变红; 触摸/释放阈值与消抖滑条在达到警告线 125% 时滑条与数显平滑过渡为橙红色
- Log 自适应: Log 纳入 app-main 网格; 左右列高度差 >15px 时平滑移动到较矮一侧下方, 反之回全宽(resize 与折叠变更触发)
- 验证: Mock 全流程(按钮切换/保存往返/退出重启/固件弹窗/警告清除/Log dock 窄屏 2/3 恢复 1/-1) + 无 JS 错误; test_round50_migration 43/43 + refit 逻辑 25/25 + UF2 5/5; 双树字节一致
- 真机待办: 固件弹窗全自动刷写实机走查; Log 自适应在真机布局下的观感

### 20260824 round55: 一代面板翻新就位 + cfg2/cfg3 新功能--Windows 动态照明开关与附加输入延迟 (固件+面板)

- 定性: 本次合并为一代控制面板翻新 (仍为 v1, 非二代); ControlPanel_v1.html 由 v3 基座 + 一代协议引擎合并重制, 协议字节级审计通过 (命令/偏移/响应串/SET_LED/时序/门禁/UF2 流程)
- 固件 nyanithm_shared.h: cfg2/cfg3 由保留位转正。cfg2 bit0 = 关闭 LampArray / Windows 动态照明 (CFG2_BIT_DISABLE_LAMP_ARRAY, disable 语义: 置 1 = 禁用; 旧配置 cfg2==0 与出厂默认保持动照明开启, 行为不变); cfg3 = 附加输入延迟 0-15ms (INPUT_LATENCY_MAX_MS=15, MBR3116 式调校旋钮, 0=关闭)
- 固件 lamp_array.cpp: lamp_array_apply 头部加 cfg2 bit0 门禁, 禁用时一次性清灯 (clearedOnDisable 锁存, 重新启用自动复位), 防最后一帧 Windows 灯色常亮
- 固件 chuni_io.cpp hid_task_chuni_input: seqlock 成功分支内加帧龄门禁--输入变化帧需距上一服务帧 >= cfg3 ms 才提交 inputState, 期间继续服务旧帧; 0ms 路径与 round47b 行为逐字节一致 (memcmp 变更检测 + latencyFrameMs 时间戳)
- 固件 controller_config.cpp: sanitizeConfig 增加 cfg2 掩码 (仅 bit0) 与 cfg3 钳位 (<=15); checkConfigBuf 不变 (magic/cfgVer/xorSum), 旧面板/旧配置读写完全兼容
- 面板: 通用设置新增 "Windows 动态照明" 复选框 (勾选=开启), 高级参数新增 "附加延迟" 滑条 0-15ms (提示需固件 round55+, 旧固件忽略该字段); pack/unpack/禁用态全部接线
- 旧固件注意: 1.5.3 及之前版本收到非零 cfg2/cfg3 会原样存入 flash (旧固件不解释), 升回 round55 固件后字段即生效; 若需回退行为, 面板中取消勾选/归零即可
- 验证: Mock 往返 (关灯+7ms 回读一致 / 复位归零 / lightLimit 字节保真) + gen2 门禁拦截 (3×ToF 拒绝) + 无 JS 错误; test_round50_migration 43/43; hw_v1/cplay 双变体编译通过 (UF2 228352B, hw_v1 SHA1 668D81EA..., cplay SHA1 B572DACE...)
- 硬件验证待办: cfg3 延迟的手感调校与动态照明开关的实际 HID LampArray 效果需真机确认 (CDC 诊断不足以替代)

### 20260824 round54e: 配置会话流式化——探测与读配置合并, 修复 Magic 不匹配根因 (面板)

- 第四份真机日志 (02:55) 判定 round54d 方向错误: 重发轻推并未放出滞留帧 (3×1.2s 全程无响应), 但紧随其后的 0xB4 写入触发一次性涌出 [0xBC 探测帧(×N)][128B 配置][read.../using 行]——探测帧粘在配置字节区开头, 即历次 "Magic 不匹配: 0xbc" 的直接来源; round54d 的剥离循环只在执行瞬间对已到字节生效, 而 0xB4 写入后缓冲区尚空, 循环立即退出, 涌出帧被 waitForBytes(128) 整体吞下
- 架构改为流式会话 readConfigStream: 0xB3 后连发 [0xBC, 0xB4], 把设备输出当字节流解析——前导 6 字节探测帧逐帧捕获为硬件探测结果 (帧数不定: 滞留+轻推各产生一帧), 遇 0x88 魔数起读 128 字节配置, 随后有界 (1.2s) 收两行展示文本 (行缺失不判失败)。等待期间每 500ms 重发 0xBC 轻推; 固件按命令顺序应答, 轻推帧只追加在流尾, 不会混入配置字节区 (离线回放测试验证: 滞留涌出/旧固件文本两场景均无歧义切分)
- 超时报错携带诊断信息 (收包字节数/探测帧数/首字节十六进制预览), 下轮若仍失败可直接从日志判读设备到底发了什么
- waitForLineNudged 修复实 bug: 旧版 0xB0 回显轻推的 waitForBytes(1) 在滞留场景会吞掉行首字符 ("check ok"→"heck ok") 造成误判; 轻推改为自识别的 0xBC 帧 + stripProbeFramesFromHead 剥离 (含遗留 0xB0 回显字节清理)
- btnEnterConfig / btnReadCfg / btnExportCfg 统一走 readConfigStream; 硬件门禁 (tofCount 4/5 + 触摸主控在位) 改用会话捕获的探测帧在读后执行, 旧固件白名单回退保留; 手动"读取配置"顺带刷新探测结果
- 设备侧机制仍未定论 (本轮面板修复对任意到达顺序/拆包均安全, 不依赖具体机制); 若流式会话仍超时, 下一步为固件侧 0xBC 路径时间戳插桩
- 离线验证: node --check 通过; check_dom_ids 110 ids 无重复无缺失; test_round54_panel_logic 28/28 (新增流解析 15 项); test_uf2_parser 5/5; test_round50_migration 43/43 (固件未改)

### 20260824 round54d: 配置模式响应滞留——重发轻推 + 滞留帧剥离 (面板)

- 真机三份日志证实配置模式下的输出滞留现象: 首条事务 (0xBC 探测) 的响应不送达主机, 直到主机下一次写入才被放出——响应延迟始终紧贴面板超时值 (2s 超时→2.x 秒到, 6s 超时→6.x 秒到), 单纯加长超时无效。设备侧静态排查无阻塞点 (handleCommand/探针/updateInputState 均为毫秒级), 机制待真机插桩定位
- probeDetect 改重发轻推: 3 次尝试各等 1.2s, 第 2 次重发 0xBC 本身即触发放出第 1 次的滞留帧; 读帧后排空冗余重复帧。预期 ~1.3s 内探测成功, 门禁回归硬件真值
- 配置读取 (0xB4) 双保险: 读取前剥离前导 0xBC 滞留帧 (magic 0x88 与 0xBC 无歧义区分); waitForBytes 超时后发 0xB0 回显轻推 (显式消费回显字节) 重试一次——修复首次自动读配置 Magic 不匹配/超时
- 保存流程 (0xB7/0xB5) 的两个行等待加 waitForLineNudged: 超时后 0xB0 轻推再等一次——修复"保存并重启失败: 等待行响应超时"误报 (保存实际未失败)
- 配置保留说明: UF2 镜像范围 0x10000000-0x1001BD00 与配置扇区 (flash 末 4KB, 0x101FF000) 无重叠, boot_flashing 只擦首 4KB——刷写不丢配置; 此前"配置丢失"观感实为滞留帧导致配置读取失败、无法查看所致

### 20260824 round54c: 真机日志二轮修正——盘符识别按真名 + 0xBC 探测超时加长 (面板)

- 真机日志证实刷写闭环已通 (写入→2s 自动重连→✅ 版本一致), 剩余两处: (1) bootrom 根目录实际文件为 INFO_UF2.TXT 与 INDEX.HTM (非 INFO.TXT/UF2INFO.TXT), round54b 的四变体盲读全部 NotFoundError, 只靠强制确认兜底通过; (2) 0xBC 探测超时后回退白名单, 迟到的探测帧 (首字节 0xbc) 污染首次配置读取 (Magic 不匹配)
- 盘符识别: 枚举目录优先, INFO_UF2.TXT (兼容 info.txt/uf2info.txt 变体) 与 INDEX.HTM (兼容 index.html) 成对在位即通过; 仅见其一读内容做二次确认 (/RP2040|RPI-RP2|UF2 Bootloader|Raspberry Pi RP2/i); System Volume Information 为 Windows 生成不计入证据; 皆无时保留 confirm 强制写入兜底; 不再盲读文件名变体 (消除 NotFoundError 日志噪音)
- 0xBC 探测超时 2s→6s: 真机实测设备侧延迟 2-2.8s——Core1 收到 0xB3 仅置 pending_config_mode 标志, Core0 需跑完当前 updateInputState() 整轮 (触摸+ToF 测距) 才进 handleCommand 处理 0xBC; 旧固件回 "unknown command" 文本不受影响, 只有完全无响应设备等满 6s; 超时后追加 600ms 等待+排空, 丢弃迟到帧
- 遗留 (未来固件轮次): 0xB3→handleCommand 的模式切换交接可提前到更新循环间隙检测, 消除 ~2s 门禁延迟

### 20260824 round54b: 刷写盘符校验再修正——名称通道废除, 标识文件双通道 (面板)

- 真机日志证实 Chrome 对 Windows 盘符根目录返回的 dir.name 为 "\" (非 "RPI-RP2"), 且 INFO.TXT 直接读取亦未命中——round54a 的名称宽松匹配与文件读取双双失效, 正确的 RPI-RP2 盘被硬拦截
- 校验改为只认盘内标识文件 (用户建议): 先试内容标识 (INFO.TXT/UF2INFO.TXT 各含大小写变体, 内容含 RP2040/UF2 Bootloader 即放行), 再试目录枚举 (dir.keys() 列出 INFO.TXT+UF2INFO.TXT 双文件在位即放行); 每次读取失败的错误名与目录内容均落日志供诊断
- 双通道皆失败时不再硬拦截, 改为 confirm() 由用户确认"确实是 RPI-RP2"后强制写入 (防误写兜底保留); 自动选择器取消文案补充"盘符可能尚未挂载完成"提示

### 20260824 round54a: 真机反馈修复——ToF 停止积压 + 刷写盘符校验误拒 (面板)

- 连续 ToF 停止不立即生效: 根因为每轮询任务固定耗时 ≥530ms (readAllText 无条件 500ms 收尾 + 事务锁 30ms) > 250ms 间隔, serialQueue 持续积压, 清 interval 后排队任务继续刷数秒。修复: 轮询代数计数器 tofPollGen (停止时 +1, 队列中旧代任务执行时直接作废); 状态逻辑提取为 doGetStatus(tailMs, quiet) 供手动/轮询共用, 轮询收尾等待降为 120ms 且不逐次写日志
- 刷写选盘符后失败: round54 新增的严格盘符名校验 (dir.name 必须恰为 "RPI-RP2") 为首要嫌疑——Chrome 对 Windows 盘符根目录返回的名称形态不可靠。改为证据优先校验: INFO.TXT/UF2INFO.TXT 内容含 RP2040/UF2 Bootloader 标识即放行, 文件缺失时退回 /RPI[-_ ]?RP2/i 宽松匹配, 两者皆无才中止; 每步 (选择目录名/识别结果/写入文件名字节数/失败错误名) 均落日志
- 刷写时序: 关串口前等待 1500→600ms (设备收到 0xBB 立即重启), 开盘符选择器前等待 800→2600ms (Windows 挂载 RPI-RP2 需 2-4s; 确认点击起约 3.2s 仍在 5s 激活窗口内)
- 自动重连轮询逐个尝试所有匹配 VID/PID 的已授权端口 (重枚举后 getPorts 可能同时含旧打不开的与新端口, 只试第一个会永远失败); 超时文案注明"固件可能已刷写成功"

### 20260824 round54: 刷写全自动闭环 + 阈值区间标定 + 灯光亮度正名 (面板, 固件不变)

- 刷写全自动闭环 (面板): 确认弹窗 (手写遮罩替代 confirm(), 其「确认刷写」点击为新鲜 user activation, 使随后的 showDirectoryPicker 落在 Chrome 5s 激活窗口内) → 0xBB+0xA5 进 BOOTSEL → 自动弹盘符选择器 → 盘符校验 (目录名必须 RPI-RP2, 并读 INFO.TXT 确认 RP2040 Bootloader, 选错盘立即中止防误写) → 写入 → 自动重连 (navigator.serial connect 事件 + getPorts 每秒轮询双通道, 20s 超时回退手动按钮) → 0xBD 握手比对固件版本/变体与 UF2 文件签名, 结果显示在新增「刷写状态」行
- 刷写健壮性: 意外拔线/设备重启时 readLoop 静默退出不再残留 connected 状态 (新增 handleUnexpectedDisconnect 幂等清理, 主动 closePort 以 readLoopRunning 区分); 手动备用按钮与自动路径共用盘符校验; 写入文件名做 FAT 非法字符净化
- 阈值区间标定 (面板): MPR121 触摸阈值提示「推荐 4–12, 实测空闲噪声底 ≤13, 真实触摸差值 20–50, >20 轻触易漏触」并超界红字警告 (依据 controller_config 默认 6/4 与 hw_devices round45b 实测注释); 释放阈值提示「推荐 3–8, 应低于触摸阈值 2 以上」并在 ≥ 触摸阈值时警告; dt/dr 提示追加「推荐 1–3 挡」; MBR3116 阈值提示「默认 128 (0x80 内置表基准), 建议 96–160, 一般 ≤200」
- MBR3116 0x1C 消抖语义修正 (面板): 依据 CY8CMBR3xxx Register TRM 001-91082 §1.5.25, SENSOR_DEBOUNCE 仅低 4 位 GLOBAL_DEBOUNCE 有效 (高 4 位保留), 字段限 0–15、显示掩码值、写回保留高 4 位原值; 内置 v1 表 0x01 (有效 1)、v2 表 0x80 (有效 0) 的差异如实在提示中说明
- 灯光上限 → 灯光亮度 (面板): 澄清 lightLimit 固件实现本就是等比亮度缩放 ((channel*lightLimit)/255, lamp_array.cpp/chuni_io.cpp/rainbow.cpp 三处一致, 255=100% 不变色), 配置字段与实时测试滑杆统一改名「灯光亮度」并附百分比回显

### 20260824 round53: 固件签名 + 控制面板刷写功能 (v1.5.3)

- 0xBD GET_VERSION 响应追加 `VAR:<变体>` 与 `NYANFW1;<版本>;<变体>` 签名 (info 缓冲 96->160)。变体经 CMake 注入 (NYANITHM_VARIANT="hw_v1"/"cplay"), 签名同时作为字符串字面量嵌入 UF2 镜像, 供控制面板在刷写前解析校验固件文件 (版本/变体/设备匹配)
- CMakeLists: pico_set_program_version 1.5.1->1.5.3 与宏版本同步 (此前两处版本定义不一致, 镜像内同时存在 1.5.1/1.5.2 两个版本串易造成误判)
- 控制面板新增固件刷写功能: 选择 UF2 文件后本地解析 (块结构/RP2040 family/地址范围/NYANFW1 签名/版本启发式), 弹窗展示固件信息与匹配性检查结果 (变体不匹配强警告、版本降级提醒、非 Nyanithm 固件拦截), 确认后经 0xBB+0xA5 进入 BOOTSEL, 优先用 File System Access API 直接写入 RPI-RP2 盘符, 不支持时回退手动复制指引; 旧固件 (无签名) 仅能用覆盖确认刷写

### 20260823 round52: 硬件身份探测命令 0xBC (v1.5.2)

- 新增 CMD_DETECT (0xBC, 配置模式): 只读 I2C 探测, 返回 6 字节二进制帧 [0xBC][mprMask][mbrMask][tofCount][flags][configHwVer]。mprMask=MPR121@0x5A-0x5C 在位掩码, mbrMask=CY8CMBR3116@0x37/0x40-0x44 在位掩码, tofCount=实际应答 ToF 数(0-5, 独立地址模式临时全通道 0x1F 使能后探测 0x30-0x34 再恢复原掩码, mux 扫描模式逐通道探 0x29), flags 含 mux 在位与 useMuxScan
- 动机: hwVer/CFG0_BIT_MBR3116 均为配置文件值 (可被写错), 控制面板需要硬件真值做门禁 (一代 27/32 寸) 与 MBR3116 芯片地址自动选择; 探测复用 hw_check.cpp 的 findI2CDevice 原语, 探测后恢复 mux 状态, 不改变任何运行行为
- 并发安全: 探测在 app_link handleCommand (Core0) 内执行, 与 updateInputState 同核顺序执行, 配置模式 Core1 仅跑 tud_task, 无跨核 I2C 竞争
- 0xBC 为预留枚举 (此前定义但无处理分支), 不占用新命令号; 缺地址探测各耗时 10ms 超时, 全量探测最坏约 150ms, 一次性命令可接受
- 控制面板: hwVer 选项标签与触摸主控解耦 (改为 尺寸/布局 描述), 并在 hwVer 3/4 (v2 布局) 时联动勾选「使用 3116」—— 固件 v2 管线只读 3116 (hw_devices.cpp:352-356) 且不检查 cfg0, 未勾选属退化组合 (阈值/消抖写入不存在的 MPR121、启动多耗 ~300ms、开机自检误报), 但触摸读取不受影响

### 20260817 round51: 控制面板协议修复 + USB 单驱动所有权 (v1.5.1)

- 控制面板: CFG_SET/CFG_ERASE 命令值与固件枚举互换修复 (0xB7/0xB6)。此前"保存配置"实际触发扇区擦除, 且后续 128 字节配置被固件当作命令流解析 (字段值 0xB9=重启 / 0xBB=变砖), 为面板配置功能全部失效的根因
- USB 跨核竞争消除: 新增 core0_owns_usb 所有权标志 -- 配置模式 / 4k / 6k / 生产模式由 Core0 独占驱动 tud_task() (handleCommand 空闲循环 / other_modes 循环 / program3116 循环自行泵送), 正常模式仍由 Core1 独占; stdio_cdc 内部的 tud_task() 调用不再与 Core1 主循环竞争
- CMD_SET_LED 96 字节接收增加 500ms 超时: 主机半包发送 / 拔线不再永久冻结 Core1 (此前 Core0 持续喂狗导致设备永不自愈)
- CMD_FLASHING 增加二次确认字节 0xA5: 防止命令流错位/误触发的不可逆变砖 (擦除 flash 前 4KB)
- 配置存储: eraseConfigSector 后 currentPage=0xff, 下次保存从 page0 顺序写入 (修复"擦除后保存即丢失"); saveConfigSafe 对 0xff/满页状态先擦除整扇区再写, 消除对非擦除页编程的隐患
- 配置读取: flash 页接受前执行完整 magic+cfgVer+xorSum 校验 (撕裂写自动回退到上一有效页); 新增 sanitizeConfig 语义收敛 (hwVer 1..4, 阈值 1..63, airMin<airMax) 并重算 xorSum 后再保存
- other_modes: 进入时置 game_connected=true 抑制 LampArray, 消除 Core0/Core1 并发驱动 WS2812 PIO 与 HID 端点的竞争
- VL53L0X selfCheck 增加 500ms 超时 (原为无界循环, 依赖看门狗形成重启循环)
- 键盘报告注释修正: report_buf[8] bit7 实际发出 F1 (usage 0x3B), AIR0..5 为 F2..F7
- 防御性加固 (round51 复审): CY8CMBR3116 writeData 增加 count>16 guard; lamp_array clamp_lamp_id/除零保护; readConfigSafe 消除 void* 指针算术; VL53L0X writeReg16/32Bit 窄化转换显式化; GCC -fanalyzer 全量分析 22 个翻译单元无真实告警

### 20260810 round50: MBR3116 触控优化迁移 + 驱动修复 (v1.5.0)

- MBR3116 软件验证: 使用 get_DIFFERENCE_COUNT_SENSOR() 验证芯片触摸判定,振幅分级确认(强信号1周期/中2/弱3),I2C错误恢复(重读一次),滑动加速(邻居活跃跳过二次验证),粘性dip保护(50ms内保持验证状态)
- updateTouch_v2 完整优化管线: 从 updateTouch_v1/MPR121 迁移脉冲拉伸(5ms)、粘性触摸(15周期阈值+12/15 grace)、dip容忍(3周期)、空间滤波(孤立电极+1确认周期)、软件验证
- updateTouch_v1 MBR3116 分支: 新增 else 分支,3芯片(A/B/C)按 12/12/8 电极布局执行软件验证
- BUTTON_STAT I2C 错误处理: v1/v2 均添加返回值检查,I2C 失败时清零(安全默认)
- touchData[0..3] 赋值时序修复: v2 中移至 touchData32[] 之后(与 v1 round45t 一致,消除跨核竞态)
- 驱动修复: cy8cmbr3116.cpp setPointer()/requestData() 返回真实 I2C 错误码(原始终返回0),writeData() 改用栈分配(原 new 无 delete 内存泄漏)
- baseline 修正说明: MBR3116 芯片内部管理 baseline,无需软件校正(round50 验证层补偿残余漂移)
- 字符修复: round41 注释中 +/- 损坏为 ? -> 修复为 +/-
### 20260806 round49: Kalman 滤波 + 预测边缘检测 (v1.5.0)

- Kalman 1D 滤波器: 每传感器独立状态(x/v/p),差分速度(meas-lastMeas)不受预测步影响
- 超前预测: xAhead=x+v*lookahead,速度自适应,slider 上手时保守
- 锁定状态机: IDLE->ACTIVE(inRange)->COOLDOWN(1周期),迟滞 ±5mm,stretch 10ms
- 触发条件: inRange(实际或预测位置在范围内),不依赖速度估计
- Phase 2 位置预测(x+=v)+ 速度衰减(v*=0.85),减少 Kalman 滞后
- 首次测量硬设置(p>=199时 x=meas,v=0),消除初始速度瞬态
- 速度钳位 ±50mm,防噪声极端值
- 审查修复: gotNewData 防双重推进, startTimeout 初始化, stretch 独立于状态
- 构建: Nyanithm_round49_kalman_edge.uf2

### 20260805 round48: ToF 传感器统一优化 (v1.4.0)

- ToF 驱动: 移除 async 状态机,改用原子 i2c_write_read,修复 writeReg32Bit 冗余写
- initToF: 独立 I2C 地址(0x30+i)+ mux 回退,12ms timing budget,I2C 提速 400kHz
- updateAir: 确认 1 周期/预测 10mm/拉伸 10ms/范围验证,sensorCount 动态适配
- ControlPanel: 27/32 寸下拉切换,tof4 动态 UI,heightRange 可配置
- 代码质量: checkHardwareState 修复,v2 新增 i2c_write_read,HID 1ms 轮询
### 20260803 round47b: NCLR 回退 + 审查风险修复 (v1.4.0)

**round47: 硬件触摸期冻结 + 软件单写者**
- FDLT=0xFF 冻结触摸期间 baseline（MPR121 不再下漂），NCLR=14 加快释放回升
- 自动校准新增 bl_reg 合理性校验（bl_reg 与 filt>>2 差 >15 LSB 视为损坏读，跳过写入）
- 删除 maindev_loop 死代码（chuni_io.cpp，含无 seqlock 读 touchData32）

**round47b: NCLR 14→31 回退**
- 实测 NCLR=14 使 idle 噪声尖峰 (~14ms) 可推高 baseline，NCLF=127 下降极慢导致棘轮效应
- 回退至 round34 已验证值 NCLR=0x1F（31 个连续样本 ~31ms 才推高 baseline）

**round47b-patch: 审查风险修复**
- filt==0 验证后门加固：重读 filt2，仍为 0 则跳过（防 I2C 连续失败绕过验证层）
- seqlock 5ms 超时撕裂读修复：hid_task 超时 return（跳过本周期 HID，游戏保持上一帧）
- CMD_GET_INPUT 超时路径 air 变量一致性修复（Gibbs 审查）
- 注释修正：STRETCH_MS "covers main-loop peak" → "smooths normal-speed releases"
- 构建：Nyanithm_round47b_patch.uf2 (137728 bytes)

### 20260802 round47-review: 安全审查修复 (v1.4.0)

- 全固件审查：自动校准 bl_reg 校验、FDLT 冻结、单写者 baseline
- seqlock 有界自旋保留（Core0 卡死时 Core1 不阻塞 >5ms）
- 构建：Nyanithm_round47_hwfreeze.uf2 (137216 bytes)

### 20260802 monitor_chain3: 触发事件实时终端报告 (脚本)

- monitor_chain3.ps1：实时终端报告触发事件，快照 8 帧→4 帧（I2C 突发减半）

### 20260801 round46j-v: 防塌陷/I2C 防御/seqlock 超时/flick 优化 (v1.4.0)

**round46j: 统一阈值 + 配置模式 + WDT + seqlock**
- UNIFIED_THRESHOLD_V1=1：所有电极使用全局 th_touch/th_release（ConfigApp 可调）
- seqlock touchStateGen：消除 Core0/Core1 跨核撕裂读
- WDT 看门狗 + I2C 5ms 超时 + 0xC1/0xC2 诊断命令
- round46j-fix1：冻结增强（3s 周期 >5s 长按 diff<bth+2 时写 filt+bth+4）
- round46j-restore：回退到 46j 基线（删除防塌陷/恢复器/sticky 三档/100ms 验证窗口）

**round46k: baseline 主动防塌陷 + 释放后恢复**
- 按住 >3s 的电极每 2s 重置 baseline = filt+bth+4（不等塌陷）
- 释放后 0.3-10s 窗口轮转检查 idle diff < bth → 重置 baseline

**round46l-n: 防塌陷写入频率/安全化**
- 46l: 事件驱动防塌陷（仅 touch bit 翻转时写入，非每周期）
- 46m: 写入频率限制（最小间隔 50ms）
- 46n: 写前重读确认（防 I2C 损坏样本写坏 baseline）

**round46o-q: Air 预测/验证窗口/恢复器**
- 46o: AIR_PREDICT_MM 15→10，恢复 approach>=gap 条件
- 46p: 验证保留窗口 100ms→50ms
- 46q: 释放后恢复器修复（冲突检测 + 写前验证）

**round46r-s: 防塌陷冲突 + flick 优化**
- 46r: 防塌陷与恢复器冲突修复（写前检查恢复器是否活跃）
- 46s: flick 释放延迟优化（sticky 三档→两档）

**round46t: seqlock 5ms 有界自旋**
- CMD_GET_INPUT / CMD_DEBUG_CHAIN / HID 的 seqlock 读取加 5ms 上限
- 超时返回最近状态（不阻塞 Core1）

**round46u: I2C 损坏样本防御**
- 防塌陷 + 恢复器写前重读 filt，两次差值 >3 LSB 视为损坏 → 跳过写入

**round46v: 防塌陷写点抬高**
- 写窗口 [bth, bth+8) → [bth+2, bth+10)，写后目标 bth+12 → bth+14
- 写前最低 diff 13 > 置位边界 10 → 长按稳定

### 20260731 round46a-i: 统一阈值构建 + 配置模式修复 (v1.4.0)

- round46: UNIFIED_THRESHOLD_V1=1 统一阈值 + seqlock 快照读 + WDT
- 46b: 0xC2 bulk I2C 诊断（[AA][55] sync + 44B payload）
- 46c-f: 配置模式 USB/stdio/idle exit 修复
- 46g-h: CMD 日志 + reboot 修复
- 46i: diff 计算修复（filteredData vs baseline 对齐）

### 20260731 round45n-u: sticky/dip/air/slide/跨核竞态 (v1.3.x)

**round45n-o: sticky hold + flick 优化**
- sticky 机制：触摸后保持 STRETCH_MS=3 防微小断触
- flick 释放边沿优化

**round45p: baseline 冻结**
- 触摸期间冻结 baseline（NCLF 不再下漂），STICKY_GRACE_SHORT=8/STICKY_GRACE_MAX=15

**round45q-s: Air 确认 + slider 门控 + 方向门控**
- Air 确认：正常 2 周期 (~5ms)，手在 slider 上 3 周期 (~7.5ms)
- Slider 门控：有触摸活跃时需更多确认
- Air sticky + 方向门控（预测只在手上升时触发）
- AIR_PREDICT_MM=10, AIR_STRETCH_MS=15

**round45t: 跨核竞态修复（phantom touch 根因）**
- touchData[0..3] 赋值移到 touchData32[] 之后（最小化竞态窗口）
- Core1 HID 读到混合新旧数据的概率极低

**round45u: slide miss 修复**
- 最终调优版本，UF2=135680 bytes

### 20260730 round45a-m: 振幅分级/电极阈值/dipGrace/baseline 冻结 (v1.3.x)

**round45: 振幅分级确认 + 波动容忍**
- 方案 A：diff 振幅替代时间盲判（th+4 → 1 周期，th+2 → 2 周期，<th+2 → 3 周期）
- 方案 B：dipGrace 缓冲（DIP_TOLERANCE_CYCLES=2，容忍短暂波动不清零 touchCount）
- 硬件 dt 保持 1，不牺牲快速音符捕获率

**round45b: 电极级差异化阈值**
- 7 个高危电极单独设高 touch threshold（M2E0=16, M2E3=12, M2E1/M0E5/M0E6=11, M1E7/M2E2=10）
- idle diff max 分析（423 idle 样本），真实触摸 diff 20-50 不受影响

**round45c-f: dipGrace 移除/stuck-key/stucklog**
- 45c: 移除 dipGrace（修复卡键/占用未释放）
- 45d: stuck-key 快速恢复
- 45e: stuck log 诊断
- 45f: 移除诊断代码精简

**round45g-h: 阈值调优**
- 45g: M2E0 阈值 16→15（降低 miss）
- 45h: 阈值微调

**round45i-l: baseline 修复/dipGrace 精调**
- 45i: baseline 修复
- 45j: 恢复版本 + 空间滤波
- 45k: NCLF 修复
- 45l: dipGrace 精调

**round45m: hold 修复**
- 长按保持优化

### 20260730 round44: 振幅确认旁路 + debounce (v1.3.1)

- 退回 MPR121 debounce 到 dt=1（用户可在 ConfigApp 自行调整）
- 移除 dt>=3 强制检查（dt=3 经实测造成快速音符 miss）
- 振幅感知确认旁路：diff >= sw_th+4 标记为强信号，1 周期确认 (~0.5ms)
- 弱信号仍需 CONFIRM_CYCLES=2 (~1-2ms)
- 效果：真实触摸延迟 ~2ms（比 dt=3 的 5ms 少 3ms），噪声尖峰仍被过滤
- 构建：UF2=131584 bytes

### 20260729 round32-34: baseline 偏高乱触修复 (v1.2.x)

**round32: MPR0 ELE3 持续乱触**
- 根因：baseline 比 filtered data 高 4 LSB，diff 恰好等于 th_touch=4
- th_touch: 4→6, th_release: 3→4（真实触摸 diff=30-37 不受影响）

**round33: 根治 baseline 偏高**
- calibrateBaseline baseline -= 1 LSB 余量
- NCLF 127→63（下降追踪 63.5ms→31.5ms）
- CONFIRM_CYCLES: 2→3

**round34: NCLR=31 + MHDF=1**
- NCLR=0x1F（31 个连续样本才推高 baseline，抑制噪声尖峰）
- 构建：UF2=129024 bytes

### 202607282340 round28-31: I2C 竞争/波特率/CL 值 (v1.2.x)

**round28-29: I2C 竞争条件诊断**
- CL 值校准（CL=11 修复），ECR 配置修正

**round30: I2C 波特率 400kHz→200kHz**
- 降低波特率提升可靠性

**round31: I2C 原子 write_read**
- 根因：touched()/readRegister 的 write+read 是两个独立 I2C 事务，Core0/Core1 交错导致寄存器指针被改变
- 新增 i2c_write_read() 原子操作（单个 spinlock 内完成 write+read）
- touched() I2C 失败时返回 0（无触摸）

### 202607282210 round27: MPR121 初始化参数优化

- debounce(dt/dr)、NCLF/NCLR/MHDF/MNFI 等参数调优
- CONFIG1=0x10 (FFI=6, CDC=16uA), CONFIG2=0x28 (TDT=0.5us, SFI=2, ESI=1ms)
- th_touch=6, th_release=4, debounce=0x11 (dt=1, dr=1)
- 构建：UF2=129024 bytes

### 202607241000

- 修复 ToF 传感器 VL53L0X 初始化时序

### 202607191155

- 支持左右反转
- 修复 CDC 配置模式通信

### 202607191130

- 修复灯光亮度调节

### 202607191101

- 支持切换 4k/6k 模式

### 202607190600

- 烧录固件增加防呆
- 断连检测时间改为 2s
- 支持调节延迟

### 202603161921

- 部署套件初始版本（ConfigApp.exe + chuniio DLL + UF2）

### 202603101301

- 修复 CDC 通信稳定性

### 202603041716

- 优化触摸响应延迟

### 202603021324

- 修复 VL53L0X 测距异常

### 202602072018

- 支持调节灯光亮度

### 202602031615

- 修复 air 传感器误触发

### 202601220447

- 优化 I2C 读取性能

### 202601120058

- 修复配置模式退出

### 202601012233

- 新年快乐
- 修复 CDC 数据解析

### 202512262212

- 修复 ToF 传感器初始化失败

### 202512150206

- 支持配置文件选择手台型号
- 适配 32 寸第五个 ToF 传感器
- 修复触摸阈值只对 mpr0 生效的问题
- 支持空闲时禁用 Air 输入
- 空闲时炫彩灯光

### 202512052357

- 修改配置信息结构体定义，适配 32 寸
- 不兼容旧固件、配置

### 202511252235

- 支持 32 寸的额外一个 ToF 传感器

### 202511160247

- 支持选择是否启用 Slider 的键盘输入
- 调整灯光测试变色速度
- 移除 ToF 传感器超时检查
- 设备上电时自检
- 进入警告状态时按下拨轮可强制跳过

### 202511140027

- 支持切换为 CY8CMBR3116 触摸
- 进入调试模式时默认不开启 HID 上报

### 202510290104

- 支持设置灯珠数量，兼容 16 灯固件

### 202510260046

- 游戏内自动重置设备，尝试解决 ToF 延迟问题

### 202510242223

- 支持调节去抖

### 202510241912

- 新增拨轮映射到功能键（投币, 刷卡, TEST）

### 202510210000

- 初始版本
- 3x MPR121 (32 slider cells) + VL53L0x ToF air sensors
- USB composite device: HID keyboard + LampArray HID + CDC serial
- 基于 Pico SDK 开发，部分芯片驱动修改自 Arduino 库
  - Adafruit_MPR121 (BSD)
  - vl53l0x-arduino (Pololu)
  - CypressCY8CMBR3116 (MIT)
  - Pico_WS2812
